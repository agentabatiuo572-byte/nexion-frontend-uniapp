# Project Instructions

## UniApp GitHub Sync Rules

- After each completed task, ask the user whether to sync changes to GitHub.
- Before syncing to GitHub, check local and remote file differences.
- If there are file differences that would be overwritten, ask the user before overwriting.
- Do not overwrite remote repository contents without explicit user confirmation.
- Do not create a new branch unless the user asks for one.
