# 赚取手机卡视觉验收（2026-08-25）

## 验收范围

- 参考实现：`http://127.0.0.1:5174/?nx_device_inner=1#/pages/earn/earn` 的 5174 手机设备展开卡；仅作为视觉层级、颜色、弱分隔和交互密度参考。
- 正式实现：`http://127.0.0.1:5173/?nx_device_inner=1#/pages/earn/earn` 的真实 Java 数据手机卡。
- 共同截图规格：1280 × 720；当前运行内由应用内 Browser 在同一轮分别捕获展开卡上半区与下半区，并在同一比较输入中并排复核。
- 数据约束：5173 不复制 5174 Mock 收益；手机当日 5 笔服务端收据各 `$0.012`，合计 `$0.06/天`。

## 对比证据

- Source capture：当前运行的 5174 展开态截图，页面滚动容器 `.nx-content` 分别位于上半区和 `scrollTop=850`。
- Implementation capture：当前运行的 5173 展开态截图，页面滚动容器 `.nx-content` 分别位于上半区和 `scrollTop=900`。
- DOM 证据：手机卡显示 `今日已完成（5）`，`[data-phone-today-task="true"]` 精确计数为 5；每行 `$0.012`，点击 `查看全部` 后进入收据页并展示对应 5 张服务端收据。

## 全页与重点区域检查

| 表面 | 验收结果 |
| --- | --- |
| Typography | 复用 5174 的设备卡标签字号、辅助色、等宽数字与绿色收益色；标题为弱标题，任务模型优先、工作负载次级。 |
| Spacing | 新区块位于“当前任务”之后、“后台模式”之前；沿用 20px 横向内边距、弱分组留白和 28px 最小行高，5 行不产生横向溢出。 |
| Colors | 继续使用 `--v5-ink-*`、`--v5-brand`、`--v5-success-ink`，未引入新的硬编码主题色。 |
| Assets | 未新增图片或图标资产；保留 5174 设备卡既有图标、圆角、分隔线和展开层级。 |
| Copy / Content | 标题数量动态来自当天服务端任务，不硬编码 5；每行展示模型、工作负载、收益和上海业务时钟；`查看全部` 进入完整 Proof-of-Compute 收据页。 |

## 迭代记录

1. 首次真实页面检查发现新区域为 `今日已完成（0）`，而页底历史已有 5 笔当日收据。
2. 根因是 Java `LocalDateTime` 无时区字符串被浏览器按本机 UTC+09 解析，上海业务日被移到前一天；先补失败测试，再在任务 API 边界把无偏移时间按 UTC+08 解析。
3. 修正后手机卡稳定显示 5 行，每行 `$0.012`，合计与卡头 `$0.06` 一致；设备映射同时传播 Java `serverNow`，上海业务日不再回退到浏览器本机时钟。
4. Java 启动归一化把 8/20–8/25 的既有 DEV-HOME 任务和收据统一重绑到当前手机设备；两页历史共 30 条，均为 `$0.012`，未再出现旧 `$323.89`。
5. `查看全部` 实际点击进入 `/pages/me/receipts`；当天 5 张与全部 30 张手机收据均可见。

## 交互与运行检查

- 设备卡展开/收起正常；`查看全部` 具备 `role="button"`、`tabindex="0"`、Enter 与 Space 键激活契约。
- 5173 与 8110 在目标源码编译后重启；重新加载并通过真实开发 Passkey 会话复验：今天 `$0.06 / 5`、本周 `$0.12 / 10`、本月与全部 `$0.36 / 30`。
- 目标 Vitest 19/19、后端 JUnit 7/7、H5 production build 均通过。

## 对抗复审

- 收益/历史复审：初审 98.2/100、复审 99.1/100；数据库核对 task 与 receipt 各 30 条、总计 `$0.360000`、异常 `$323.89` 为 0。
- UI/交互复审：初审 98.7/100、复审 99.2/100；P0–P3 无遗留问题。
- 服务门禁复审：初审 98/100、复审 99/100；5173、8110、3002、9000、Gemma/RAG 均健康且项目归属正确。

## 结论

- 未发现 P0/P1/P2 遗留问题。
- 5174 的 Mock 数量与金额不作为正式数据标准；本次只复用其视觉语言，正式收益与任务完全来自 Java 收据权威。

final result: passed

---

# 算力市场视觉验收

## 验收范围

- 参考实现：`http://localhost:5174/#/` 的首页算力市场区域。
- 正式实现：`http://127.0.0.1:5173/#/` 的首页算力市场区域。
- 状态：已通过开发登录、完成 Legal Terms 确认、Java 首页接口返回六类 PC 后台任务数据。
- 共同截图规格：1280 × 720，浏览器默认设备像素密度；两张图使用相同手机画布与首页滚动位置。

## 对比证据

- 参考截图：`C:\Users\72921\.codex\visualizations\2026\08\19\01a0196a-e747-7811-80e9-c5b5a7d28d4a\market-ui-audit\5174-compute-market.png`
- 实现截图：`C:\Users\72921\.codex\visualizations\2026\08\19\01a0196a-e747-7811-80e9-c5b5a7d28d4a\market-ui-audit\5173-compute-market-after-aligned.png`

## 全页与重点区域检查

| 表面 | 验收结果 |
| --- | --- |
| Typography | 标题、辅助文字、表头、主价格和单位已沿用 5174 的层级与对比度；缺失值统一使用 `—`。 |
| Spacing | 标题行、卡片内边距、六行密度、列间距、分隔线和圆角节奏与 5174 对齐。 |
| Colors | 黑色页面、深灰卡片、绿色操作/标签和中性缺失态与参考实现一致；正负涨跌仍可由符号识别，不只依赖颜色。 |
| Assets | 本区域没有位图素材；有真实曲线时复用现有 `HomeSparkline`，接口无曲线时显示文本缺失态，不注入 mock 图。 |
| Copy / Content | 表头为“类型 / 模型 / 1h / 价格 / 24h”；名称、价格、单位来自 Java 接口，接口未提供的成交量、曲线、24h 变化明确显示 `—`。 |

## 迭代记录

1. 初始差异（P1）：5173 的真实接口分支退化为两列列表，5174 为五列行情表，信息结构与行密度明显不一致。
2. 修正：移除正式 App 内失效的硬编码 mock 行情分支，所有真实行统一进入五列表格；任务代码仅映射为稳定的短标签。
3. 数据校正：修复 PC 后台单位已含 `/` 时出现 `//job` 的展示问题，并补充回归测试。
4. 对抗初审发现 Java 名称为空时曾回退显示 `IG` 等技术代码；已改为 `—`，保证任务代码只作为左侧视觉标签使用。
5. 最终对比：未发现 P0/P1/P2 视觉问题。5173 与 5174 的行文内容不同是预期约束——前者展示 PC/数据库/Java 的真实任务数据，后者只作为 mock 视觉参考。

## 交互与运行检查

- 键盘 Enter 激活 `IMG 图像生成` 行，成功进入“赚取”页的真实行情看板；底部“首页”Tab 可返回首页。
- 首页重新加载后仍显示六条 Java 接口数据；没有出现错误遮罩、白屏或组件崩溃。
- 运行环境未提供独立 console-message 导出接口，因此以页面错误事件外显、DOM 快照、主交互和开发服务日志共同检查；未发现与本次组件相关的运行错误。

## 对抗复审

- 初审首次为 96/100，发现名称为空时错误回退技术代码；修复后重新初审为 99/100。
- 独立复审为 99.4/100，P0–P3 均无遗留问题。

final result: passed
