# Nexion 前端 · 产品更新日志

> **范围**：前端 app（`Nexion-uniapp`，dev :5173）的功能更新。运营后台日志在 `Nexion-admin-prototype/docs/后台产品更新日志.md`。
> **目的**：对齐产品进度——每次的新增 / 修改 / 移除功能记录在此。
> **图标**：🆕 新增 · ✏️ 修改 · 🗑️ 移除。**格式**：标题 → 概述 → 要点 → 入口 → PRD 章节。**顺序**：最新在上。
> **维护**：功能任务收尾在最上方当天日期下追加一条；同一批次的纯 UI / 视觉打磨合并记一条「视觉打磨集」，不逐处单列。

---

## 2026-07-14

### ✏️ 风控参数默认口径对齐 PRD(同收款工具上限 2 · 冻结建议线 0.7)

**概述**:同一收款方式关联账号上限默认值由 1 修正为 2、风险簇冻结建议线由 0.82 修正为 0.7,对齐后台 PRD K1③ 权威口径(原 seed 为镜像抄错值)。规则语义不变:超上限仍是强关联标记入簇,冻结建议线仍只作建议与提现分诊,不自动冻结。(本条 07-14 首记,07-15 因工作树 reset 丢失后重放。)

**要点**:
- 与运营后台参数寄存器 defaultVal 同源;verify 新增双端「值 parity」哨兵(16 个风控/验证码闸参数逐键比值,「键在但值抄错」类漂移以后机器拦)。
- 用户侧实际影响:两个账号共用同一收款方式不再触发强关联标记(第 3 个起才触发);提现风控升人工分诊的强度线 0.82→0.7(更保守)。

**入口**:无界面变化(风控引擎与提现分诊内部参数)。
**PRD 章节**:后台 PRD K1③ / SPEC-7 参数寄存器。

## 2026-07-13

### 🆕 银行卡解绑 / 换绑引导两条推送模板 + 解绑即时通知

**概述**:配套运营后台新上的「银行卡查询与解绑」能力,前端新增两条通知模板:①「银行卡已解绑」(普通优先级,后台解绑或用户自己解绑都会收到,点通知直达绑卡页)②「请更换试用担保卡」(高优先级,运营发送换绑引导时收到,提示绑新卡以保障试用结束顺利完成购买)。用户在绑卡页自己解绑(含试用担保卡经挽留仍解绑)现在会**即时收到解绑通知**进通知中心。

**要点**:
- 模板为注册形态(builder),文案双语六键镜像(`notifs.cardUnbound*/cardRebind*`),深链 `/pages/me/wallet-cards`;换绑引导无前端触发源,mock 期由后端事件桥/演示入口调用,生产由后端在 `card.unbound` / `card.rebind_notified` 事件流上触发。
- 解绑通知按卡号幂等(解绑是终态,事件重放不重复入 feed);换绑引导可重复发送(每次新条目)。
- 触发接线覆盖两条解绑路径:常规解绑确认、试用担保卡挽留面「仍要解绑」。

**验证口径**:vue-tsc 0 错;verify 240 项全绿(合并+接线后主工作树重跑);browser 实景 en/zh 各走一轮完整链(真绑卡→真解绑→通知入 feed(幂等 id/优先级/文案/深链字段逐项断言)→通知中心渲染→点通知深链回绑卡页→已读标记),console 全程 0 error。挽留面解绑路径与常规路径共用同一模板与推送调用,代码级核对(独立试用担保场景未另搭)。

**入口**:我的 → 钱包 → 银行卡(解绑后见通知中心);通知中心(`/me/notifications`,system 类)。**PRD**:前端 v3.7 §11.2.5(已同步);后台侧见运营控制后台 PRD v1 Ch5 [C1·deepening] 支付方式。

### ✏️ 登录 / 注册区号选择器移动端化

**概述**:手机号区号从窄小的桌面下拉框改为移动端底部选择面板。登录与注册共用同一组件,选项行统一为 56px 触控高度,支持遮罩和关闭按钮退出、当前区号高亮与勾选、列表滚动且不显示桌面滚动条;国家名和辅助功能标签完整支持中英文。

**入口**:登录页(`/pages/login/login`) + 注册页(`/pages/register/register`)。**PRD**:纯 UI / 交互可用性优化,不调整功能契约。

### ✏️ 首日任务「连接钱包」改为「绑定银行卡」并接通触发发奖

**概述**:首日新人任务第 1 项从「连接钱包」改为「绑定银行卡」(任务 id `connect_wallet` → `bind_bank_card`),点任务直达绑卡页;原任务一直没有完成判定(点了也永远完不成),本次一并接通——用户在绑卡页成功绑一张银行卡,任务即时完成并入账 +50 NEX。运营后台 H3 首日任务默认配置同步对齐(名称「绑定银行卡」、跳转 `/me/wallet/cards/new`、完成事件 `card.bound`)。

**要点**:
- 任务行标签/跳转:zh「绑定银行卡」/ en "Link bank card",点击 → 绑卡页(`/pages/me/wallet-cards-new`),不再指向充值页。
- 触发接线:绑卡成功 → 幂等发奖(首次 +50 NEX 入余额 + 账单 bonus 行「首日任务 · 绑定银行卡」+ 任务完成 toast);再绑第二张卡只出常规绑定确认,不重复发奖。试用流里绑卡同样计入(绑卡即绑卡)。
- 老用户存量:历史已完成的 `connect_wallet` 记录成为废弃键,不崩溃、不误计数;「绑定银行卡」按新任务重新可完成。
- 后台:H3 任务清单 t0 seed 三字段对齐前端真实路由与事件;运营在面板上的增删改/启停能力不变。

**验证口径**:双工程 tsc 0 错;uniapp verify 240/240、admin verify 244/244;Playwright 实景 17/17(zh/en 标签、点击跳转、真绑卡发奖入账、二次绑卡幂等、admin 面板 seed 渲染),旧存量数据注入场景 3/3,console 全程 0 error。

**入口**:首页首日任务卡第 1 项;绑卡页(我的 → 钱包 → 银行卡 → 添加)。**PRD**:前端 v3.7 首日任务章节 + 后台 H3 章节需同步(待主人确认后走 prd-sync / admin-prd)。



### 🗑️ 移除用户评价功能(前端 + 运营后台全线下线)

**概述**:按主人指示,把「用户评价」从产品里整体拿掉——前端商品详情页的评价区(平均分 + 星级分布条 + 3 条买家评价 + 「查看全部 N 条评价」),以及运营后台 E1 的整套评价管理(用户评价表、筛选/翻页、新增/编辑/隐藏/删除评价抽屉、SKU 表单里的「评分/评论数」两个字段)一并删除。商品的销量、库存、媒体背书(Trusted by)、FAQ 等其余社会证明不受影响。

**要点**:
- **前端**:详情页删 Reviews 区(评分汇总 + 分布条 + 评价列表),删 RatingBar 组件(已 Move .trash),商品数据模型删 rating/reviews 两字段(6 款 SKU 全部),i18n 删 4 个评价文案 key(en/zh 对称);删后 AI 性能表直接衔接 Trusted by,布局无缝。
- **后台**:E1 删用户评价表 + 评价抽屉 + 评价 API 面(client 函数 / 代理路由 / 本地 mock 端点 / store 类型与动作全链);SKU 卡不再显示 ★评分与评价数;SKU 抽屉「营销 & 社会证明」组只留累计销量 + 库存。store 持久层版本升 v9,老浏览器缓存里的评价数据与 SKU 旧字段自动清理重建。
- **哨兵同步**:SKU 字段镜像门去掉 rating/reviews 映射;动作完整性门 e-view 死控件基线 2→1 收紧;E2 任务列表复用的评价表翻页样式改通用名 `list-pager` 保留(避免误伤)。

**验证口径**:两工程 tsc/vue-tsc 0 错;uniapp verify 240/240 全绿;admin verify 与本次相关门全过(4 个存量挂门经 A/B 证伪与评价无关——用改动前代码复跑同样挂);browser 实景:详情页/商城页/后台 E1 页 console 0 error,评价区/评价表/评分字段运行时确认消失,SKU 抽屉 ①-⑦ 字段组结构完好。

**入口**:商品详情页(`/store/detail`)评价区已移除;后台 E1(`/devices/pricing`)评价管理已移除。**PRD**:前端 v3.7 商品详情章节 + 后台 PRD E1 章节需同步删评价描述(待主人确认后走 prd-sync / admin-prd)。



### 🗑️✏️ 产品完整性打磨:删 2 偏离设计 + 补 5 缺口

**概述**:按《产品完整性核查对照表》逐条对照真实代码打磨——删掉 2 处偏离设计(凭空发钱 / 空壳 tab),补齐 5 处「产品自己知道没做完」的缺口。删:①创世节点挂单每 6 秒 18% 概率被系统凭空成交给卖家打真钱(用户间二级交易根本无真实买家,代码注释自认「生产环境由服务端撮合、非客户端赌概率」)——保留用户主动买二级节点 + 自己挂单/取消;②Learn/教程 tab 整个(15 节课纯空壳,归后期教学阶段)。补:①组合购结算从只弹 toast 接成真下单;②活动中心 2 个死 CTA 补跳转;③NEX 加减钱补防脏数据守卫;④Monthly Challenge 占位卡收掉;⑤提现劝阻卡 APY 焊到质押主表单源。

**要点**:
- **删① 18% 随机成交**:删 `App.vue pollOrders` 的 Genesis resale 段 + 连带死代码(`fulfillSale` action、`GENESIS_ROYALTY_RATE`/`GENESIS_SALE_CHANCE` 常量、2 个 toast i18n);保留 `acquireSecondary`(主动买)+ `listNode`/`cancelListing`(挂单管理)。挂单从此不会被凭空成交(诚实:无真实买家)。
- **删② Learn tab**:5 个专属文件(页面+3 卡片组件+mock)Move .trash;清路由/header 标题映射/搜索入口/i18n(命名空间+标签+副标 en/zh 对称)。回源发现 Learn 非底 tab、唯一入口是搜索项,删后搜「learn」已无结果。
- **补① Monthly Challenge**:收掉「即将上线」死占位卡(section+专用样式+i18n);连带清 hero 宣传文案里的「月度挑战」(否则宣传已删功能);发现的 `monthlyChallenge` 完整死文案库(Q-2 预留、零引用)主人定保留作将来 port 素材。
- **补② 组合购结算**:`bundle.vue onCheckout` 余额直付——复用单品 checkout 下单内核(扣款+建单+账单+余额不足拦截),一次下 N 单、组合折扣按单价分摊、清空组合车、跳订单页。
- **补③ 活动死 CTA**:Regional PK「加入竞赛」→排行榜、Re-invest「立即复投」→复投页(补 href,复用活动卡既有导航)。全站排查确认无第 3 个死 CTA(upcoming/ended 按状态门本就不渲染按钮)。
- **补④ NEX 守卫**:`creditNex`/`debitNex` 补 NaN/负数守卫(对齐 `creditBalance`);防 `debitNex(-x)` 因「余额<负数」判 false 反向增币、脏 amount 污染余额成 NaN。verify 焊 `dirty-amount ×4` 哨兵。
- **补⑤ 劝阻卡 APY**:核查表「5/12/20 不一致」实为误报(实际 12/35/80、Round-7 已 match);改劝阻卡读 `STAKING_APY` 主表单源(消除硬编码镜像、根治未来漂移),verify 焊 `apy single-source` 哨兵。

**验证口径**:每件走 tsc 0 + verify.sh(238→240,新增 dirty-amount×4 + apy-single-source 2 哨兵)+ browser 实景 + done-review 六维。重点运行时证明:删① behavior-verify 四页 live poll 0 error + genesis/marketplace 主动买保留;删② 搜 learn 无结果;补② e2e 账本精确——加 2 台设备($649+$1199,组合价 −5%=$1755.6)点结算,余额精确 24856.56→23100.96、2 订单、账单 −1755.6 三处单源一致;补③ 2 CTA 实景真跳转;补⑤ 输入 $1000 劝阻卡显示 $1010/$1086/$1395(12/35/80% 派生)。console 全程 0 error。

**入口**:创世页/二级市场(`/genesis`)· 任务中心(`/missions`)· 商城组合购(`/store/bundle`)· 活动中心(`/events`)· 提现页(`/me/wallet-withdraw`);Learn tab 已移除。**PRD**:多处涉及(二级交易叙事收窄 / Learn 移除 / 组合购结算 / 活动 CTA / 提现劝阻口径,待主人确认后同步)。

### ✏️ 修复试用「提前购买」扣款失败仍记账单(账单一致性)

**概述**:试用期内点「立即购买」提前兑换设备,页面先做余额预检查、再弹确认框。但确认框停留期间若余额被别处(多端/其它购买)消耗光,原代码扣款那步没检查是否扣成功,会照常记一笔「已购买」账单并发设备——出现钱没扣却记了账单的账不平(非偿付性,审计相邻发现)。本次改为:扣款失败即报错中止、不记账单,并把试用状态纠正为「失败」(否则会卡成页面显示已兑换、实则没钱没设备的死状态)。

**要点**:①扣款调用检查返回值,false(余额不足)时报错 return,后续记账单/加余额/发设备全不执行;②因试用状态在扣款前已被 `redeemEarly()` 推进到「已兑换」,失败分支同步翻「失败」并保留冷却起点(对齐自动兑换路径 App.vue 的既有处理);③全站 12 处扣款调用同形排查,本处是唯一漏检查、已补;verify 新增 1 条哨兵(`debit_return_checked`)防未来任何扣款调用丢弃返回值复发。

**验证口径**:vue-tsc 0 错 + verify.sh 238/238(含新哨兵);运行时真代码实景两路径——(并发失败)确认框停留期间扣光余额→不记账单+余额不变(未扣)+试用转失败+报错「余额不足」;(正常)余额充足→记账单 -$629+发设备+扣款+成功提示;独立 code-reviewer 复审 APPROVE(root cause,0 高危);console 0 error。

**入口**:试用页(`/me/trial`)提前购买。**PRD**:不涉及(纯防御修复,不改产品规则)。

### ✏️ 修复提现可把总余额提成负数(凭空取钱的 P0)

**概述**:此前买任何东西(创世承接 / 商城下单 / 质押 / 试用 / 兑换 / 复购)都从总余额扣款,但「可提现额度」不跟着减;而提现只校验可提现额度、不校验总余额,于是买过东西后可提能提走比实际余额更多的钱、把总余额提成负数。审计另发现**同一症状的第二条路径**:同一账号开多标签页/多端时,各自本地合法地买东西,账号快照合并会把两笔扣款独立累加,也能把总余额直接扣穿为负——两个独立审查用可执行脚本复现坐实。本次**两条一并根治**:①花钱时可提现额度同步收敛到不超过剩余总余额、提现加一道总余额兜底门;②多端快照合并在写盘前统一收口资金不变量(余额不为负、可提额度不超总余额)。

**要点**:①扣款侧修在所有购买共用的扣款原语一处(花钱先消耗不可提的充值本金,花穿了才动可提收益),一处覆盖全部购买链;提现门增加总余额校验作纵深防线。②合并侧——多端账号快照合并层(account-cloud)在写盘前统一过资金不变量收口:余额字段 clamp≥0、可提额度 clamp 到不超总余额,补住扣款原语单会话 clamp 管不到的多端 merge 旁路(真后端单事务天然无此问题,mock 层收口兜底)。「充值进总余额不进可提额度(充值不可直接提现、收益才可提)」产品口径不变。verify 新增 4 条防回退哨兵。

**验证口径**:vue-tsc 0 错 + verify.sh 236/236(含 4 条新哨兵);两个独立审查各用可执行脚本对抗证伪旁路;运行时真代码实景——(单会话)承接 13400 后可提额度从 24856.56 收敛到 11456.56、提现到 max 后总余额=0 不负、写盘+刷新不回弹;(多端)模拟两端并发各扣 24000、合并本会算出 -23143.44、被收口成 0 且落盘同步为 0;人为构造可提额度虚高时提现被总余额门拦;NaN/负数/超额的扣款与提现均拒(提现函数补 store 层金额有效性守卫)。console 0 error。

**入口**:全站提现(`/me/wallet-withdraw`)+ 所有购买链扣款。**PRD**:提现校验口径 / 资金桶不变量(待主人确认后同步)。

### ✏️ 存储作用域整改第二批:28 个账号资产 store 全部按账号隔离(彻底根治换账号继承)

**概述**:承 2026-07-09 创世/V 等级的账号隔离,本批把余下仍是「设备级单键」的用户资产 store 全部改为按账号分行(连同前批共收口 28 个),彻底根治同一浏览器/设备换账号登录时私人资产互相串号。覆盖:订单 · 账单 · 质押持仓 · 佣金事件 · 券包 · 试用状态机 · 兑换记录与风控计数(含 KYC/日限/终身额)· 绑卡 · 签到 · 钱包配对 · 任务/周任务/活动任务 · 里程碑 · 成就 · 目标 · 幸运转盘票据 · 每日增益 · 通知 · 算力凭证 · 工单 · 购物车 · 资料 · 安全设置(2FA)· 奖励已读水位线。换号后每个账号只见自己的数据;平台态(创世售出进度 / 兑换市场汇率 / 转盘降级 flag / 运营配置)仍全端共享。

**要点**:统一走 `lib/account-scope.ts` 的 `rebindAccountScopedStores()` 收口(登录/注册/冷启动三入口自动继承,遵守 store 不互 import 铁律);每个 store 判「用户资产 vs 平台态 vs 设备偏好」三分,只迁用户片(exchange 市场汇率、exchange-v3 平台日计数、转盘降级 flag 等平台态留全局或标注 ponytail);旧设备级存量不臆断迁移(mock 可重建),旧键就地废弃;nex-faucet 顺带废除旧积分 legacy 迁移路径。修复审计抓出的真跨账号泄漏:wallet-exchange 页把设备级钱包配对态镜像进账号级 KYC 字段 → 把钱包配对 store 一并按账号隔离,根治 KYC 换号继承。新表结构 backend-replaceable(accountKey 即用户主键,行即 per-user 服务端资源)。

**验证口径**:vue-tsc 0 错 + verify.sh 232/232(新增 P2-8 哨兵 56 条 = 28 store ×〔收口重绑 + store 层 writeAccountRow〕,含 spec6 反泄漏护栏跟踪迁移键;探针验证摘任一行必红);四批各走对抗审计(正确性/时序/跨-store 镜像/残留多镜头证伪),抓出并修复 2 类真问题(KYC 镜像泄漏 P1、spec6 护栏盯死键静默失明 P1)+ 补齐收口墙缺口;Playwright 实景 A/B/A 切号:四批 28 store 逐一「登录 A 产数据 → 切 B 断言干净零继承 → 切回 A 断言还原」,持久层确认按 `{[accountKey]:{…}}` 分行、零跨账号泄漏、rewards-seen 红点单源按账号、KYC 经镜像路径复验堵死;console 全程 0 error。

**入口**:全站所有 per-user 页面(订单/账单/质押/佣金/券/试用/兑换/绑卡/签到/任务/成就/通知/工单/购物车/资料/安全 等)· 登录/注册/冷启动重绑收口。**PRD**:账号数据归属口径 + 各 store persist key 更新(待主人确认后同步)。

---

## 2026-07-09

### ✏️ 创世持仓与 V 等级按账号隔离(修复换账号继承他人资产)

**概述**:同一浏览器/设备上换账号登录,创世节点持仓(持有数/席位/二级挂单)与 V 等级(等级+晋升进度)此前是设备级全局存储,会被新账号原样继承——新注册账号凭空高等级、创世限购额度被前一账号占用、创世持有页显示别人的席位。本次改为按账号隔离:每个账号只看到自己的持仓与等级,换号互不串;创世的全平台售出进度与上所信号保持全端共享不受影响。

**要点**:存储由设备级单键改为按账号行表(创世用户片 `nexion-genesis-accounts-v1`、等级 `nexion-v-rank-accounts-v1`),全平台市场态留原键;账号切换收口 `lib/account-scope.ts` 的 `rebindAccountScopedStores()`,登录 / 注册 / 冷启动恢复三入口统一重绑(遵守 store 不互 import 铁律,编排放 lib 层);旧设备级存量用户数据无账号归属、不做臆断迁移(mock 可重建),旧键随下次写入自动清理;新表结构 backend-replaceable(accountKey 即用户主键,行即 per-user 服务端资源)。

**验证口径**:vue-tsc 0 错 + verify.sh 177/177(新增 P2-8 哨兵 ×5:重绑收口存在性 ×2 + 三入口调用顺序断言 ×3,探针验证摘行必红);对抗审计 7 维证伪全过、唯一 P1(哨兵缺口)已修;Playwright 实景:A 号登录买创世($11,999 扣款正确、持仓落 A 行)+ 种 V4 → 切 B 号 = 持有页空态 / V2 seed 档 / A 行未被覆写 / 售出进度全局共享 → 切回 A = 1 席 + V4 完整还原;console 0 error。

**入口**:`/genesis/holder` 创世持有 · `/genesis` 预售 · `/team/rank` 等级页 · 登录/注册/冷启动。**PRD**:账号数据归属口径(待主人确认后同步)。

### 🆕 创世节点迁入商城「尊享席位」+ 认购资格制(四通道稀缺门)

**概述**:创世节点不属团队业务,从团队页移除其入口;商城页「更多型号」之后新增独立「尊享席位」压轴分区,专属金色入口卡实时显示当前档价与余席。同时新增**认购资格制**——创世节点不再人人可买:满足四通道任一(累计入金 ≥$5,000 / 持有旗舰设备 / 等级 ≥V4 / 创世邀请码)才解锁认购,一级认购与二级市场承接同门,单账户限购 5 个。不够格用户看到锁定态卡片与「认购资格」明细面板(各条件进度 + 补齐入口:去充值 / 看旗舰 / 去升级 / 输邀请码),资格门槛同时构成全产品线的转化引导。全部阈值后台可配(admin G4 新增「认购资格门」参数卡,8 参数,调整走确认+理由+审计)。

**要点**:四层纵深防御(商城卡态 → 预售页 dock 门 → 购买确认复验 → 账本层单人限购守卫,复验顺序资格→限购→余额→铸造,失败零资金动作);邀请码为按账号隔离的用户凭证(随账号快照走,换号不继承);对抗审计 3 镜头(资金安全/交互规范/规格一致)P0=0、5 项 P1 全修(含审计抓出的邀请码跨账号旁路、签到 60 天奖励文案与资格模型冲突改「邀请码候选」语义、Nova 推送编造余席数字删除);顺手修复:预售页售罄 dock 死按钮(现跳二级市场)、团队页旧卡过时死数字随移除消灭;休眠死件 genesis-dock-host 清入 .trash;verify 新增资格门防回退哨兵(L3/L4/二级三针)。规格 `PRD/specs/FEAT-GEN07-store-entry-eligibility.md`(三单元 7 块,spec-lint 过)。

**验证口径**:vue-tsc 0 错 + verify.sh 175/175(含新哨兵)+ i18n 4027 键 en/zh 镜像;admin tsc 0 + canon-sentinel 30/30 + ops-actions gate PASS;Playwright 实景:锁定/解锁双态翻转(注入不达标 → 0/4 锁定;入金/邀请码任一达成 → 解锁)、四层拦截逐层实测(含二级承接零扣款拦截、限购满 5 拦截)、邀请码非法拦/合法核销/换账号不继承结构证明、双语、5 路由 console 0。

**入口**:`/store` 尊享席位卡 · `/genesis` 预售页 dock · `/genesis/marketplace` 承接 · 资格面板(锁定态卡/dock 点击唤起)· admin G4 认购资格门。**PRD**:§7.1 尊享位入口 + §10.1 认购资格块 + §8 团队页移除(待主人确认后同步);后台 PRD G4(待同步)。

### ✏️ 邀请人奖励金额可配置化(消除写死的 +200 NEX)

**概述**:核对发现 me 页邀请入口「+200 NEX」是**邀请人奖励**(邀请人自己每拉一人得的 NEX),不是新人礼(被邀请人得的 $5+20NEX,早已可配置动态)。此前 200 写死在 3 处文案。本次纳入后台可配置:前端 `config.rewards.inviterReward.nexAmount`(默认 200)+ 3 处文案改占位动态读(`networkInviteMeta`/`inviteFriendsHint`/`twoWayGift`,en/zh 镜像);消费面 me 页邀请入口 + quota 页邀请提示改 `fmt()` 单源派生。后台 K2 面板新增「邀请人奖励配置」块(与新人礼块并列、独立键 `K.rewards.inviterReward.*`,调整走确认+理由+审计)。运营改后台 → 前端自动变。

**要点**:邀请人奖励与新人礼是两笔独立的钱(邀请人得 vs 被邀请人得),前后台键与语义均分开;`twoWayGift` 为死 key,一并占位化保持文案库一致。真后台契约 = `PATCH /api/admin/rewards/inviter-reward/config { nexAmount, reason }`。

**验证口径**:前端 vue-tsc 0 错 + verify.sh 175/175(i18n en/zh 镜像哨兵);admin tsc 0 错 + verify 237 pass(1 项 uniapp-port-coverage 为既有 runtime 证据缺口,与本改动无关);Playwright 实景:me 邀请入口 +200 NEX 自 config 动态渲染、admin K2「邀请人奖励配置」块渲染 200 NEX + 调整钮,双端 console 0 错。

**入口**:`/me` 邀请入口 · `/team/quota` 邀请提示 · admin `/risk/abuse`(K2)邀请人奖励配置。**PRD**:已同步——前端 §13.3 关键参数集 + 配置→后端映射表加 `rewards.inviterReward.nexAmount`;后台 PRD v1 §K2 ③b「发放/闸门配置」补齐。
**(注:该后台面板同日已迁 H8 `/growth/referral-rewards`,前端消费面不变;检测与拦截仍归 K2。)**

### ✏️ 个人中心入口按「频率 × 业务权重」重排信息架构

**概述**:me 页各分组入口按「用户使用频率 × 业务权重」双维度重排。分组大顺序(我的网络→我的设备→账户→偏好→帮助与支持)已符合权重递减,保持不动;重排 3 个分组内顺序:①**我的网络**:团队→邀请→佣金→等级(拉新裂变「邀请」提前、「等级」偶尔看殿后);②**账户**:奖励→收据→订单→创世节点→支付卡→身份验证→个人资料→安全设置(高频交易/资产/激励在前、低频身份/安全在后);③**帮助与支持**:消息中心→在线客服→FAQ→工单→信任中心→风险披露→开发者(有未读的消息中心提最前、dev 开发者殿后)。我的设备/偏好两组已合理,保持。纯顺序调整,入口内容/图标/跳转均不变。

**验证口径**:vue-tsc 0 错;verify.sh 174/174;Playwright 实景 evaluate 逐组核对 5 分组顺序全符合方案 + 三分组截图 + console 0 error。

**入口**:`/me` 各分组。**PRD**:不涉及(纯 UI 顺序,PRD 不写 UI)。

### 🗑️ 移除「重看新手引导」入口 + 整个 dev 演示工具页(/me/replay-tour)

**概述**:「帮助与支持」分组「重看新手引导」入口及其背后整个 dev/PM 演示工具页 `/me/replay-tour`(含新手引导回放 + 生命周期演示面板 + 试用参数调试面板)全部移除。该页 dev-only(生产环境用户不可达),主人拍板整页彻底删。共享 store(trial-config 试用参数、product-phase 平台阶段 override)全站其它地方仍在用,**保留不动**;仅移除 replay-tour 专属资产。

**移除清单**:页面 `replay-tour.vue` + 3 个专属组件(replay-config-number-row / toggle-row / demo-action)Move 到 `.trash`;me 页入口行、pages.json 路由、header-title 标题映射、i18n(en/zh 各:`me.replayTour` 入口 label + `headerTitles.meReplayTour` 页标题 + 整个 `replay` namespace 48/49 行文案块)清除;product-phase / lucky-spin 两处注释里指向已删页的 dead reference 更新。help 分组由 8 项回到 7 项。

**验证口径**:vue-tsc 0 错(无编译遗漏,证明共享 store 无断链);verify.sh 174/174(i18n en/zh 镜像哨兵 + 路由有效性哨兵是本次重点,均过);nexion-audit 五层自对抗 P0=P1=0;全站 grep `replay-tour`/`replayTour` 残留=0(仅注释已更新);Playwright 实景:me 页帮助与支持 7 项无「重看新手引导」(全页文案 0 命中)、直接访问 `/me/replay-tour` 路由 no-match 不可达、console 0 error。

**入口**:(已移除)。**PRD**:§7.5.6 Lifecycle demo section + §11.5 me 页结构需标注移除(待同步)。

### ✏️ 个人中心「账户」分组新增创世节点入口 + 信任中心移至帮助与支持

**概述**:①「账户」分组新增「创世节点」宫格入口(皇冠图标),点进创世节点持有看板(`/genesis/holder`)——未持有显示引导 + 预览、持有显示持有席位数 + 预留 $NEX 额度 + 上所进度 + 逐 seat 明细;入口 meta 持有时显示「N 个节点」、未持有无 meta。②「信任中心」从「账户」分组移至「帮助与支持」分组(faq 之后)。两分组各保持 8 项 2×4 宫格整齐(账户移走信任中心 -1、加创世节点 +1;帮助 +1=8)。

**要点**:全复用既有资产——i18n(`me.genesisNode`/`me.myGenesisNodeValue` 既有)、holder 持有看板既有页、`ownsGenesis`/`myGenesisValue` 既有 computed;零新逻辑 / store / 页面,纯入口 wiring。

**验证口径**:vue-tsc 0 错;verify.sh 174/174;nexion-audit 手动五层自对抗 P0=P1=0;Playwright 实景:账户新入口渲染、点击跳转 holder、未持有空态引导、持久化注入持有 2 席验证 me 页 meta「2 nodes」+ holder 持有态(2 seats · 160,000 NEX · 逐 seat 明细 NEX-GEN-4192/4055)、信任中心全站仅 1 处(已移至帮助)、en/zh 双语、console 0 错;验后清除注入恢复 demo。

**入口**:`/me`「账户」分组「创世节点」→ `/genesis/holder`;「帮助与支持」分组「信任中心」。**PRD**:§11.5 入口结构(待同步)。

### ✏️ 版税页三板块 + 对碰页轨道差距 + 奖池页我的状态磨砂玻璃卡片(视觉打磨集)

**概述**:①版税页(`/team/unilevel`)的「直推版税」「网络影响力奖金」「合伙人等级」三个板块由透明发丝线分组改为磨砂玻璃卡片(半透明玻璃底 + 背景模糊,复用全站 chrome 玻璃 token,双主题自适应);直推与网络奖金由合并分组拆为两张独立卡。②对碰页(`/team/binary`)「轨道差距」板块同款磨砂玻璃背景。③领导力奖池页(`/team/leadership-pool`)「我的状态」板块(未解锁 / 已解锁两态同壳)同款磨砂玻璃背景。④主人同日终裁「带背景色的卡片禁加边框线」——玻璃卡零边框,规则已写入 nexion-design SKILL(嵌套表格 accent-banner 例外收回 + 大块卡边框铁律收紧 + bg+border 同现自检 grep)。

**验证口径**:vue-tsc 0 错;verify.sh 174/174;Playwright 实景 dark/light 双主题 + 390px 移动宽度,computed style 实测卡片模糊生效、border 为 none;console 0 error。

**入口**:`/team` → 影响力网络版税 / 平衡匹配。**PRD**:不涉及(纯视觉,PRD 不写 UI)。

### 🆕 个人中心「我的奖励」入口 + 红点提示 + 多层级奖励页(风险提示书移位)

**概述**:「我的奖励」页(`/me/rewards`)此前无任何入口可达(孤儿页),用户领完优惠券后无处查看。本次:① 个人中心「账户」分组新增「我的奖励」宫格入口(占原「风险提示书」格位,分组保持 8 项 2×4),带未读红点——有未使用的有效优惠券(持续亮,直到用掉/过期)或有新入账奖励(打开奖励页即视为已读熄灭)时显示;② 奖励页改**多层级**(主人同日拍板):L1 类别汇总页——参照 OKX 奖励枢纽排版做大数字卡(页首利益句 + 每类一张卡:类别名 / **大号数值**(优惠券=可用张数、USDT/NEX=累计总额)/ 一句用途描述 / 右侧圆形箭头,整卡可点),点分类进 L2 记录页——优惠券页为**券票样式**(每张券独立 ticket 卡:面值大字存根 + 打孔分隔 + 券名·范围·有效期·去使用;过期券同形置灰)、USDT/NEX 记录页(一屏 10 条,滚动到底自动续载分页);③「风险提示书」项移至「帮助与支持」分组末位,页面与强制确认逻辑零改动。

**要点**:红点、L1 总额、L2 记录三处共用同一奖励条目判定(单源,永不口径分叉);已读水位持久化,真后台契约=用户档案 `rewardsSeenAt` + `PATCH /api/me/rewards/seen`(服务端盖时间戳);记录分页真后台契约=`GET /api/me/bills?type=reward&symbol=&cursor=&limit=10`;后台客服补偿/活动入账按账单落库即自动点亮红点并计入总额,无需额外接口;非法分类参数回退优惠券页不白屏。

**验证口径**:vue-tsc 0 错;verify.sh 174/174;两轮独立审查 agent 对抗复核均 APPROVE(P0=P1=P2=0);Playwright 实景全链路(红点三态、L1 总额与持久层逐分对账 $32.50/5,380、分页 10 条/批加载到 21/21、非法参数回退、L2→L1→me 返回链、en/zh 双语、dark/light 双主题);console 0 error。

**入口**:`/me`「账户」分组「我的奖励」→ L1 类别页 → L2 分类记录页;「帮助与支持」分组「风险提示书」。**PRD**:§11.5a(入口句修订 + 多层级结构 + 红点与分页规则,待同步)。**规格**:`PRD/specs/FEAT-ME01-rewards-entry-reddot.md`(v2)。

### 🗑️ 首页 $NEX 行情卡当前阶段整卡隐藏(含删「币安 T1 已通过」文案)

**概述**:首页 ZONE 6 的 $NEX 行情卡(币对 + 现价 + 24h 涨幅 + sparkline + 「币安 T1 已通过」信任行)当前阶段不展示:卡内「币安 T1 已通过」文案永久删除(en/zh 死 key 同步清理),整卡从首页编排摘除。组件文件保留,后续阶段可一行恢复渲染(恢复时已不含 T1 文案)。同口径横扫:NOVA 气泡市场事件消息池同强宣称一条(「$NEX cleared Binance tier-1 listing review」)一并删除,余下 3 条概率配平;信任页/FAQ 均为「审核中」弱表述,与当前阶段一致,保留。

**验证口径**:vue-tsc 0 错;verify.sh 174/174(含 i18n 双语镜像 4035 key);Playwright 实景确认收益流水→算力市场直接衔接、全页无 $NEX/币安 残留、「已通过/cleared」强宣称全站源码 0 命中;console 0 error。

**入口**:首页 ZONE 6(收益流水与算力市场之间,原位)+ NOVA 气泡随机推送。**PRD**:§5.7 标注当前阶段隐藏 + 链接勘误 `/trust`→`/market`;§11.9.1 入口宣称同步(已同步)。

### ✏️ 全站二级页面视觉去卡片化改版(83 页 · 视觉打磨集)

**概述**:此前只有 5 个一级 tab 页(首页/赚取/商城/团队/我的)做过去卡片化,二级及以下页面卡片大量堆砌、信息层级混乱、缺呼吸感。本次把去卡视觉语法推广到全部 83 个二级/三级页面:降卡片为透明发丝线列表或单容器多行、大数字/hero 直接坐页面地板、区块靠留白+发丝线+小标题分区,只保留白名单(分段控件/nav 行/spotlight hero/输入框/主 CTA/聊天气泡/内容卡商业身份)。

**统一间距系统(参照支付宝/币安/微信)**:导航栏→内容 24px(SubPageHeader 页由组件单源提供,chassis-nav 页每页 padding-top:24px)· 主版块间 36px · 说明页正文层级(标题彩色 accent / 正文 13.5·行距 1.65·中灰)· 步骤序号与标题中线对齐、正文缩进 · 单元间发丝线。

**要点**:钱包/交易金融数字保大字主色、支付控件零改;商品/创世/内容卡保商业身份;每日签到火焰头图等游戏化件保视觉重量;试用转化钩子不削弱;10 个说明页迁到统一的 how 组件系;文案清除 markdown 记号 + 程序路径改人话。

**验证口径**:vue-tsc 0 错;verify.sh 174/174(含新增 markdown-residue + 导航栏 24px 呼吸两条防回归哨兵);7 批各派独立审查 agent 对抗复核(P0=P1=0);main 逐页浏览器实景(含全页滚动)+ 5 个一级页 chrome-baseline 0 像素回归 + 违禁词全站 0;console 全程 0 error。

**入口**:全站二级页面(团队子页 / 钱包系列 / 设置账户 / 商城 / 创世 / 质押 / 说明页 / 营销入口等)。

**规格**:`docs/DECARD-SPEC.md`(去卡语法 + 间距系统)· `docs/DECARD-LEDGER.md`(83 页进度台账)。

---

## 2026-07-08

### 🗑️ 订单页头部删除「追踪硬件发货状态」描述

**概述**:「我的订单」页头部副标题原文案是"追踪您的硬件发货状态"(英文 "Track your hardware shipments"),不符合平台实际形态——平台硬件是 IDC 数据中心托管(colocation),不存在向用户实物发货这回事,店铺页页脚本身也写着"zero shipping"。删除该副标题,页头只保留标题「我的订单」,不做文案替换。

**验证口径**:vue-tsc 0 错;verify.sh 171/171;Playwright 实机确认头部标题正常显示、副标题元素完全不渲染(非留空字符串)、头部高度联动收窄为单行 44px;中英文皆验证;console 全程 0 error。

**入口**:我的 → Account → Orders → 页头。

### ✏️ 订单入口从 Store 页迁移到「我的」页账户板块 + 删账户板块重复通知按钮

**概述**:Store 页底部原来有个独立的「Orders」入口按钮(需滚到页面最底才能看到),现移除,订单查看统一收口到「我的」页「Account」板块——新增「Orders」行(有订单时显示订单数),与 Profile / Security / Receipts 等同级,始终可见不用滚动。同时「我的」页「Account」板块原有的「Notifications」行删除,因为它和页面顶部常驻的通知铃铛(点开消息中心抽屉)是同一件事,保留会让用户以为是两个不同入口。

**验证口径**:vue-tsc 0 错;verify.sh 171/171;Playwright 实机验证——Store 页底部确认「Orders」按钮已移除、页面结束在「Coming soon」卡片+页脚;「我的」页「Account」板块确认「Orders」行存在且点击正确跳转到订单列表页、「Notifications」行已移除、顶部通知铃铛未受影响;中英文案通过语言切换页真实验证;console 全程 0 error。

**入口**:我的 → Account → Orders(原 Store 页底部入口已移除)。

**概述**:分享海报视觉升级——① 海报预览卡加固定色描边 + 投影,把卡从弹窗底色上抬起(此前深色模式下黑底黑卡层次不明显);② 海报改为恒定深色画稿,浅色 / 深色模式生成同一张海报(此前品牌色随主题读取,两种模式出图不一致);③ 海报背景加入质感装饰层:品牌绿 + 紫双光晕、细点阵、右侧金币 ×2 + 斜置钞票贴纸、底栏 hairline 分隔(此前背景仅一层纯渐变);④ 新人礼模板主标改「注册即送」(en "Sign up & get");⑤ 海报 / 渠道两弹窗底部留白对齐全站 38px + iOS 安全区基准(此前贴屏幕底边,压 iOS 小横条区)。

**验证口径**:vue-tsc 0 错;verify.sh 171/171(含 i18n 双语镜像);Playwright 手机视口(390×844)双主题 + 双语实测——两模式海报画稿逐像素同源、边框投影在深色弹窗上层次清晰、贴纸落在正文右侧空区不压文字与二维码,console 0 error。

**入口**:Team → 邀请卡「海报」/ 立即分享 → 分享海报。

### ✏️ 邀请榜列表分页(点击查看更多)+ 分享按钮改吸底磨砂样式

**概述**:「邀请榜」页(Top Inviters)#4-#100 用户列表原来一次性全量渲染,改为默认只显示前 20 条,列表下方「查看更多」按钮,每点一次追加 20 条,直到全部加载完按钮自动消失——减少首屏渲染量与未来真实接口的单次拉取量,且不依赖滚动/视口尺寸,行为完全可预期。底部「Share my invite link」分享按钮原来在普通文档流最下方(需拉到页面最底才可见),改为滚动视口内吸底(`position: sticky`),打开页面即可见、滚动过程中持续吸底,背景磨砂透明(与已有 chassis 级 sticky-cta-bar 同源 design token),不遮挡列表可读性。

**验证口径**:vue-tsc 0 错;verify.sh 171/171(含 i18n 双语镜像);Playwright 实机验证——冷启动精确渲染 20 行,连续点击「查看更多」精确按 20 递增(20→40→60→80→97),加载完按钮正确消失;分享按钮 scrollTop=0 时已处于可视区域内(无需滚动)、滚动中途持续吸底、深浅双主题磨砂效果均确认;中英文案通过语言切换页实测(非本地存储硬改)均正确渲染;全程 console 0 error。

**踩坑记录**:首版分页用 IntersectionObserver 滚动自动加载,`watchEffect` 读写同一个 `visibleCount` 在桌面宽视口(20 行不足以撑满屏幕)下与浏览器真实回调时序脱节,连锁加载到 40+ 行——主人实测发现"进入页面还是全部渲染",定位根因后先加门控修复,但主人认为滚动自动加载本身对视口尺寸太敏感,直接指示改为点击按钮手动加载,彻底去掉这类视口相关的边界情况。

**入口**:Team → 邀请榜(Top Inviters)。

### ✏️ 「切换主题」弹窗改为居中显示

**概述**:「我的 → Theme」弹窗由底部弹出(bottom sheet)改为屏幕居中弹窗,与「确认 / 网络异常」等全局弹窗视觉范式统一;选项选择 / 关闭按钮 / 点遮罩关闭交互不变。

**入口**:我的 → Preferences → Theme。

### 🗑️ 移除「我的」页「增值玩法」板块 + 年度回顾(Wrapped)功能页

**概述**:「我的」页整块删除「增值玩法」板块(任务 / 签到 / 活动 / 学习 / 质押 / 创世节点 / 成就 / 奖励 / 年度回顾 9 个入口的聚合分组)。其中「年度回顾」(Spotify Wrapped 风全屏 6 卡片)因仅由该板块进入,连同页面 / 路由 / 文案 / 专用动画一并下线;其余 8 项的功能页与路由未改动,仅从「我的」页移除该聚合分组入口。

**🗑️ 移除**:
- 「我的」页 sections 删除 `earn-extras`(增值玩法)整组 9 个入口。
- 年度回顾页 `pages/me/wrapped` 移入 `.trash`;`pages.json` 路由注销。
- i18n 清理(en/zh 对称):`me.secEarnExtras` / `me.wrappedRow` / `me.meWrapped` 三个死 key + `wrapped.*` 整个命名空间;镜像门 4035 keys 通过。
- `tokens.css` 删除死动画 `.nx-story-in`(唯一消费者是已删的年度回顾页)。

**验证口径**:vue-tsc 0 错;i18n 中英镜像门通过;vite 实发 `me` 模块 / `tokens.css` / `pages.json` 均确认 0 残留、HTTP 200。

**入口**:(已移除)原「我的 → 增值玩法」分组及其「年度回顾」。 **PRD**:年度 Wrapped 页规格 / 路由表 / i18n 命名空间清单(同步移除)。

### 🆕 邀请分享全链路打通(海报 / 邀请码 / 链接 / 立即分享 → 落地 → 注册 → 下载引导)

**概述**:team 页邀请卡的四个分享入口此前两个是「点了只复制链接」的半成品(海报、立即分享),本次全部接真:点「海报」弹出全屏海报面板——带**可扫真二维码**的邀请海报(新人礼 / 我的产出 / 我的网络三套模板可切,可关用户名),扫码直达 H5 落地页;点「立即分享」弹出渠道面板(Zalo / Telegram / WhatsApp / Messenger / 短信 / X / 复制 / 海报 / 系统分享,顺序运营可配)。好友侧:落地页 → 注册页邀请码**自动填入并锁定置灰**(不可修改删除,防归因被截断;中途溜走晚点再注册也能靠本地暂存的码找回归因)→ 注册成功进新「成功页」:礼包到账确认(已入账 / 风控锁定两态)+ 引导下载 APP(地址运营可配,未配置显示「即将上线」降级态)。首次任何分享动作自动完成首日任务「邀请好友」(+200 NEX +$1,幂等不重发)。

**🆕 新增**:
- 分享海报面板:canvas 生成 750×1000 海报 + 真二维码(payload=单源分享链接);模板轮播(无设备时「我的产出」模板自动隐藏);展示用户名开关;保存图片(受限浏览器降级长按引导)/ 复制 / 发渠道;生成失败态可重试。
- 立即分享渠道面板:渠道 grid 按平台配置驱动;H5 上 Telegram/WhatsApp/短信/X 直接唤起预填文案,Zalo/Messenger 无网页直发口走「复制文案+引导粘贴」降级;渠道打开被拦截自动复制兜底;配置为空时兜底「复制+海报」两项,面板永不空。
- 注册成功页(`pages/register/success`,仅 H5 注册后出现):礼包两态确认 + 「为什么装 APP」权益 + OS 检测下载 CTA + 「先用网页版继续」;返回键视同继续,不可回注册流。
- 分享底座:分享链接单源构造(短链前缀运营可配,dev 未配置时自动用本机地址,扫码演示当场可用);分享事件记录;pendingRefCode 断链归因(落地页捕获 · 注册前 last-touch 覆盖 · 绑定即清 · 非法码不写)。

**✏️ 修改**:
- 注册页邀请码输入框:链接/扫码来源(含 pendingRefCode)一律锁定置灰带锁标,仅无码自然进入可手输(主人 2026-07-08 拍板);注册完成 H5 端改跳成功页(APP 壳仍直进 onboarding)。
- 落地页 `/pages/ref/code`:无码/非法码 → 隐藏邀请人卡与 REF 徽章的通用落地态(不阻断注册);已登录 → 提示条 + CTA 变「进入 Nexion」(不重复领礼)。
- 邀请卡复制码/复制链接:剪贴板失败不再误报「已复制」;邀请码为空四入口置灰。
- proof 页分享链接收编单源构造(原两处硬编码 `nexion.ai/ref/` 消除)。

**验证口径**:vue-tsc 0 错、`verify.sh` 171 项全绿(含 i18n 中英镜像门,新增 share 命名空间)、实景全链路走查(海报三模板生成 / 渠道降级 / 落地三态 / 注册锁码含 pending 源 / 真注册到成功页 / 任务奖励幂等)console 0;三镜头审计修到 P0=P1=0。

**入口**:团队 → 邀请卡四入口;好友侧 `/#/pages/ref/code?code=…` ｜ **PRD**:规格 `PRD/specs/FEAT-SHARE01-invite-chain.md`(§4.1 / §4.3.1 / §8.1.1 / §13.3 同步锚点)。

### ✏️ 主题切换升级为三态弹窗(深色 / 浅色 / 跟随系统)

**概述**:「我的」页「主题」项此前点一下就在深色↔浅色之间来回切,现改为点击弹出底部「外观」选择弹窗,一屏看清三个选项——浅色 / 深色 / 跟随系统——当前选中项高亮打勾,点选即时生效并关闭弹窗。新增「跟随系统」态:随设备系统的深色/浅色设置自动切换,且系统切换时实时跟随。

**✏️ 修改**:
- 主题从两态(直接 toggle 深↔浅)升级为三态(浅色 / 深色 / 跟随系统),点击改为弹出底部选择弹窗(复用现有 sheet 范式),三选项带图标 + 当前选中打勾。
- 新增「跟随系统」:实时跟随设备外观设置(H5 通过系统偏好监听双向切换);用户三态选择本地持久化,刷新后保持。
- 主题行右侧说明文字与图标随当前态更新(浅色 ☀ / 深色 ☾ / 跟随系统 ▢)。

**验证口径**:vue-tsc 0 错、`verify.sh` 171 项全绿(含 i18n 中英镜像门)、实景三态切换即时生效 + data-theme 真变 + 刷新持久 + emulate 系统深浅双向跟随、console 0。

**入口**:我的 → 主题 ｜ **平台**:uniapp H5(App 端主题在打包层,弹窗与系统跟随仅 H5 生效)。

### 🆕 收据列表分页加载(首屏 + 下滑加载更多)

**概述**:「我的→收据」列表页(全部 / 图像 / 视频 / LLM / 微调 / Embedding / 语音 / KYC 各 tab)不再一次性渲染该分类下全部收据,改为首次载入只出一屏(10 条),用户下滑接近底部时再加载下一屏,减少一次性渲染量,也为将来接真分页后端预留好交互契约。切换 tab 会重置回第一屏。

**🆕 新增**:
- 收据列表滚动到底部触发的「加载更多」(每次 10 条),复用现有的 IntersectionObserver 滚动检测能力,未新增依赖。

**入口**:我的→收据 ｜ **平台**:uniapp H5 / App 原型。

### 🗑️ Proof-of-Compute 收据弹窗移除底部「查看/分享」按钮

**概述**:任务完成收据弹窗(Earn 任务中心点击已完成任务 · 我的→收据详情共用同一弹窗)底部的「在 Explorer 中查看 / 分享」两个按钮无真实后端(仅复制 tx_hash 到剪贴板),予以移除,弹窗在明细行后直接收尾。

**🗑️ 移除**:
- `components/me/receipt-modal.vue` 底部 CTA 行(Explorer / Share 按钮 + 对应点击逻辑);同步清理仅被其引用的 4 个 i18n key(`receipt.viewOnExplorer`/`share`/`explorerHint`/`shareHint`,中英同步)。

**入口**:Earn 任务中心已完成任务行 · 我的→收据详情 ｜ **平台**:uniapp H5 / App 原型。

### ✏️ 创世节点排放迁移收尾:清残留「分红」文案 + 前后端术语对齐

**概述**:承接 2026-07-07 的创世节点(Genesis)分红→排放改造,清掉前端残留的 genesis「分红」措辞,并与运营后台同步统一到「排放」口径(后台此前几乎没跟改,本次两端一起收尾)。前端此前大半已迁(排放 44 处 / 分红仍 28 处),本次把最后的 genesis 残留清零。

**✏️ 修改**:
- 创世列表空态 banner「即可开始分红 + 后续挂单」→「即可锁定排放额度 + 后续挂单」(中英 i18n 镜像同步)。
- holder 页 / genesis store / product-phase 里描述创世收益的「live 分红 / 分红数字 / 等不到分红」等注释统一为「排放」,消除代码内术语残留。
- **保留**:领导池 / 团队网络 / NEX 持有者分红(非创世,原样);改造 codename「分红延期改造」;刻意对比句「不是每日现金分红」(教育用户排放≠分红);代码标识符(`dividendsOpen`·`GENESIS_DIVIDEND_OPENS_AT_PLATFORM_MONTH` 等)。

**验证口径**:vue-tsc 0 错、`verify.sh` 171 项全绿(含 i18n 中英镜像门)、实景创世节点页 console 0 且渲染全「排放」(排放优先额度 / 协议排放优先额度 / 排放何时开始);canon 数字口径门 0。

**入口**:创世节点页 · holder 面 ｜ **PRD**:已同步前端 PRD §10 Genesis(持有人看板重构为「上所前额度+积分 / 上所后 NEX 排放 vesting」两态模型 + 玩法页权益 + §13.3 参数口径)+ APP 端开发落地规格。

### 🗑️ 收益页移除车队任务产能卡片

**概述**:收益页不再展示「车队任务产能」聚合卡片,设备列表下方直接进入收益提醒、市场行情和任务中心。

**🗑️ 移除**:
- 移除 `/#/pages/earn/earn` 中的车队任务产能聚合卡片入口。

**入口**:`/#/pages/earn/earn` ｜ **平台**:uniapp H5 / App 原型 ｜ **PRD**:收益页产能说明口径。

## 2026-07-07

### 🆕 会话中心体验四连修:发送限频 + 自动到底 + 已读/输入中 + 返回记位

**概述**:会话中心(顾问 / 客服 / Nova AI 三通道)补齐四项对话体验。发送加防刷屏频率限制;进入会话和收发消息时自动定位到最新一条;消息补「已读 / 未读 / 对方正在输入」状态提示(前端聊天页 + 运营后台客服台两端);App 二级页返回上层页面时恢复跳转前的滚动位置。

**🆕 新增 / ✏️ 修改**:
- **发送频率限制**:三通道统一「15 秒内最多 5 条」滑动窗口,超限不发出并弹提示「发送太频繁 · N 秒后再试」(中英双语);挡刷屏,接真后台即对应 429 限流。
- **自动定位最新消息**:进入会话、发出消息、收到回复、出现「输入中」时都自动滚到底(H5 直接驱动真实滚动层,修掉旧方案「差一截没到底」的问题)。
- **已读 / 未读 / 输入中**:自己发的最后一条消息尾部显「未读」,对方读后转「已读」;对方回复前显三点「正在输入」气泡 + 会话列表预览同步显「正在输入…」。后台客服台同机制:坐席回复后经「未读 → 已读 → 用户正在输入 → 用户短回执」完整闭环。
- **返回恢复滚动位置**:任意二级页返回上层页面(如从会话中心返回「我的」)时,恢复到跳转前所在的滚动位置,不再跳回顶部;聊天页前进离开再返回也自动回到最新消息。

**验证口径**:双 agent 对抗审查(一致性 / 不变量 + 正确性 / 时序),P0=0;正确性审查揪出 2 个 P1(前进离开聊天页导致 Nova 未读被永久压制 · 返回后消息列表跳顶)已修复;另修复自动滚动「查错滚动元素假通过、实际没到底」的真 bug。vue-tsc 0、`verify.sh` 171 项全绿、i18n 中英镜像 PASS;Playwright 实景确认四项 + 两个 P1 修复,console 0。

**入口**:`/#/pages/support/messages`(会话中心)·`/#/pages/support/chat`(聊天页) ｜ **平台**:uniapp H5 / App 原型 + 运营后台(M3 即时会话台) ｜ **PRD**:会话中心 / 客服章节(待同步)。

### ✏️ 创世节点改造:每日分红延期到上所开放 + 前期上所排放权益化

**概述**:创世节点(Genesis Node)从「买日分红债券」改造成「买创世 OG 席位 + NEX 上所排放优先权」。收益来源从「平台交易量 0.1% 日分红」换成「NEX 协议排放」,且**分红延期——上所前(运营前期)只卖预留额度/权益,NEX 上所(运营中期,平台第 7 月)后才开排放阀**。定价从单一 $9,999 改成 3 档阶梯(白名单 $7,999 / 公售 $9,999 / 尾盘 $11,999,售罄跳价)。排放按 vesting 曲线释放(TGE 解锁 10% + 前低后高 18 月),非一次性空投、上所前零累积可领余额。文案走高端稀缺席位的克制路线,清掉旧的日分红/回本/$25K 地板/点名交易所等叙事。

**✏️ 修改**:
- **预售页**:hero 极简(「优先领 $NEX · 限量 1000」)+ 3 档阶梯价卡(当前档/售罄态)+ 权益 4 层(优先领 NEX / 生态奖励池 / DAO 治理权 / 年度空投);删日分红权益条、每日收益卡、$25K 地板链接。
- **持有人看板**:按「是否上所」自动切两态——上所前=排放优先额度 + 上所进度 + 积分榜 + 锁仓加倍(无每日分红/无可领);上所后=NEX 排放 vesting 进度环(已释放/锁定中)+ 排放明细。
- **二级市场**:地板价 $25K→$13.4K(对齐尾盘档溢价),挂单口径改「额度 + OG 倍率 + 剩余 vesting」;二级承接改为「转让不铸造」,修复售罄承接可能扣款无货的隐患(失败退款)。
- **玩法说明**:机制叙事重写成「买权益 → 中期开排放 → 上所领额度」,明示「不是每日现金分红、无保证收益、应视作 venture 投资」。
- **首页今日收益**:创世不再计入每日 USD 聚合(改上所后 NEX 排放,在看板单独展示)。

**验证口径**:双 agent 对抗审查(业务逻辑 + 用户视角视觉/交互);修掉审查发现的 1 个 P0(二级承接资金安全)+ 全部 P1;vue-tsc 0、i18n 中英镜像 PASS、admin canon 三端对账绿、用户可见创世内容禁词 0、console 0;视觉审查评分 88→修后。

**入口**:`/#/pages/genesis/genesis`(预售)`/holder`(看板)`/marketplace`(二级)`/how-it-works`(说明) ｜ **平台**:uniapp H5 / App 原型 + 运营后台(H1 加「创世排放开阀」逐月旋钮 + G4 加空投占比/排放曲线/锁仓期参数) ｜ **PRD**:§10 Genesis 创世节点(待同步)。

### ✏️ 上架节奏门覆盖置换路径 + 置换侧抢先购开关

**概述**:未正式上架的机型(按平台阶段分批开售的 Pro v2 / Rack P2)此前可从置换侧提前买到——商城正门锁着、置换侧门大开;现上架门对置换目标列表、设备行入口、商城横幅、结算深链全部生效。同时新增运营开关「置换侧抢先购」(默认关闭):开启后仅置换用户可在正式上架前 N 天(可配 7/14/30/60/90)买到新机型,目标行带「抢先升级」标——把提前购留作置换专属特权由运营掌控。

**验证口径**:三态实景走查 14 项全过(默认关=未上架机型从置换面消失+深链弹回;开启=窗口内机型带标可完整走通置换支付;全面上架=列表复原),`verify` 171 项全绿,console 0。

**入口**:`/#/pages/me/devices`(置换目标列表)、`/#/pages/store/checkout`(深链拦截) ｜ **平台**:uniapp H5 / App 原型 + 运营后台(E1 上架节奏门配置项) ｜ **PRD**:§7.1 / §7.5.1 / §7.5.2 / §13.3。

### 🆕 无代际升级置换:随时下架 + 产出阶梯抵扣

**概述**:取消设备代际概念,已购设备随时可下架,按「累计产出 ÷ 实付价」落五档阶梯(75%→60%→45%→30%→15%)折算抵扣金额,仅在购买更高价设备时结算抵减——产出越多抵得越少,引导尽早升级。抵扣永不进余额、不可提现;阶梯与规则运营后台可配。

**🆕 新增**:
- 设备仓库每台可置换设备加「可抵 $X」标签(点开阶梯规则说明弹层,当前档高亮)+「升级置换」入口;免费设备零痕迹,已是最高档位的显示原因。
- 升级目标点选弹层(仅列合规目标 SKU,每行带抵后价)→ 置换确认卡(累计产出 / 抵扣档位 / 本次抵扣 / 预计应付)→ 去结算。
- 结算页抵扣行 + 一键移除(移除即恢复原价);支付成功一笔完成扣净额、旧设备下架移除、订单记录抵扣明细。
- 设备有任务进行中时给阻断提示(查看任务进度 / 知道了),任务完成即可下架,无强拆。
- 商城顶部横幅改为按用户最优设备动态显示「你的 X 可抵 $Y,升级 Z 只需约 $N」。

**🗑️ 移除**:代际概念全站清除(固定 $300/$800 代际抵扣映射、按月龄残值公式、最短持有月数门槛、「上一代 / 新一代」文案)。

**验证口径**:`type-check` 0 错、`verify` 170 项哨兵全过(含阶梯结构哨兵)、5173 实景走查 48 项全过(余额差=净额精确、抵扣未入余额、旧机原子移除、双语渲染、console 0)、四镜头对抗审计修复后 P0=P1=0。

**入口**:`/#/pages/me/devices`(下架置换)、`/#/pages/store/checkout`(结算拦截 + 抵扣行)、`/#/pages/store/store`(横幅) ｜ **平台**:uniapp H5 / App 原型 + 运营后台(E 域阶梯面板) ｜ **PRD**:§7.5 重构(待同步),规格 `PRD/specs/FEAT-DEV02-tradein-ladder.md`(已签字)。

### ✏️ 设备收益机制叙事切换:任务产能模式(替代硬件衰减)

**概述**:设备收益递减的机制解释从「硬件效率衰减」全面切换为「平台任务结构演进」——AI 任务算力要求逐步提升,高阶任务增多、低阶任务减少,老设备接单量随之下降;引导升级更高算力设备接高阶任务。用户实际收益数字完全不变(等效换皮),曲线参数改为后台可配阶梯表。

**✏️ 修改**:
- 设备卡效率指标改为「任务产能 + 今日接单数」口径,新增新购设备「新机任务补贴」标识(30 天,纯展示)。
- 收益页新增「AI 任务算力需求升级中」提示线与车队产能横幅;产能规则说明弹层(W-CAP1)。
- 风险披露「硬件会衰减」整段重写为「任务分配机制」披露;商城卡新增「可接高阶任务」能力线;硬件衰减 / 损耗类文案全站清零(双语)。

**验证口径**:曲线等效哨兵 97 采样 diff<1e-9(数值零变化)、`verify` 全过、5173 双语实景走查 console 0、三镜头对抗审计 P0=P1=0。

**入口**:`/#/pages/earn/earn`(设备卡 / 提示线)、`/#/pages/me/risk-disclosure` ｜ **平台**:uniapp H5 / App 原型 + 运营后台(E3 任务产能节奏面板) ｜ **PRD**:§6.8 口径重述(待同步),规格 `PRD/specs/FEAT-DEV01-task-capacity-schedule.md`(已签字)。

## 2026-07-06

### 🆕 短信验证码防轰炸闸门(滑块人机验证)

**概述**:登录 / 注册 / 重置密码三个发验证码入口接入统一防轰炸闸门——同一手机号 24 小时内第 3 次发码前必须通过拼图滑块人机验证;发码 / 验码从纯前端计时器升级为真后台同构接口(60 秒冷却服务端双保险、验证码 5 分钟有效、一码一机制、输错 5 次作废),全部阈值运营可配。

**🆕 新增**:
- 拼图滑块验证弹层(拖块入缺口,失败抖动重试、连续 5 次失败暂锁、可刷新可取消),登录 / 注册 / 重置三场景共用。
- OTP 生命周期接口(发送 / 人机验证 / 校验),含"发送太频繁""需要人机验证""验证码过期 / 失效"等完整错误反馈,双语文案。
- 平台配置新增 `otpGate` 参数组(冷却秒数 / 触发阈值 / 有效期 / 输错上限 / 票据有效期),后台可调不写死。
- 验证码校验测试规则:`000000` 演示输错、`999999` 演示过期、其它 6 位通过(演示零摩擦)。

**验证口径**:`type-check` 0 错、`verify` 168 项哨兵全过(含 10 条本功能新哨兵)、5173 实景全链走查(闸门触发 / 滑块成败 / 错误链 / 登录闭环 / 双语)console 0、三镜头对抗审计 P0=P1=0。

**入口**:`/#/pages/login/login`(验证码登录 / 忘记密码)、`/#/pages/register/register` ｜ **平台**:uniapp H5 / App 原型 ｜ **PRD**:§4.6.2 / §16.2.1 展开落地,规格 `PRD/specs/FEAT-AUTH01-otp-antibomb-gate.md`(已签字)。

## 2026-07-03

### ✏️ 新人礼金额改为平台配置驱动

**概述**:注册新人礼($5 USDT + 20 NEX)金额从前端写死常量改为读平台配置 `rewards.welcomeGift`,运营在后台可调;前端所有展示新人礼金额的地方统一从配置派生,不再有第二处写死值。

**✏️ 修改**:
- 邀请落地页、注册页、NEX 说明页、团队配额页 4 处的新人礼金额改为从 `rewards.welcomeGift.{usdtAmount,nexAmount}` 派生展示,发放金额与展示同源。
- 后台「套利 & 刷量检测(K2)」新增「新人礼发放配置」面板,可调金额与发放模式(风险桶 / 直入可提),走操作确认 + 理由 + 审计。

**验证口径**:双端 `type-check` 0 错、`verify` 全过、3002 后台调金额实景传导、5173 邀请页 / 注册页金额渲染无占位符泄漏、console 0。

**入口**:`/#/pages/ref/code`、`/#/pages/register/register` ｜ 后台 `/risk/abuse` K2 ｜ **平台**:uniapp H5 / App 原型 + 运营后台 ｜ **PRD**:前端 §4.3.2 / §4.3.5 / §13.3;后台开发落地规格 K2 参数表。

## 2026-07-02

### ✏️ 团队页佣金卡与工具四宫格重排

**概述**：团队页本月佣金卡重新排版为主金额 + 开放式 2×2 指标区，底部硬件配额、区域大使、影响力网络、族谱合并成统一四宫格，降低页面杂乱感。

**✏️ 修改**：
- `/pages/team/team` 的本月佣金卡去掉装饰粒子、内层小卡片和散列布局，保留主金额、NEX、累计、贡献者、增长和四项明细。
- `硬件配额 / 区域大使 / 影响力网络 / 族谱` 改为一个 2×2 四宫格模块，入口和路由保持不变。

**验证口径**：`npm run type-check`、`bash scripts/verify.sh` 和团队页浏览器实景核验。

**入口**：`/#/pages/team/team` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯视觉层级调整，无需同步。

## 2026-07-01

### ✏️ 团队页进度条弱化

**概述**：团队页移除两处装饰性条形进度条,减少卡片内横线噪音,保留关键数值和入口。

**✏️ 修改**：
- `/pages/team/team` 的 Genesis 创世节点卡片不再显示库存进度条,保留价格、预估收益和剩余数量文案。
- 团队佣金卡不再显示 Direct / Extended 双色占比条,保留占比、金额、已到账和冷却中金额。

**验证口径**：`npm run type-check`、`bash scripts/verify.sh` 和团队页浏览器实景核验。

**入口**：`/#/pages/team/team` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯视觉层级调整,无需同步。

### ✏️ 我的页 OKX 式功能宫格重构

**概述**：参考 OKX 个人中心的信息结构,把我的网络、我的设备、增值玩法、账户、偏好、帮助与支持六个板块统一改为「标题 + 图标宫格」,减少长列表和重复摘要卡,让入口更密、更好扫。

**✏️ 修改**：
- `/pages/me/me` 的「我的网络」从独立进度卡改为团队、等级、邀请、佣金 4 个入口。
- 「我的设备」改为设备仓库、添加设备、算力槽位、收益目标 4 个入口,保留槽位计数。
- 增值玩法、账户、偏好、帮助与支持统一为 OKX 式 4 列功能宫格,保留 KYC、2FA、通知、收据、主题、语言、客服等动态状态。

**验证口径**：`npm run type-check` 通过;`bash scripts/verify.sh` 110 pass / 0 fail;浏览器实景确认六个区块标题和 34 个入口全部渲染、430px 宽度无横向溢出、底部帮助区和退出按钮未被底栏遮挡,console error=0。

**入口**：`/#/pages/me/me` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯个人中心信息架构和视觉层级调整,无需同步。

### ✏️ 商品详情预计回报区域去卡片嵌套

**概述**：商品详情页「预计回报」区域去掉四个内层小卡片,改为外层容器内用弱分割线划分每日、每月、每年、回本四个指标,降低视觉噪音。

**✏️ 修改**：
- `/pages/store/detail?id=stellarbox-s1` 的 ROI 四指标不再使用内层背景卡和圆角。
- 四个指标改为 2×2 分区,仅保留很淡的横向/纵向分割线。

**验证口径**：`npm run type-check` 通过;`bash scripts/verify.sh` 110 pass / 0 fail;浏览器实景确认四个指标背景透明、圆角为 0、分割线存在且弱化,console error=0。

**入口**：`/#/pages/store/detail?id=stellarbox-s1` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯视觉层级调整,无需同步。

### ✏️ 商城卡片详情默认折叠

**概述**：商城列表卡片减少首屏信息密度。等级门商品默认只显示「解锁后购买」标题,即将上线商品默认只显示商品名和阶段标签;点击收放图标后再展开条件、参数、进度、通知和排队信息。

**✏️ 修改**：
- `ProductCard` 的购买门详情默认收起,只保留标题和收放图标,展开后显示等级/直推/团队业绩条件和解锁进度。
- `LockedProductCard` 默认隐藏详细参数、解锁进度条、上线通知和排队人数,只显示简化阶段如 `阶段1/5`。

**验证口径**：`npm run type-check` 通过;`bash scripts/verify.sh` 通过;浏览器实景确认默认态无被收起详情,点击收放图标后详情出现,console error=0。

**入口**：`/#/pages/store/store` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯首层展示层级调整,无需同步。

### ✏️ 商城首层商品卡参数收口

**概述**：商城首层商品卡移除硬件规格、AI 性能参数、手机倍数对比、评分和评价,首层只保留商品名、收益、价格、购买入口和弱库存提示;详细参数继续在商品详情页展示。

**✏️ 修改**：
- `/pages/store/store` 的 `ProductCard` 不再展示 `4× RTX 4090 / 96GB VRAM / 新加坡数据中心` 等硬件 chip。
- 首层商品卡不再展示 `117× 对比手机` 和 `图像 / LLM / 视频` 性能 chip。
- 首层商品卡不再展示评分、评价和已售行;库存提示改为日产数值行右侧弱文案 `仅剩47/本周`。
- `/pages/store/detail?id=stellarbox-s1` 保留硬件参数表和 AI 性能参数表。

**验证口径**：`npm run type-check` 通过;`bash scripts/verify.sh` 110 pass / 0 fail;浏览器实景确认商城 S1 首层卡片无上述参数、评分/评价和旧库存长文案残留,日产数值行右侧显示 `仅剩47/本周`,详情页参数仍存在,console error=0。

**入口**：`/#/pages/store/store` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯首层展示层级调整,无需同步。

### ✏️ 我的页创世节点入口收口

**概述**：我的页移除独立的创世节点营销大卡,把创世节点收进「增值玩法」列表入口,减少页面重复营销块。

**✏️ 修改**：
- `/pages/me/me` 不再在设备区下方展示独立 GenesisNodeCard。
- 「增值玩法」列表保留创世节点入口,文案显示为「创世节点」,点击进入 `/#/pages/genesis/holder`。

**验证口径**：`npm run type-check` 通过;`bash scripts/verify.sh` 110 pass / 0 fail;Playwright 393×852 实景确认旧大卡特征文案不再出现,列表入口存在且可跳转到 Genesis 持有人页,console error=0。

**入口**：`/#/pages/me/me` → 增值玩法 → 创世节点 ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯入口位置调整,无需同步。

## 2026-06-30

### 🆕 三端入口首页新方案（SPEC-6）

**概述**：新增三端入口首页新方案。签名版 APP、H5 网页版、白 APP 接管首页拆成三个独立页面展示,并新增一个完整链接索引页；旧 `/home-signed`、`/home-h5`、`/home-cloak` 继续删除,正盘默认首页不改。

**🆕 新增 / ✏️ 修改**：
- 新增 `/#/pages/entry-surfaces/index`,展示三条完整可点击链接。
- 新增 `/#/pages/entry-surfaces/signed`,展示签名版 APP 在线增强首页。
- 新增 `/#/pages/entry-surfaces/h5`,展示 H5 基础托管首页。
- 新增 `/#/pages/entry-surfaces/white?entry=white-app`,展示白 APP 接管首页。
- `scripts/verify.sh` 新增 SPEC-6 哨兵,同时检查新路由存在、旧三页继续删除、完整链接可点。
- App 启动、页面壳和全局 UI 将 `entry-surfaces` 识别为静态评审路由:不绑定账户云、不认领会话、不启动业务轮询、不展示业务弹层。

**验证口径**：`npx vue-tsc --noEmit` PASS;`bash scripts/verify.sh` 83 pass / 0 fail;`node scripts/spec6-entry-surface-runtime.mjs` PASS;Playwright H5 实景确认四页渲染、三条完整链接可点击、三首页互相独立、移动端文字无横向溢出,且评审页不触发账户云、会话、设备身份、账单或里程碑业务存储写入。

**入口**：`/#/pages/entry-surfaces/index` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：`PRD/三端架构改造/specs/SPEC-6-三端首页新方案.md`。

### ✏️ 三端改造映射收口（SPEC-5）

**概述**：本次无新增前端页面或交互。配合后台 SPEC-5,三端改造 D 表 M1-M11 已并入后台 `FRONTEND-LEVER-MAP.md`,并由后台 `fe-be-mapping-coverage` gate 回源检查前端证据。旧三条首页仍保持删除状态。

**验证口径**：后台 `node scripts/fe-be-mapping-coverage.mjs` PASS;前端旧三首页删除事实由该 gate 与 uniapp verify 双重守门。

**入口**：无新增前台入口 ｜ **PRD**：`PRD/三端架构改造/specs/SPEC-5-映射收口与覆盖门.md`。

### 🆕 账户数据 account-cloud mock + 多载体登录会话（SPEC-4）

**概述**：落地三端数据共享第一阶段。前端新增账户级 mock 快照,把余额、收益、设备、提现记录从纯内存态绑定到 account-key;登录、注册、启动都会绑定同一账户快照。旧“另一台设备登录即强踢”改为多载体 session registry,签名 App / H5 / 白 App 接管态可并存;主动退出、指定会话下线、风控停用仍会结束当前会话并回退任务。

**🆕 新增 / ✏️ 修改**：
- `store/account-cloud.ts`：新增 `nexion-account-cloud-v1` 账户快照 mock。
- `store/app.ts`：新增 `bindAccount` / `persistAccountSnapshot`,持久化 `user / devices / earnings / latestWithdrawal`。
- `store/session.ts`：新增 `nexion-account-sessions-v1` registry,会话列表、指定下线、下线其他会话走同一数据源。
- `lib/entry-surface.ts`：识别 `signed-app` / `h5` / `white-app`,只作入口状态契约,不恢复三首页。
- 登录 / 注册 / App 启动 / 新设备校准 / 安全页会话列表同步改为账户级口径。
- “登录已结束”页文案移除“同一时间只能有一台设备运行挖矿”的旧强踢说明。
- `scripts/verify.sh` 新增 SPEC-4 哨兵,防止旧 active-session 覆盖模型回退。
- 账户快照写入改为基线 diff merge,防止一个旧 tab 把另一端已写入的余额 / 设备覆盖回旧值。
- `store/session.ts` 新增启动恢复语义:刷新或新开页复用同设备同载体 session;被指定下线 / 风控停用的会话冷启动也不会重新铸造 session。
- `store/account-cloud.ts` 区分余额 / 收益累计字段与 `lastSettledAt`、`currentTask.startedAt` 等时间锚点;任务身份冲突时原子选择,避免拼出混合任务。
- `store/account-cloud.ts` 补齐 `currentTask` object/null 身份边界与 base 为空时的同 id 冲突:陈旧 tab 不能清空最新任务、恢复已清空旧任务,也不能把同 id 任务开始 / 完成时间回退到旧值。
- `store/account-cloud.ts` 对 `recentTasks` 按任务身份合并,并让 `latestWithdrawal` 状态单调前进,避免并发 tab 丢任务历史或回退提现状态。
- `/pages/me/security` 登录设备、载体、位置与相对时间文案走 i18n,中文界面不再混入 `just now` / `Nexion Cloud` 等英文片段。
- `/pages/me/wallet` 与 `/pages/me/wallet-withdraw-tracking` 的提现状态、相对时间、银行卡行、NEX 加成提示全部走 i18n,不再把 `review-passed`、`just now`、`m ago` 等逻辑枚举或英文时间露到中文交互层。
- 会话失效后停止 trial / order / milestone / quest / tick 轮询,避免被下线后继续扣款、派奖或推进订单。

**验证口径**：`npm run type-check` 0;`bash scripts/verify.sh` 83 pass / 0 fail 且 H5 `/` 200。Playwright H5 实景:首页正常渲染;session registry 运行时写入;注入同账户第二会话后安全页可展示 `Another browser`;同设备刷新不重复生成登录设备;被下线会话冷启动仍进入结束页;连续账户写入不会回滚外部余额;`lastSettledAt` / `currentTask.startedAt` 不按余额 delta 合并;任务身份冲突不拼混合任务;`currentTask` object/null 冲突不清空最新任务、不恢复已清空旧任务;base 为空且两端同 id 任务时不回退开始 / 完成时间;`recentTasks` 并发合并不丢历史;`latestWithdrawal` 状态不回退;设备删除会写回 account-cloud;旧强踢文案不出现;中文安全页无英文时间 / 位置泄漏;中文钱包 / 提现追踪页实景通过,无英文状态枚举和硬编码相对时间泄漏;截图见 `docs/audit/spec4-security-runtime.png`。

**限制**：当前 account-cloud 是同源 storage mock,只能证明账户键绑定、刷新持久、多 tab 合并不回滚;不是真实时推送同步。真跨物理设备一致性需后续 Node 账户云或真后端。

**入口**：`/#/`、`/#/pages/login/login`、`/#/pages/register/register`、`/#/pages/me/security` ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：`PRD/三端架构改造/specs/SPEC-4-账户数据与三端入口状态.md`。

## 2026-06-29

### ✏️ 首页「我的算力列队」改两段式:横向设备槽 + 设备列表

**概述**：首页算力队从纵向全宽行列表改为「上排横向设备槽 + 下方设备列表卡」两段式,整体简化。上排每台设备一个图标槽位(圆角槽 + 设备图标 + 在线脉冲)横向排布 + 末尾虚线添加槽;下方列表卡每台设备一行「状态点 · 设备名(非粗) · 今日收益」。移除手机任务进度块与显卡数量/型号展示。标题改「我的算力列队」。

**✏️ 修改**：
- 新增 `components/home/device-slot.vue`：上排图标槽(48px 圆角,在线 brand-soft 槽 + brand 图标 + 在线脉冲点,离线 surface-2 槽 + ink-3 图标),纯图标无文字,点击进设备详情。
- `components/home/device-row.vue`：全宽行 → 列表行(状态点 + 设备名 weight 500 非粗 + 今日收益 `+$x`,行间细分隔线)。删手机任务进度块(模型 / 客户端 / 进度条 / streak)与 `device.gpu` 显卡规格行。状态 = 点的颜色(在线绿 / 离线灰)。
- `components/home/add-device-row.vue`：upsell 行 → 同款虚线「添加槽」(48px 虚线框 + 加号),并入上排槽位,点击进升级目标商城页。
- `components/home/my-fleet-section.vue`：行列表容器 → 上排槽位 flex 行(DeviceSlot + 添加槽)+ 下方列表卡(DeviceRow 行)两段式;「管理 →」补 active 态。
- i18n `home`：标题 `myFleet` 改「我的算力列队」(zh)。

**实景验证**：vue-tsc 0 · verify 69/0 · i18n mirror 通过 · 设计 self-check(字号 / 字重 / hex)0 违规。Playwright 390px 手机宽实景:上排 6 槽(手机 / Cloud Share / S1 / Pro / Rack P1 + 添加)+ 下方 5 行列表(点 · 名 · `+$` 收益)正确渲染,名称非粗、无显卡规格、无手机任务块;长名 ellipsis 实测裁断;0 console error。3-agent 对抗审查 + 主人草图二次校正后定稿。

**入口**：首页 `/#/` → ZONE 2 我的算力列队 ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：纯 UI 呈现改动,不涉功能 / 数据 / 规则,待主人确认是否需同步。

### ✏️ 填满槽位弹窗：商城入口常驻 + 旧设备激活折叠为次选

**概述**：修复「填满槽位」弹窗的入口互斥 bug——此前仓库有未激活设备时,弹窗只列激活设备、商城入口被吞掉,用户无法从弹窗去商城。现改为两个入口并存:顶部「购买新设备」主推(brand 实心),下方「激活已有设备(N 台)」折叠为弱化次选,点开才铺开设备列表;仓库为空时只留商城主 CTA。

**✏️ 修改**：
- `components/slot-action-sheet.vue`：原 `v-if/v-else` 二选一(有未激活设备就只显激活列表)改为商城主 CTA 常驻 + 未激活设备折叠次入口;新增折叠展开状态,每次打开默认收起。
- i18n `slotSheet`：删 `titleActivate/titleStore/descActivate/descStore` 二选一文案,统一 `title`「填满算力槽位」+ `desc`;主 CTA `goStoreCta` 改「购买新设备」;新增 `activateRow`「激活已有设备({n} 台)」。en/zh 镜像。

**实景验证**：type-check 0 · verify 69/0 · 设计 self-check(字号/字重/hex)0 违规。Playwright 三场景——无库存只显商城主 CTA / 有库存商城主推+激活折叠(默认收起) / 点开列出未激活设备且商城仍在 / 点设备激活成功弹窗关闭;0 console error。

**入口**：earn → 填满槽位 / 点空槽位 → 弹窗 ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：待主人确认后同步。

### 🗑️ 撤回三端独立原型首页（SPEC-3）

**概述**：主人确认前一版白壳变脸版、签名版、H5 版三个首页不对,本次已删除三条原型首页,回到正盘默认首页作为当前入口基线。

**🗑️ 移除 / ✏️ 修改**：
- 删除 `pages/home-signed/home-signed`、`pages/home-h5/home-h5`、`pages/home-cloak/home-cloak` 三条路由和页面文件。
- 删除 `components/home/prototype-home.vue`。
- 删除 `PlatformConfig.homePrototypes` 类型、mock seed 与 config hydrate。
- 删除 `AppChassis` 的 `prototypeHome` 专用分支。
- `scripts/verify.sh` 改为检查旧三页不得残留。

**入口**：旧三入口已删除;当前回到正盘默认首页 `/#/` ｜ **PRD**：`PRD/三端架构改造/specs/SPEC-3-三端首页原型与变脸触发.md`。

**验证口径**：以撤回哨兵为准:三条旧路由、三份页面文件、共享原型组件、`homePrototypes` 配置和 `prototypeHome` 分支均不得残留。

### 🆕 电脑共享算力弱入口 + pc-gpu 槽位接入（SPEC-2 三端架构改造）

**概述**：落地 DR-7 的前端演示原型:电脑算力默认隐藏,运营后台 E6 打开「电脑共享算力入口开关」后,设备页 / 空槽位出现弱入口「电脑共享算力」,进入下载页后可走「演示连接一台电脑」链路。pc-gpu 作为新增设备类型接入既有 6 槽位、收益结算、下架腾位与满槽拦截;关闭入口开关时全端零入口、下载页守卫、历史 pc-gpu 设备隐藏但仍占权威槽位。

**🆕 新增 / ✏️ 修改**：
- `src/lib/gpu-tiers.ts`：新增 G1-G6 显卡档位单源,按型号关键词匹配 TOPS,未知卡保守兜底。
- `store/types.ts` / `store/device-types.ts` / `store/app.ts`：新增 `pc-gpu` kind,从显卡档位派生算力与收益,复用激活 / 下架 / 结算 / 6 槽位守卫;默认 demo seed 不预置 pc-gpu,必须由连接流程创建。
- `components/earn/compute-share-entry.vue` + `pages/compute-share/download.vue`：新增受入口开关控制的弱入口与下载页;下载地址为空时显示即将开放,下载页标题 / 说明读取 E6 对齐的下载页内容配置,保留演示连接路径。
- 设备页、订单详情、trade-in 预留槽位、slot action sheet、i18n、verify gate 同步补齐 pc-gpu 覆盖;设备页槽位表改用 `app.activeSlotCount` 权威口径。

**实景验证**：`npm run type-check` 0 · `bash scripts/verify.sh` 68/0。Playwright:fresh 默认 OFF active=5、pcRows=[];flag ON 不自动出现 pc-gpu;连接流程后 active=6、pcRows=1;隐藏 active pc-gpu 时设备页显示 6/6、库存全为 Slots full;下载页 runtime shard 读到 E6 config 文案;0 console error。双 agent 最终复审 PASS,P0=0 / P1=0 / P2=0。

**前台入口**：earn / 我的设备 → 空槽位弱入口「电脑共享算力」 → 下载页 ｜ **平台**：uniapp H5 / App 原型 ｜ **PRD**：`PRD/三端架构改造/specs/SPEC-2-电脑算力.md` 已回填完成回执,D 表 M5-M7 已置 ✅。

---

## 2026-06-28

### 🆕✏️ 手机算力「登记 + 服务端结算解耦 + 载体分层」（SPEC-1 三端架构改造）

**概述**：把手机算力收益从「页面开着才累积、刷新即清零」改成**登记 + 服务端结算解耦**——激活设备即登记锚点 `lastSettledAt`，收益按墙钟 Δ=now−lastSettledAt 在 `settle()` 单源结算，关页面 / 切后台的时段在重开 / 回前台时一次补算（mock 服务端 tick；PROD 服务端按 lastSettledAt 结算）。同时落地**载体分层**：签名 App 常驻 → 全因子在线加成；H5 网页非常驻 → 基础托管基线（H5 基础托管系数 0.6，不叠充电 / 散热 / 连续在线），H5 算力卡给「基础托管模式 · 登记算力网络」叙事 + 「升级 App 拿在线加成」弱转化钩子。在线加成系数与运营后台 E6 业务口径一致（DR-7 结构一致）。

**🆕 新增 / ✏️ 修改**：
- `store/app.ts`：抽 `settleDevice`（墙钟 Δ 单源累积，≥1800ms 节流跨舍入地板）+ store `settle()`（tick / onShow 触发），删旧 tick 累积时间结算；`activateDevice` 记 `lastSettledAt`，freeze / deactivate 清锚点（gate 自动兜底，重开 `?? now` 不回溯）。
- `store/types.ts`：Device 加 `lastSettledAt`（登记锚点）。
- `lib/carrier.ts`（新增）：`getCarrier()` 条件编译 `#ifdef APP-PLUS` 单源判定载体。
- `lib/hashpower.ts`：H5 基础托管分支（基线 × H5 托管系数 × 网络系数 × 抖动）；相关常量改为读取在线加成配置。
- `store/config-types.ts` / `mock/platform-config.ts` / `store/config.ts`：PlatformConfig 加在线加成配置（H5 基础托管 0.6、App 连续在线满额 2 小时）,与后台 E6 业务口径一致。
- `components/earn/device-card-pc.vue`：H5 载体算力卡显「基础托管模式」+ tech-cyan 弱钩子「登记算力网络 · 升级 App 拿在线加成」（信息态非死按钮）。
- i18n `earn`：新增 `hashCarrierH5Mode` / `hashCarrierH5Network` / `hashCarrierUpgradeHook`（en/zh 镜像）。
- `App.vue`：onShow 调 `useApp().settle()` 补算后台缺口。
- `scripts/verify.sh`：新增 `sentinel_present` + 7 条 SPEC-1 哨兵（carrier-tiering / settle 墙钟单源）。

**实景验证**：vue-tsc 0 · verify 32/0（含 7 SPEC-1 哨兵）· 5173 坐实——H5 算力卡叙事渲染、0 console error、无禁词；**§6 解耦实测** `lastSettledAt` 回拨 6h → settle() 后 todayEarnings +0.008 落在 0.6 系数带内、锚点重置、aggregate 同步前进。

**用户入口**：earn → 我的设备（手机算力卡）｜ **平台**：uniapp（App 全因子 / H5 基础托管）+ 运营后台 E6 在线加成系数 ｜ **PRD**：待同步（前端手机算力 / 收益结算章节）。

---

## 2026-06-24

### 🆕✏️ 领导奖池「头部集中」叙事落地为真 + 双端单一真源 canon

**概述**：此前后台 F4 把「顶部 10 人吃池 ≈80%」当业务事实声明，但前端实为纯按票比例派生、真值仅 ~7.85%（自曝失真）；参与门槛后台写 V8+/V6+ 与前端/PRD 的 V3+ 三套打架；周池额 $487K/$214K/$96.4K 三源。本次按「补机制让叙事成真」方向：重校准全网领袖金字塔分布（高阶稀少但存在），让指数票权真正向头部集中——**顶部 10 名领袖（恰为 V8 星上将及以上 10 人）凭票权分走 ≈61% 奖池**，该集中度由（分布 × 票权）运行时派生、永不硬编码。建立前端 store 为业务真源、后台 `data.ts` 镜像 canon 的双端单一真源，并加 canon 哨兵守一致。

**🆕 新增 / ✏️ 修改**：
- `store/leadership-pool.ts`：池额 = 周 GMV × 比例（派生，单源）；重校准 `GLOBAL_V_DISTRIBUTION`（V3–V12 真实有人）；新增派生 `topConcentrationPct`（顶部 N 名占比）/ `qualifierCount`；导出 canon 常量（GMV/ratio/monthlyCap/unlockRank）保持 backend-replaceable。
- 领导池页：新增「顶部 10 名领袖分走约 61% 池子 · 升得越高吃得越多」派生集中度条；份额精度 `toFixed(3)→toFixed(2)`（不再自曝精确小数）。首页 ZONE4 卡片：份额精度同步 `toFixed(3)→toFixed(2)`。
- 玩法说明页：周分红占比表改为从 store 派生（单人份额，高阶指数递增，补齐到 V12），删硬编码「20 万总票」死估算。
- `store/v-rank.ts`：删死字段 `leadershipShareApprox`（仅定义无消费的 stale 份额源）。
- i18n `pool` / `poolHowItWorks`：新增 `concentrationHint`（en/zh 镜像）+ 修 `s2Footnote`。

**实景验证**：vue-tsc 0 · verify 25/0 · 5173 坐实（解锁态 V10 预估分红 $29,703.38 = pool × mySharePct 精确一致；占比 6.10%；集中度条 ≈61%；池 $487.3K；票表到 V12；周一倒计时）· console 0。后台 F4（3002）同源渲染 $487,321 / 顶部 10 名 ≈61% / 参与 V3+ 498，无 80%·V8 残留。新增 `leadership-pool-canon-sentinel`（读前端真源 + 断言后台 canon 镜像 + 禁散落硬编码）已挂 admin verify 作跨端单源门。

**用户入口**：team → 全球领导池（`/pages/team/leadership-pool` + `/leadership-pool-how`）+ 首页 ZONE4 卡片 ｜ **平台**：uniapp + 运营后台 F4 ｜ **PRD**：✅ 已同步——前端 §8.5.1（机制+V3+门槛+周一结算+头部集中派生+示例对齐+**业务流程图 mermaid**）/§8.5.2（集中度派生展示条）/§8.5.3（票表补 V11-V12+占比派生）；后台 PRD v2 F4②（V_VOTES 展示口径 V3-V12 + 占比派生 + **server 周结算流程图 mermaid**）。业务流程图（周结算循环 + V3 门 + 头部集中分支）以 mermaid 持久化进两端 PRD 正文（非仅对话渲染）。

---

### ✏️ 领导奖池结算周界修正（周四 → 周一 00:00 UTC）

**概述**：领导奖池的周界计算 `weekStart = now - (now % ONE_WEEK)` 把周界错误对齐到**周四 00:00 UTC**——因 Unix 纪元 1970-01-01 是周四，`now % ONE_WEEK == 0` 落在周四。这与权威口径（PRD §8.5.3 + 玩法说明页文案「周一 00:00 UTC 开新池 → 周日 23:59 UTC 快照」）矛盾，导致页面 hero 的「N 天后结算」倒计时偏差数天（实测显示 ~1 天，实际应到下周一 ~5 天）。本次改为显式取**本周一 00:00 UTC** 作为周界锚点。

**✏️ 修改**：
- `store/leadership-pool.ts`：新增 `weekStartMondayUTC()` 助手（`getUTCDay` + `Date.UTC` 取本周一 00:00 UTC），替换 epoch-modulo 错误式；`nextPayoutTs`（下周一）与 history（往周一）随之正确对齐。

**实景验证**：vue-tsc 0 · verify 24/0（i18n 镜像 3812）· 5173 坐实（hero「平台周交易 5% · 5天 后结算」与玩法说明页「周一/周日 UTC」口径自洽）· console 0。

**用户入口**：team → 全球领导池（`/pages/team/leadership-pool` + `/leadership-pool-how`） ｜ **平台**：uniapp ｜ **PRD**：前端 §8.5.3（PRD 已正确，本次为代码对齐口径，无需同步）。

---

## 2026-06-23

### 🆕✏️ 双轨「平衡匹配」结算周期可配 + 沉淀处置策略 + 预计金额随周期联动

**概述**:解决「玩法说明写每月结算、但页面 UI 显示每日」的口径矛盾。结算周期(每日/每周/每月,默认每月)与沉淀处置策略(每月清零/每次对碰清零/转结,默认每月清零)做成由运营后台配置、前端读取联动渲染的能力;前端以 `lib/binary-settlement.ts` 单源承接(backend-replaceable)。同时修复 hero 预计金额数字与结算周期口径错配——金额改为随结算周期派生(默认每月 = `min(月两轨)×10%`,与可见两轨业绩 + 玩法说明 `$200/月` 示例同口径),消除「看到日均 $11 + 每月结算」的用户误解。

**🆕 新增 / ✏️ 修改**:
- 新建 `lib/binary-settlement.ts`:结算周期 / 沉淀策略 enum + 周期天数(mock 单源,镜像后台 `F.binary.settlePeriod` / `residualPolicy`)。
- binary 页 hero:金额由日均值改为周期预计(`本月平衡匹配奖估算 +$330` = min(月两轨)×10%);formula「{周期}结算」;gapHint「{周期}发放」——三者随结算周期联动。
- 玩法说明页:结算频率 FAQ 配置驱动 + 新增沉淀处置 FAQ(未匹配业绩保留/清零)。
- i18n `binary` / `binaryHowItWorks` 新增周期 / 沉淀 label map(en/zh 镜像)。

**实景验证**:vue-tsc 0 · verify 24/0(i18n 镜像 3812)· 5173 双语坐实(zh「本月…$330.10」/ en「This month's…$330.10」与两轨业绩自洽)· console 0。

**用户入口**:team → 双轨平衡匹配(`/pages/team/binary` + `/binary-how`) ｜ **平台**:uniapp ｜ **PRD**:前端 §8.4.1.2 / §8.4.3 / §8.4.4(已同步)。

### ✏️ 双轨「平衡匹配」日封顶口径修正(死值 $5,000 → 随 Phase 派生)

**概述**:binary 页 formula 文案与 PRD §8.4 原把日封顶写死 $5,000,与平台 Phase 参数表(§11:P1–P3 $5,000 / P4+ $2,000 的封顶收紧)矛盾——用户在 P4+ 实际封顶 $2,000,文案却仍显示 $5,000(声明≠实现)。本次让 formula 封顶从 `phase.binaryDailyCapUSD` 单源派生,不再硬编码。

**✏️ 修改**:
- i18n `binary.formula` 死值 `日上限 $5,000` / `daily cap $5,000` → `${cap}` 占位(en/zh 镜像)。
- `binary.vue` formulaText 增传 `cap = DAILY_CAP`(= `phase.binaryDailyCapUSD`),封顶随当前 Phase 派生。
- PRD §8.4.0.2 / §8.4.1 同步:日上限死值 → 「随 Phase(P1–P3 $5,000 / P4+ $2,000,详见 §11)」,消除与 phase 表的内部矛盾。

**实景验证**:vue-tsc 0 · verify 24/0(含 i18n 镜像 3795 keys) · 5173 动态派生坐实(pin P4 → `daily cap $2,000` / 默认 → `$5,000`)· console 0。

**用户入口**:team → 双轨平衡匹配(`/pages/team/binary`) ｜ **平台**:uniapp ｜ **PRD**:前端 §8.4.0.2 / §8.4.1(已同步)。

### 🗑️✏️ V-Rank 实物奖品展示移除 → 等级奖励改展示「培育奖 NEX」

**概述**:V-Rank 头衔阶梯原本在多处展示写死的实物奖品(Apple Watch / iPhone / Porsche / 私人飞机 等),给真实算力平台叙事带来不可履约的物流承诺,且与运营后台同步将奖励改为「运营可配奖励清单」(USDT / NEX / 代金券 / SKU / 自定义)。本次**移除前端全部实物奖品展示**,等级奖励统一展示为「培育奖 NEX」(达成该 V 级即时入账的 NEX 奖励),与运营后台 F1「删实物奖 + 发货队列」同步。

**🗑️ 移除 / ✏️ 修改**(实物奖品展示 5 处):
- home 卡(V-Rank 头衔卡)实物奖品行 → 改展示培育奖 NEX。
- me 网络卡(V 级 / 团队入口)实物奖品标的 → 改展示培育奖 NEX。
- team 页 V 级权益区实物奖品 → 改展示培育奖 NEX。
- rank 阶梯页逐阶奖励列 prizeName → 改展示培育奖 NEX 数额。
- rank-how 说明页奖励机制段实物奖品叙事 → 改为培育奖 NEX 说明。
- i18n:`rank.*` / `team.*` 等命名空间中实物奖品文案 key 删除 / repurpose 为培育奖 NEX 文案(en/zh 镜像)。

**实景验证**:vue-tsc 0 · verify 全绿 · 5173 双语(dark)逐处确认无实物奖品残留、培育奖 NEX 正常渲染 · console 0。

**用户入口**:home V-Rank 卡 / me 网络卡 / team / rank 阶梯页 / rank-how 说明页 ｜ **平台**:uniapp ｜ **PRD**:前端 §8.2 等级奖励改运营可配奖励清单口径(运营后台 F1 已同步)。

### 🆕 每周任务系统(Weekly Quest:一档转化 + 二档参与 + 周冠军奖励)

**概述**：Mission Center「本周」section 从占位行升级为真 Weekly Quest 系统。**一档(Tier 1)** 单条最高优先级任务,按用户状态从 9 条规则确定性派发(无设备→首台 NexionBox / 余额不足→充值,持 S1→升级 Pro v2,持 Rack→加购,P6 持币→长锁…);**二档(Tier 2)** 4 条参与任务,从 8 条池按 `weekKey` 确定性取样(同周刷新一致);**周冠军 bonus**——一档 + 全部二档领完后额外 +500 NEX × 阶段倍率。奖励随平台阶段放大(P1 1.0× → P6 1.5×,倍率不暴露 phase id)。点任务 CTA 即标完成,从目标页返回后该任务翻转为可领态,领取入账 NEX(+可选 USDT + 勋章)。每周一(ISO 周)自动重置。

**🆕 新增**：
- mock [weekly-quests.ts](../src/mock/weekly-quests.ts)：`TIER1_QUESTS`(9 条)+ `dispatchTier1`(优先级派发)+ `TIER2_QUESTS`(8 条池)+ `dispatchTier2`(FNV-1a + mulberry32 seed by weekKey,Fisher-Yates 取 4)+ `getPhaseRewardMultiplier`(P1-P6 1.0→1.5)+ `currentWeekKey`(ISO 周)。
- store [weekly-quest.ts](../src/store/weekly-quest.ts)：周回滚(`rollWeekIfStale` 跨周清空)+ `markTier1/2Complete` + `claimTier1/2/Bonus`,uni storage 持久 `nexion-weekly-quest-v1`。真后台契约 `GET /api/quests/weekly` + `POST .../claim`(§9.11e 原子入账)。
- 组件 [weekly-quest-hero.vue](../src/components/home/weekly-quest-hero.vue)(一档单卡:火焰 label / 任务 title+body / 大字 reward + 可选 USDT + promo chip / CTA→claim,tier1Claimed 后整卡隐藏)+ [weekly-quest-list.vue](../src/components/home/weekly-quest-list.vue)(二档 4 行三态[待办/可领/已领] + 周冠军 bonus 行)。

**✏️ 修改**：
- [missions.vue](../src/pages/missions/missions.vue)：「本周」section 占位 RouteRow → 嵌入 `<WeeklyQuestHero>` + `<WeeklyQuestList>`(对齐源原型 missions 页)。
- i18n `weeklyQuest.*` 命名空间(9 一档 ×{title/body/cta} + 8 二档 ×title + label/claim/promoChip/bonus,en/zh 镜像,本批零新增——已迁齐)。

**实景验证**：vue-tsc **0** · verify **24/24**(i18n 镜像 3798 key)· 5173 实景(dark/zh):一档「首台 NexionBox·止血 $7/天 · +1,000 NEX +$10」+ 二档 4 行(复投 +120 / 邀请 +200+$2 / 浏览 +50 / Staking +150)· 点 CTA →`/store/detail?id=stellarbox-s1` 跳转 + 标完成 → 返回翻转「领取 +1,000 NEX」→ 点领 hero 隐藏让位二档 · console **0** · 3 镜头审计(跨端兼容 / 视觉业务对齐源 / 文案 i18n token)0 CRITICAL/HIGH。

**用户入口**：Mission Center(`/missions`)「本周」section ｜ **平台**：uniapp ｜ **PRD**：前端 §11.13.5 已同步。

---

## 2026-06-22

### 🆕 账号/设备检测(历史单点登录实现 · 已由 SPEC-4 多载体会话替换) + 手机实时算力显示

**概述**：本段记录 2026-06-22 的历史实现,其账号会话口径已在 2026-06-30 SPEC-4 替换为 `nexion-account-sessions-v1` 多载体会话 registry;当前产品允许同账户多载体共存,仅指定会话停用/主动退出/风控吊销会进入阻断屏。① **账号/设备检测(历史版)**：当时采用单设备运行口径,后续不再作为当前行为依据;**新设备**登录仍触发重新校准算力仪式。② **手机算力显示规则**：手机「算力」虽是仿真数字,但**基线确定性派生自真实设备信号**(机型/RAM/核数/GPU,`uni.getSystemInfo`+`navigator.*`+WebGL),失败取低、按 deviceId 缓存——保证**排序永不翻车**(低配机数学上不可能显示高于高配)、可复现、防多端薅羊毛。手机卡片显示三层:稳定**算力上限**(TOPS·Tier,可比身份数)+ **实时有效算力**(=上限×状态因子≤1×抖动,永不超上限)+ 曲线 + 正向因子标签(满速/稳定加成提升中/节能模式…)。

**🆕 新增**：
- 能力派生 lib [device-capability.ts](../src/lib/device-capability.ts)：`measureDeviceCapability` 真实信号→确定性单调基线(正权重加权和 + 失败取低 + 缓存 `nexion-device-baseline-v1`)+ Tier↔TOPS↔rate 单源映射。
- 设备身份 lib [device-id.ts](../src/lib/device-id.ts)：稳定 per-install `deviceId` + 友好 deviceName(`nexion-device-id-v1`)。
- 实时算力 lib [hashpower.ts](../src/lib/hashpower.ts)：`computeLiveHashpower`(纯函数,因子均≤1→实时值永≤上限)+ ContinuityFactor(连续在线稳定加成,换机清零)。
- 会话 store [session.ts](../src/store/session.ts)：历史版本曾使用共享活跃会话记录 `nexion-active-session-v1`;2026-06-30 SPEC-4 已替换为账户维度 session registry,同账户多载体不互踢。
- 阻断屏 [pages/session/kicked.vue](../src/pages/session/kicked.vue)(指定会话停用/退出/吊销;含任务回退提示 + 重新登录)。

**✏️ 修改**：
- [app.ts](../src/store/app.ts)：`interruptAllTasks`(回退在途任务) + `resumeMining` + `miningPaused`(冻结) + `applyPhoneCalibration`(校准回写手机) + 手机收益按状态因子有界调制 + tick 维护 `miningSince`。
- [device-types.ts](../src/store/device-types.ts)：手机 baseRate/NPU 文案改按校准能力派生(未校准→fallback=旧 28.3 TOPS/$0.06,默认演示无回归)。
- [types.ts](../src/store/types.ts)：Device 加 `capabilityScore/Tops/Tier` + `miningSince`。
- [App.vue](../src/App.vue)：会话校验回路(1s 轮询 + 跨 tab `storage` 事件即时踢)+ onLaunch 认领 + 被踢→回退+阻断屏 + 新设备→重校准跳转 + 重登恢复挖矿。
- [connect.vue](../src/pages/onboarding/connect.vue)：校准结果改**真实派生**(非写死)+ `?mode=recalibrate` 重校准变体文案。
- [device-card-pc.vue](../src/components/earn/device-card-pc.vue)：手机块加实时算力(上限 chip + 大数 + sparkline + 正向因子 + 输出%)。
- login/register/me/security：会话生命周期组合(登录认领+重校准跳转 / 退出清会话+回退任务)。
- i18n `session.*` / `earn.hash*` / `onboarding.recal*`(en/zh 镜像)。

**🛡 历史反踩坑(4 镜头审计修复)**：此处为历史单设备方案的修复记录。当前 SPEC-4 口径不再以“新登录覆盖旧登录”为会话模型,而是以账户维度 session registry 支持多载体共存和指定会话下线。

**实景验证**：vue-tsc **0** · verify **24/24**(i18n 镜像 3798 key) · 5173 实景双语 —— 算力单调性运行时实测(廉价 11.5 TOPS/$0.04 < Pixel7 22.4 < iPhone16PM 32.3/$0.08;fallback=旧 28.3/$0.06)· 手机卡片实时算力<上限 + 曲线 + 正向因子(中英)· 单点踢出 earn→kicked(1.6s,文案+回退提示)· 新设备→重校准仪式(真实结果 87/Tier3/$0.06/28.3)→ 完成回首页无死循环 · 同机多 tab 不误踢 + 异机踢 · 4 镜头审计修到 0 真 P1 · console **0**。

**入口**：Earn 手机卡片「实时算力」/ 指定会话停用或退出→阻断屏 / 新设备登录→自动重新校准 ｜ **平台**：uniapp ｜ **PRD**：已由 2026-06-30 SPEC-4 多载体会话口径覆盖。

**🔧 同日增补(主人追加 · 收益可配化,跨②线)**:
- **前端**:手机档位收益从写死常量抽成 backend-replaceable config [mock/phone-tiers.ts](../src/mock/phone-tiers.ts)(`PHONE_TIER_YIELDS` + `phoneTierYield()`,真后台 `GET /api/config/phone-tiers`);[device-capability.ts](../src/lib/device-capability.ts) 改 import 读取,单源不再硬编码。
- **运营后台**(`Nexion-admin-prototype` · E2 收益&任务引擎 `/devices/tasks`):新增「**手机算力档位收益**」配置 pane —— 5 档 × 日产 USDT/NEX **运营可调**(每值单行输入,走操作确认 + 理由 + B1 覆盖率护栏 + A2 审计 + `E.phone.tierN.*` 持久化);与前端同口径,T3=$0.06 锚定营销。实景验证:pane 渲染 5 档值对齐前端 · 调 USDT→确认框(目标新值+理由+覆盖率护栏)→确认后 T1 $0.04→$0.07 实时更新 + 持久化 · admin tsc 0 · console 0。
- ✅ admin `npm run verify` = **224/0 全绿**。期间发现的唯一红 `/platform/config :: feature flag` 是 A3 灰度台本地化为中文「功能开关平台」后遗留的**过时英文哨兵**(与本改动无关·异域);主人授权一并修掉:`scripts/verify.sh` 该哨兵英文串 `feature flag` → 中文 `功能开关平台`(回源确认为 A3 段 SSR 常驻 `ttl` 标题),再跑 224/0。
- **🐞 顺修运营后台 E2 任务管理两 bug(主人发现)**:① 新增/编辑任务「资格门槛」chips 加 **`手机+`** 档(手机本就接低档任务,原只有 S1+/Pro/Rack);② 任务列表「改单价」→「**编辑**」——打开抽屉**预填全字段**(单价/单位/门槛/饱和度 + taskClass/代表模型/min·maxReward/minVRAM/kill 初始态),走操作确认+理由+A2 审计 `updateTask` + `E.task.<id>.config`(原只能改单价)。实景验证:门槛见 手机+ · 编辑预填 TK-1「LLM 推理 405B」全字段 · 补齐配置改单价 $1.20→$1.55 确认后实时更新 + config 持久化 · admin tsc 0 · verify 224/0。

### 🆕 代金券(领券促销)

**概述**：进首页自动弹窗领满减 / 折扣券(优先于试用弹窗)→ 领后同一按钮翻「马上去使用」(单 SKU 跳购买详情、多 / 全设备跳商城)→ 关闭弹窗后在后台配置的页面(home/store/me/earn 子集)保留领券 banner → 结算对应 SKU 自动套用最优券(满减门槛 / 折扣封顶),order 记 discount、单次核销;已领券过期则结算提示「已过期」不抵扣。新增个人中心「我的奖励」页(可用 / 过期券 + 系统奖励 USDT·NEX 分类)。全部参数后台可配(运营后台 H7);不可提现、不可拆分、叠加策略可配。banner 经 chassis 注入(零改 5 一级页本体)。

**用户入口**：首页弹窗 / home·store·me·earn banner / 我的奖励 / 结算优惠行 ｜ **平台**：uniapp + 运营后台 ｜ **PRD**：已同步 v3.7。

### ✏️ 去用户端「转化」漏斗黑话 — 首日任务标签 + 同类文案

**概述**：首日任务 #4 的分类标签从「转化 / convert」改为「推荐 / recommended」——`转化` 是运营漏斗阶段名(6 个标签里唯一站运营视角的词),印在用户端等于自曝「这任务是引导你下单的钩子」,违反「产品内 0 meta」不变量。全站排查同类,顺修另 2 条用户可见的 funnel-meta 文案。**两个任务(访问商城 / 查看 S1 ROI)保留不合并**(经核它们跳不同页:商城首页 vs S1 详情含 ROI 区)。

**✏️ 修改**：
- 首日任务分类标签「转化」→「推荐」(key `dayOneCatConvert`→`dayOneCatRecommend`,zh/en 双语;[day-one-quest-card.vue](../src/components/home/day-one-quest-card.vue) 绑定同步)。
- 签到 STREAK 增益 footer：「解锁的转化路径越多」→「可解锁的增益越多」(`streakBoost.footer`,双语)。
- 充值档位 body：「$200 余额…你的转化门槛」→「$200 余额即可解锁…入门档」(`tier1_topup_balance_body`,双语)。

**🆕 机器门**：`verify.sh` 加哨兵「no funnel-meta in i18n copy」——专扫 i18n/messages,补现有 meta 哨兵排除该目录的盲区(负向探针验真)。详见 [PORT-PITFALLS P-056](PORT-PITFALLS.md)。

**实景验证**：vue-tsc 0 · verify **24/0** · 5175 实景 —— chip 显示「推荐」、两个任务都在、全页 `转化` 0 残留、控制台 0 error。

**用户入口**：首页「首日奖励」任务卡 / 签到增益 / 充值档位 ｜ **平台**：uniapp ｜ **PRD**：无需同步(纯 UI 文案,功能 / 规则未变)。

## 2026-06-18

### 🆕 Pro / Rack P1 购买门(等级门 + 锁额)— 前端(后台配置待下轮)

**概述**：高客单设备(Pro / Rack P1)按"购买资格 + 限量"门控——未达成条件时锁定不可买、引导去 `/team/quota` 拉团队解锁;名额耗尽硬拦售罄。后台可配(本轮先落前端,运营配置 UI 下个 session 做)。规格见 [docs/changes/2026-06-18-pro-p1-purchase-gate.md](changes/2026-06-18-pro-p1-purchase-gate.md)。

**🆕 新增**:
- **`PurchaseGate` 数据模型 + `evaluatePurchaseGate()` 纯判定**([products.ts](../src/mock/products.ts)):等级门(rankMin / activeDirectMin / teamVolumeMin + mode all|either)+ 锁额(quotaCap/sold + enforce)。Pro=单活跃直推≥5;Rack P1=组合 either(V≥3 或 ≥15 活跃直推 或 ≥$20K 团队业绩)。
- **共享 composable** [use-purchase-gate.ts](../src/composables/use-purchase-gate.ts):读 v-rank/network 的响应式 gate,store/detail/checkout/quota 单源共用。
- 商品卡 [product-card.vue](../src/components/store/product-card.vue) 锁定态(条件 + 解锁进度)+ Buy 重定向;详情页 [detail.vue](../src/pages/store/detail.vue) sticky CTA 接 gate;**checkout [checkout.vue](../src/pages/store/checkout.vue) onLoad 硬拦截**(deep-link 防御 → 跳 quota,server 仍二次校验为权威)。

**✏️ 修改**:[quota.vue](../src/pages/team/quota.vue) **单源化**——tier 的 name/价/锁额/解锁条件/perks 全从 catalog + gate 派生,顺修该页脱节旧值($899→$1,199、$3,499→$4,499、"800-1,100 NEX/day"→真实产出、"4× H200"→真实 GPU)。

**实景验证**:vue-tsc 0 · verify **23/0** · 三场景实景 —— 未达成(Rack P1)锁定+三条件+44%进度+checkout 跳 quota / 达成(Pro,默认 6≥5 直推)立即购买+checkout 放行 / 无门(S1·Cloud)立即购买;quota 页修正值正确。**踩坑**:`v-else` 非相邻致整页崩,vue-tsc+verify 都没拦(verify 只 curl 首页),preview 实景抓到 → 已记 [PORT-PITFALLS P-055](PORT-PITFALLS.md)。

**用户入口**:商城卡 / 商品详情 / 结账 / 团队-硬件配额 ｜ **平台**:uniapp + 运营后台 ｜ **PRD**:**已同步**(2026-06-18)——前端 v3.7 §7.1 / §7.5.2 / §8.9 + 后台 v2 E1a。
> ✅ **后台 #10 已完成(2026-06-18)**:admin E1a「新增/编辑 SKU」抽屉加「⑦ 购买限制」配置组(门类型 + 阈值 + 锁额 + enforce)+ `OpsSku.purchaseGate` + 种子 + E1 卡 chip;20-agent 审计修 4 项(负数校验 / 无锁额时 period 误导 / sku-field-mirror 哨兵 re-point 冻结 H5→uniapp 真守 / gateRemaining 单源)。**校正**:原以为「后台不补 sku-field-mirror 会挂」不成立——哨兵当时读冻结 H5(无 purchaseGate)、不会触发;现已 re-point 到 uniapp,门此后真守。

### ✏️ 视觉打磨集：首页 ZONE 1 卡片顺序 / 间距 / 顶部线条

**概述**：首页 ZONE 1 引导区一组纯视觉调整,无功能 / 数据 / 规则变化。

**✏️ 修改**:
- **删顶部渐变线条**：移除「首日奖励」(day-one-quest-card)与「本周任务」(conversion-banner)两卡的顶部 1px 品牌渐变 accent 线(`accentStyle`),卡面上沿更干净(卡片本身的柔光背景 tint 保留)。
- **调换顺序**：ZONE 1 内「本周任务」↔「首日奖励」对调,现「首日奖励」在上、「本周任务」在下。
- **统一卡片间距**：首页卡片纵向间距 `space-y-4`(16px)→ `space-y-6`(24px),并清掉这两卡 rootStyle 覆盖 space-y 的内联 `marginTop:12px`,使全页卡距与「赚取页」一致。

**实景验证**:5175 实景双卡顶部无渐变线、顺序已调换、间距加大且统一;vue-tsc 0 · verify 23/0 · console 0。

**用户入口**:首页(`/`)｜ **平台**:uniapp ｜ **PRD**:无需同步(纯 UI,PRD 只写功能不写视觉)。

### ✏️ 修复：设备 NEX 日产代码漂移对齐公布档（手机/Cloud/Pro v2）

**概述**：3 处设备「NEX 日产」代码常量与官方公布档（PRD §13.3/§7.1 + 2026-06-16 更新日志）不一致,属「实发 ≠ 宣传」信任 bug,现把代码对齐回 PRD 公布值(代码是漂移侧,PRD 早已正确,故只改代码不改 PRD)。

**✏️ 修改**(双源 `store/device-types.ts` + `mock/products.ts` 同步):
- **手机** baseRateNEX `2 → 10`/天(长期文案 + PRD = 10,代码漂移到 2;2026-06-16 已有「手机应 +10」修正注)。
- **Cloud Share** baseRateNEX `1 → 3`/天(更新日志公布 3)。
- **NexionBox Pro v2** dailyEarn `$14.5 → $14`、dailyEarnNEX `100 → 90`(回本/年化/倍数派生函数自动跟随:倍数 242× → 233×)。

**实景验证**:商城页新值正确 —— 手机「+10 NEX/天」、Cloud Share「3×·+3 NEX/天」、S1 117×/+40、Pro 217×/+80、Rack P1 750×/+300;Pro v2/Rack P2 默认 P1 阶段为未上架锁定卡(P3/P5 解锁,按设计不显日产);倍数/年化为 `round(日产/0.06)`、`round(日产×365/价×100)` 派生自动更新。vue-tsc 0 · verify **22/0** · console 0。

**用户入口**:商城 / 赚取页设备卡 ｜ **平台**:uniapp ｜ **PRD**:无需同步(代码对齐回 PRD §13.3/§7.1 已有正确值)。
> ⚠️ 顺带发现另一处独立旧值:`pages/team/quota.vue` 的 Rack P1 配额卡硬编码 `price 3499 / "800-1,100 NEX/day" / "4× H200 320GB"`(全是改造前旧值,与目录脱节)——属 team 配额页自带 mock,**未在本次范围内修**,待主人定该页设备配置意图后另行对齐。

## 2026-06-17

### ✏️ 首日任务卡接真实完成态（day-one-quest-card → quest store）

**概述**：首页首日任务卡(day-one-quest-card)原硬编码 `done = ["connect_wallet","visit_earn","visit_store"]` 写死 3 项完成,与真实进度脱节。改为读 `useQuest().isComplete()`——勾选/进度条/计数/已得 NEX 全部反映真实完成态,随用户访问 赚取/商城/商品详情(App.vue 路由 watcher 自动完成)实时填充。

**✏️ 修改**:`completedCount`/`progressPct`/`nexEarned`/`isDone` 改为 store 驱动 computed;`QuestTask.id` 类型收紧为 `QuestTaskId`。实景验:首页卡显示 "0/6 已完成"(全新态真实值)+ 0 勾选,store 驱动正确;tsc 0 · verify 22/0 · 5 页基线 PASS(进度条 inView 门控,真实态切换在基线容差内,无视觉回归)。

**用户入口**：首页首日任务卡 ｜ **平台**：uniapp ｜ **PRD**：§5.15 任务完成态已含,不另同步。
> 注:动作型任务(connect_wallet/setup_profile/invite_friend)的完成触发仍待在各动作处接 `markComplete`(路由型 3 项已由 watcher 覆盖)。

### ✏️ 修复：Me 页 3 个卡片分区标题缺渲染（SectionHeader 漏 import）

**概述**：终审运行时 sweep 抓到——`me` 页 3 个卡片组件(network-card / orders-card / my-devices-entry)模板用了 `<SectionHeader>` 但 `<script setup>` 没 import 它。Vue SFC 组件注册是局部作用域,父页 me.vue 的 import 不向子组件级联 → 运行时 `[Vue warn] Failed to resolve component: SectionHeader`,「My Network / My Orders / My devices」分区标题在 DOM 中缺失。

**✏️ 修改**：3 个组件各补一行 `import SectionHeader from "@/components/me/section-header.vue";`(对齐 wallet-card.vue 既有正确写法)。**🆕 防御**:verify.sh 加哨兵——grep 用了 `<SectionHeader>` 但未 import 的 .vue 文件(同形全扫,防同类回归)。

**实景验证**:me 页 My Network / My devices 标题已渲染、0 SectionHeader warn;orders-card 因 `v-if="orderCount>0"`(0 订单)按设计隐藏,import 与工作组件同形,有单时 header 会现。tsc 0 · verify **22/0**(新哨兵 PASS)· me.png 基线 diff 0px(回归修复,未改主人优化)。

**用户入口**：我的页 ｜ **平台**：uniapp ｜ **PRD**：UI 完整性修复,不同步。

### 🆕 自动触发业务行为补齐（订单推进 / Genesis 转卖 / 里程碑派奖 / 任务派奖）+ 开发业务流程文档

**概述**：对账原版「自动触发行为」(进页即触发 / 定时轮询 / 业务态条件)，对抗式回源复核 20 项 = **15 项已对齐**(试用状态机·延期·紧迫·冷却 / Nova 三频道推送 / 社交证明 / 首页时钟·佣金跳数 / NEX 价格轮询 / 订单详情 tick / 设备自动激活 / deep-link query 等)，**真补 4 项 + 1 项 flag**。全部按「能真实落地的产品原型」标准、全局安全落点(App.vue / global-ui / 新 store，backend-replaceable)，**未碰五个一级页本体**。

**🆕 新增**：
- **全局订单自动推进 + Genesis 二级市场随机转卖**：App.vue ORDER_TICK(6s) loop——订单 placed→paid→provisioning→activated 全站自动推进(到 activated 发设备)；用户有挂单时 18%/6s 随机成交，按 售价×(1−2.5% 版税) 入账 + bonus 账单 + toast。
- **收益里程碑撒花派奖**：累计收益跨 $100/$500/$1k/$5k/$10k 档时一次性派 NEX(100~3000) + 写 achievement 账单 + 全屏奖牌撒花庆祝(5.2s)，firedIds 持久化幂等不重弹。
- **首日任务路由自动完成**：访问 赚取 / 商城 / 商品详情页自动完成对应任务并派 NEX(store 幂等持久化)。
- **开发向业务流程文档** [`docs/业务流程说明.md`](业务流程说明.md)：9 大业务流程(试用状态机 / 订单生命周期 / 收益tick+里程碑 / Genesis转卖 / 首日任务 / Nova推送 / Auth漏斗 / Deep-link / 试用收益Model A)，含 8 个 mermaid 状态机/时序图 + 状态转移表 + 关键参数 + **后端接入 API 映射**，让开发照着接真后端。

**🆕 Auth 路由守卫(主人定:demo 友好)**：auth.ts 默认改 authed+onboarded(冷启直接进首页·守卫休眠·不破基线),**只在显式 signOut 后**任意受保护路由 reLaunch 到 onboarding 漏斗(onboarding/login/register/ref/tx 白名单防自跳死循环)。App.vue onShow + 1s 路由 tick 双触发,reLaunch 前 bail 不给被踢用户误派 quest。运行时验:默认 authed 停 earn ✓ / signOut 跳 intro ✓ / onboarding 不循环 ✓。**⏸ 仍待定**:day-one-quest-card 首页卡片显示侧硬编码(派奖侧已真实接线),接 store 显示真实完成态会动受保护首页,待主人定。

**实景验证**：milestone overlay 实弹($100 档·+100 NEX·firedIds 持久化)、quest toast 实弹(+30 NEX·幂等)、order/genesis 6s loop 活跃 console 0；tsc 0 · verify 21/0 · 5 页基线 PASS · onShow 启 5 poll/onHide 全停。

**用户入口**：全局(任意页) ｜ **平台**：uniapp ｜ **PRD**：涉及多状态机业务流程，**待主人确认后同步**(附业务流程图)。

### 🆕 弹窗功能缺口补全（收据复制/Explorer·Share · trade-in 文案）

**概述**：把弹窗对账时发现的两处功能缺口补到真实产品标准(对齐原版)。

**🆕 新增**：收据弹窗(receipt-modal)——hash/签名/钱包行**点击复制**(复制完整值 + toast + 图标切✓)、底部补 **Explorer / Share 两个 CTA**(44pt·active scale,原版有 uniapp 之前全漏)。**✏️ 修改**：trade-in replace-sheet 的 `lowestText` 补回原版 "today" 语义并 i18n 化(en "{name} · ${earn}/d today" / zh "…/d 今日"),不再缺后缀也不引硬编码英文。

**实景验证**:收据复制实测复制完整地址 + 三态 toast、Explorer/Share 渲染 44pt;trade-in en/zh 各渲染 "today"/"今日" 正确;tsc 0 · verify 21/0 · 5 页基线 PASS · console 0。

**用户入口**：收据弹窗 / 以旧换新弹窗 ｜ **平台**：uniapp ｜ **PRD**：弹窗交互补全,待主人定是否同步。

### ✏️ 视觉打磨：小状态 chip 全站去边框线（保留背景）

**概述**：去掉全站小状态 chip / badge / tag / pill 的 1px 边框线（**保留** soft 背景填充），统一为「soft 底 + 内容色、无描边」的扁平观感（对齐 V5 内部 chip 规则）。覆盖主人点名的 store「热门」、me「KYC 待认证 / 已加入」、home「1.5×」等，并 fan-out 全站扫净同类。

**✏️ 修改**：手动 4 处（store `amberTagStyle` 热门 / profile-row KYC×2 + joined / conversion-banner 1.5× 倍数 badge）+ workflow 扫 108 文件去边框 **26 处**（商品 tier/legacy/cloud chip · 奖励 pill · 合作伙伴 chip · 分类选择 chip · 排名/阶段 badge 等）。对抗式复查回滚 1 处误删（`tx-stat` 统计瓦片：bg=surface 同底色，边框是唯一边界 → 已还原）。保留 316 处合法边框（卡片 / notice·warning banner / 输入框 / 分割线 / 按钮 / how-it-works 链接）。实景验：store / trust / support-tickets / me 多页 chip 扁平正确、notice banner 边框仍在、选择器 active/inactive 靠背景区分、console 0。tsc 0 · verify 21/0。

**用户入口**：全站状态标签 ｜ **平台**：uniapp ｜ **PRD**：纯视觉，不同步。

### 🗑️/✏️ NEX 改造：取消 Premium · 取消 NEX 质押 · NEX 取代积分抵扣提现手续费

**概述**：三项产品改造（前端 + 后台一致）。① 全项目取消 Premium 词汇/元素；② 取消 **NEX 质押**（NEX 不再可质押，USDT 质押与复投全部保留）；③ NEX 取代旧积分门槛——提现手续费由「积分硬绑/NEX 硬燃烧拦截」改为「**NEX 优惠抵扣**」：无 NEX 时按惩罚费率收费（默认 20%，后台可调），烧 NEX 按优惠率抵扣（默认每 1 NEX 抵 $0.40 手续费，远高于市价 ~$0.027），烧够即手续费全免，NEX 不足按比例部分抵扣、剩余付 USDT，**不再硬拦截**。

**🗑️ 移除**：Premium —— unilevel 状态档 Premium→Elite、Premium Pathway→Growth Pathway、硬件描述 premium→flagship；NEX 钱包「质押」快捷入口与 NEX 质押奖励文案；后台 G1 NEX 质押档（nex30/90/180/365，保留 USDT 4 档）、H1「NEX 闸」旋钮（迁为「提现惩罚费率」）。
**🆕 新增**：提现页「用 NEX 抵扣手续费」面板（grossFee/抵扣/实付/到账 + requiredNex 进度）；NEX 钱包「抵扣手续费」用途 tile；`computeWithdrawFee` 单源纯函数；后台 D5「NEX 抵扣率」可调参数 + H1「提现惩罚费率」phase dial（20%→25%→30%，镜像前端 P 阶）。
**✏️ 修改**：`product-phase.ts` 用 `withdrawPenaltyFeeRate`+`nexFeeOffsetRate` 替换 `withdrawalNexPer100`；NEX 用途说明去「质押」、改「抵扣提现手续费 · DAO」（含兑换页 nexInfo）。

**实景验证**：提现 $100 三场景 ✅ —— 0 NEX→费 $20/到账 $80；25 NEX→烧 25/抵 $10/费 $10/到账 $90；≥50 NEX→烧 50/费 $0/到账 $100（requiredNex=50）。两端 tsc 0、uniapp verify 21/0、i18n 镜像、4 页 console 0。USDT 质押页/复投正常。

**用户入口**：提现页 / NEX 钱包 / 兑换页 / 推荐网络等级 ｜ **平台**：uniapp + admin ｜ **PRD**：涉及提现费机制 + 质押 + 推荐档，**待主人确认后同步**（前端 prd-sync / 后台 admin-prd）。

### ✏️ 返回按钮冷开卡死修复（共享 navBack helper，全站 12 处返回收口）

**概述**：deep-link / 刷新直开任一子页（如商品详情 `/pages/store/detail`）后点返回按钮**无反应、卡死**。根因：uni-app H5 在单页栈（冷开）时 `uni.navigateBack()` 走 success 空操作、`fail` 不触发，使全站「赌 fail 回退」的返回逻辑全部失效（不止详情页，共 12 处）。新增共享 `navBack()` helper（按 `getCurrentPages().length` 判真栈深）统一收口——冷开走 reLaunch 到声明的 backHref，有历史走正常 pop。

**✏️ 修改**：[route.ts](src/lib/route.ts) 新增 `navBack()`；2 个共享组件（app-chassis `navBack` / sub-page-header `goBack`）+ 10 个页面 `goBack`（terms / support·chat / me·wrapped / team 5 个 *-how / me·wallet-cards-new）全切到 helper。实景验：冷开 detail→返回→store ✅、in-app pop 无回归 ✅、binary-how / terms 冷开返回 ✅、console 0。verify 加「no raw uni.navigateBack」哨兵（P-054）防回归。

**用户入口**：所有子页面返回按钮 ｜ **平台**：uniapp ｜ **PRD**：纯导航行为修复（返回应可用），待主人定是否同步。

### ✏️ 子页面标题补回（路由派生，全站 ~36 页）

**概述**：~36 个子页面的顶部栏只有返回/铃铛、**中间标题空白**（原版每个子页都有标题）。原版标题由外壳按路由计算（`getHeaderTitleKey`→`headerTitles`）。现让共享组件 `SubPageHeader` 在未显式传 `title` 时**按当前路由派生标题**——一处改，36 页一次性补回标题，与原版一致。

**✏️ 修改**：新增 [header-title.ts](src/lib/header-title.ts)（46 路由→headerTitles key 解析器，镜像原版；how-it-works 页取父标题+后缀；未映射→空，与原版一致）；[sub-page-header.vue](src/components/sub-page-header.vue) 标题优先级 = 显式 prop → 路由派生 → 空。实景验 20 页双语标题正确（Staking/质押、Genesis/创世节点、Help center/帮助中心…），显式传 title 的页不受影响。

**用户入口**：几乎所有子页面顶部栏 ｜ **平台**：uniapp ｜ **PRD**：纯外壳呈现，不同步。

### ✏️ 锁仓最小金额 $100→$20（修正文案矛盾 + 单源化）

**概述**：质押锁仓的最小金额从分档 $100/$500/$1000/$5000 统一改为 **$20**（主人定）；同时修正 FAQ 文案矛盾（原写「$50 minimum」与代码 $100 不符）。并把原先散在两个 .vue 里硬编码重复的 `STAKING_MIN` **提进 store 单源**（与 `STAKING_APY`/`STAKING_PENALTY` 同源，backend-replaceable）。

**✏️ 修改**：[staking.ts](src/store/staking.ts) 新增 `STAKING_MIN`（全档 $20，单源）；[stake-sheet.vue](src/components/staking/stake-sheet.vue) + [staking.vue](src/pages/staking/staking.vue) 删本地重复定义改 import；FAQ faqA1 en/zh 「$50→$20」。实景验 how-it-works 显 $20、staking 流程 console 0。

**用户入口**：锁仓页（`/pages/staking/staking`）+ 锁仓说明 ｜ **平台**：uniapp ｜ **PRD**：锁仓最小额参数（§9.11/§13.3）；**后台 G1 锁仓最小额需同步改 $20（主人将另开 session 处理）**。

### ✏️ 子页面外壳架构对齐（吸顶 nav header + 吸底 CTA 收编进外壳）

**概述**：对齐回原版的「外壳统一渲染 + 页面只声明」架构——子页面的返回/标题栏从「写在内容里、随滚动消失」改为**注册到外壳的吸顶 nav 行**（pin 在顶部 + 磨砂采样滚动内容 + 标题居中），底部购买栏从「页面级 fixed（定位逃出机身）」改为统一走外壳的 `StickyCtaBar`。一次外壳改动，后续所有子页面每页一行声明即可正确吸顶/吸底。**5 个一级页零回退**（注册是纯附加，一级页不注册 → 永远走品牌行；像素守卫验证 chrome 区 0px）。

**✏️ 修改**
- 新增 page-header 注册机制（[page-header.ts](src/store/page-header.ts) + [use-page-header.ts](src/composables/use-page-header.ts)）；外壳 [app-chassis.vue](src/components/app-chassis.vue) 加吸顶 nav 行三态分支 + contentTop 分档（对标原版 Header nav row：44/56 高、back/bell 36×36 glass、标题居中）。
- **商品详情 [detail.vue](src/pages/store/detail.vue) 作样板全打通**：删页内 back/title 行 + 删手写 fixed 购买栏 → `useSetPageHeader` + `useStickyCTA`。主人反馈的 5 个 bug（顶部样式不一致 / 滚动不吸顶 / 缺透明磨砂 / 标题错位 / 底部栏不吸底）全部由架构转换修复（实景验：nav row 吸顶 + sticky CTA「Price $649 …Buy now」+ console 0）。
- **全站子页面 header 一次性对齐**：共享组件 `SubPageHeader`（62 个子页面用它，覆盖 me/team/genesis/staking/trust/daily/市场/…）改为 in-content `position:sticky` 的 prototype-faithful 头（吸顶+磨砂+居中标题+glass tile，原为左对齐、随滚消失、surface tile）——一处改动，62 页同时修好；另有 5 个 store 页走外壳 store nav row（视觉等同）。特殊页（登录/注册/服务条款/年度总结全屏/Nova 聊天）按原版保留自有头。
- 共 67 个子页面顶部栏对齐原版（吸顶+磨砂+居中标题），主人反馈的「滚动不吸顶/缺透明/标题错位」全站根治。

**用户入口**：几乎所有子页面顶部栏（商品详情/钱包/团队/创世/质押/市场/任务…）｜ **平台**：uniapp ｜ **PRD**：纯外壳呈现架构，不同步 PRD（功能/数据未变）。

### 🆕 首页自动推送试用弹窗（补回缺失的触发）

**概述**：进首页后（满足资格 + 距上次关闭超 24h + 每会话上限内）自动延时弹出"领取免费试用"弹窗——原版有此行为，uniapp 此前**组件/store/配置都迁了、唯独自动触发的逻辑没迁**（典型"该有的行为没有"）。现补回：在外壳 `app-chassis` 的挂载逻辑里按 `autoPushEnabled + canStart + 1500ms 延时` 触发 `tryAutoPush`（冷却/会话上限由 store 把关），延时内若已离开首页则不弹。

**要点**：触发逻辑落在外壳（`isHome` 判断），**不改首页 `index.vue` 本体**；实景验证 home 弹、其它 tab 页不弹、console 0。

**用户入口**：进入首页 ｜ **平台**：uniapp ｜ **PRD**：触发参数已在 TrialConfig（§9.11b），行为对齐原版，无新功能面，暂不单独同步。

---

## 2026-06-16

### ✏️ 设备经济重校准（定价减半 + 全线 350–400% 年化 + Cloud 改入门设备）

**概述**：商城全线设备重新校准定价与产出——价格减半、日产出对齐 350–400% 年化（数字可信不自曝），Cloud Share 从「份额年化%」改为「入门级设备日产出」，商品卡片不再展示回本/年化。

**✏️ 修改**
- **全线定价减半 + 产出/NEX 校准**（[products.ts](src/mock/products.ts) + [device-types.ts](src/store/device-types.ts) 双源同步）：S1 $649·$7/d·40 NEX｜Pro $1199·$13/d·80 NEX｜Pro v2 $1319·$14/d·90 NEX｜Rack P1 $4499·$45/d·300 NEX｜Rack P2 $7499·$75/d·500 NEX。各档落 ~365–396% 年化、~92–104 天回本。
- **Cloud Share 改入门级设备**（[products.ts](src/mock/products.ts) + [detail.vue](src/pages/store/detail.vue)）：从「年化 8–15% 份额」改为「日产出设备」$19.9·$0.19/d·3 NEX（唯一不减半项，本已是入门价）；卡片 + 详情页统一日产出口径（删年化%/质押/赎回），与其他设备同形。
- **商品卡片删回本/年化**（[product-card.vue](src/components/store/product-card.vue)）：ROI 区只留「You earn $X/d + NEX」+ AI pills + 库存 + 价格，去掉回本天数 + 首年净赚 + 年化行。
- **全站锚点同步（信息岛清零）**：行情看板排名 / 算力阶梯 / vs-phone 对比 / 倍数文案 640×→**117×**（营销文案与实时计算徽章统一为单一倍数值，不再 120/117 并存）/ onboarding 估算 / 目标阈值 / FAQ / 源码注释。3-agent 审计补扫裸整数 prose 与派生值残留：旧 S1 日产裸写 `$38/日`（tier1 标题 + buyCta）、vs-phone 手机 `+12 NEX`（应 +10）、提现锁定文案「S1 13 小时达 $20」（旧 $38.50 推算的派生值，新 $7/d 改「不到 3 天」），连同 $38.50·$142.6·640× 全部消除。
- 清理死 i18n key（cardAnnualYieldEyebrow / cardPaysBackIn / cardYear1Net / cardMinStake / card30dRedemption，en/zh 同步）+ 死字段 shareYieldMin/Max。

**用户入口**：商城（`/pages/store/store`）商品卡 + 详情页 ｜ **平台**：uniapp ｜ **PRD**：产出/定价参数同步 §7（Store）+ §13.3（关键参数集）；卡片删 UI 元素为纯 UI 不同步。

### ✏️ 价格/购买 CTA 视觉权重再平衡（全站 + 9 档字号归格）

「价格被大按钮压住」的失衡在三处购买/转化 CTA 统一修正：价格放大成焦点（22→**26px**，`$`→15px、售价标签 10.5→**11.5px** 提亮 ink-4→ink-3），购买按钮收窄降权重（padding 收窄 + 字 14→**13.5px**，保留 **44pt tap** 铁律）。顺带把同带（21-25px）离格字号全部归 9 档网格：钱包余额 / 赚取今日收益 / 首页 tech-money / NEX 行情卡 / 试用续期 sheet 的 `$` 前缀 22/24→20/26、详情页价格 22→26。
- **入口**：商品卡（[product-card.vue](src/components/store/product-card.vue)）· 商品详情 sticky 栏（[detail.vue](src/pages/store/detail.vue)）· 共享 StickyCTABar（[sticky-cta-bar.vue](src/components/store/sticky-cta-bar.vue)）· 钱包/赚取/首页金额卡。纯 UI 视觉权重 + 字号网格，不同步 PRD。

### ✏️ 商品详情页 port bug 修复（标题对齐 / 磨砂 / 底部 CTA / 倍数一致）

详情页四处 port 走样修正（对照原型 store/cloud-share）：① 区块标题（信任背书 / 常见问题 / 硬件规格 等）原比 mx-4 卡片左缩 14px → 包 14px 横向 padding 对齐（x=16，根因：detail 无 store.vue 的 px-4 外层容器）；② 底部 sticky 栏缺磨砂 → 加 `backdrop blur(22px)` + 半透明渐变（复用 `--v5-sticky-bar-bg/-border`，与共享 StickyCTABar 同款）；③ 购买按钮 flex-1 浮空留白 → 价格 flex-1 占左、按钮 shrink-0 右对齐消除留白；④ 详情倍数徽章 round-to-10（220×）→ round-to-1（**217×**），与卡片 + 全站 117× 体系统一。**入口**：商品详情（`/pages/store/detail`，[detail.vue](src/pages/store/detail.vue)）。纯 UI/布局，不同步 PRD。

### ✏️ 赚取页重构（任务中心 / 收据 / 行情看板）

- **任务中心重构**（[task-center.vue](src/components/earn/task-center.vue)）：移除「正在处理」板块 + 去「进行中 / 历史」双 tab，合并为单视图＝升级解锁 teaser + 历史任务（最近 20 笔）；历史段头加「查看全部 ›」→ Proof-of-Compute 收据归档页（`/pages/me/receipts`）。
- **收据改纯信息展示**（[receipt-modal.vue](src/components/me/receipt-modal.vue)）：移除行内「复制 / 分享 / 在 Explorer 查看」三处交互，字段全保留为静态展示。
- **行情看板删「网络状态」段**（[market-board.vue](src/components/earn/market-board.vue)）：三段 → 两段（价格指数 / 设备排名）。

**入口**：赚取页（`/pages/earn/earn`）｜ **PRD**：功能改动同步 §6（Earn）。

### 🗑️ 赚取页今日收益卡清 AI Premium 残留 + 改显 USDT/NEX 双数

**概述**：赚取页今日收益卡仍显已下线的「AI Premium」分项；删进度条 / 分项，只保留收益总额 + 任务数，并同时显示 USDT（主）+ NEX（次）两数。

**入口**：赚取页今日收益卡 ｜ **PRD**：移除 AI Premium 残留同步；双数显示纯展示不同步。

### ✏️ 首页 + 赚取页视觉打磨集（去卡片化 / 去边框 / 留白 / 光晕）

**概述**：一批纯视觉打磨，对齐设计系统「有 bg fill 不叠 border」「列表用行分隔线、单 stat 用顶部锚线」「不靠 box-shadow 做层级」。**纯 UI，不同步 PRD。**

- **去卡片化贴页**：首页收益流水（+ 头部「查看全部 →」跳账单页）/ 实时动态 LiveFeed / 今日收益卡（保留网格 + 紫光晕 + 漂移粒子作背景纹理）→ 去卡框，行对齐页面边、顶部细线锚定。
- **去边框改弱填充**：本周任务 / 首日奖励卡去 1px 边框；快捷卡（质押 / 创世 / 任务 / 签到）去彩色描边 → accent 14% 弱底（保各自色彩身份）。
- **首页顶部精简**：删问候副标「今日已赚 $X」、删同侪均值行、问候语放大 26px / 600；今日收益光晕换单一左侧紫色径向（自然羽化融入黑底）。
- **本周任务卡统计行精简**：「$38.50/d · 642× phone · 回本 ~34d」三项 → 只留「$38.50/d」。
- **赚取页板块视觉**：板块标题字号对齐首页（15px/ink）+ 板块间距统一 24px + 纯色↔纯色相邻才加 hairline 分割线 +「限时免费」徽标去描边。
- 各处连带清理对应死 i18n key（en/zh 同步）。

## 2026-06-15

### 🆕 人工客服多类别 + 全屏统一会话中心

**概述**：把「人工客服」和「Nova AI 顾问」统一到一个全屏会话中心；人工客服分多类别，用户可在不同类别会话间主动切换。

- **专属顾问（Advisor）**：会主动给用户发消息引导转化（设备闲置升级 / 锁仓 / 复投），以未读呈现。
- **普通客服（Support）**：处理用户主动发起的售前 / 售后；真人回复。
- **统一会话中心**：全屏页按类型（顾问 / 普通客服 / Nova AI）分组切换，点会话进全屏聊天；浮标未读聚合全部类别。
- Nova 并入作「AI」类型（原底部抽屉改为进会话中心）；Nova「转人工」路由到普通客服；移除 Nova 抽屉内「Live Agent 就地模式」（5 坐席循环 / 3 分钟切回）。

**入口**：① Nova 浮标 ② 我的 → 帮助与支持 → 在线会话 ③ 首页 Nova 卡 ｜ **PRD**：§11.0A / §11.8.4 / §3.3 / §12.9a。

### 🆕 试用入口改版为广告 banner（首页 + 个人中心共享）

**概述**：试用入口从「票券式（设备名 + $价值 + 今日仅剩 N 张）」改版为标准广告 banner——广告图占位（真创意可后台下发替换）+ 福利文案「您有免费的试用设备福利待领取」+ 零承诺安抚 + 克制「马上领取」CTA；价值 / 紧迫感下沉到领取弹窗。抽成共享组件 [trial-promo-banner.vue](src/components/trial-promo-banner.vue)，首页与 ME 页共用一份。

**入口**：首页（`/pages/index/index`）+ 个人中心（`/pages/me/me`）｜ **PRD**：待确认后同步（前端试用入口章节）。

### ✏️ 活动中心「已领取」可点去使用

**概述**：奖励领取后按钮由死标签「已领取」升级为「已领取 · 去使用 ›」可点——折扣券跳商城、NEX 奖励跳 NEX 钱包（按事件 `useHref` 配置；未配置的保持原态无回归）。主推卡 + 活动列表卡均接动线。

**入口**：活动中心（`/pages/events/events`）｜ **PRD**：待确认后同步（前端活动中心章节）。

### ✏️ 注册礼包 / 邀请奖励文案校准（200→20）

**概述**：注册礼包 NEX 实发已从 200 降到 20，但部分落地页 / 文案仍硬编码「200 NEX」（宣传 200、实发 20 的信任 bug）；全部对齐 `WELCOME_GIFT_NEX = 20`（注册页动态绑常量防漂移），并厘清「邀请人每邀 1 人 +200 NEX」与「新人注册 +20 NEX 礼包」是两个数。

**入口**：邀请落地页 / 注册页 / 团队邀请卡 ｜ **PRD**：数值已在「NEX 产出收紧」记录，本条为文案一致性修正。

### 🗑️ 首页移除「你的等级」V-Rank 卡

从首页 ZONE4 拿掉 `<VRankCard />`（同阶「领导奖池」保留）；组件文件保留便于复用。**PRD**：纯首页编排，不同步。

### ✏️ 首页 / 商城 / 个人中心视觉打磨集

**概述**：一批纯视觉打磨（去卡片化 + 弱化引导 + 降噪 + 留白），对齐设计系统。**纯 UI，不同步 PRD。**

- **首页卡片疲劳优化**：信任区 / 领导奖池 / 我的算力队去卡框（行分隔线 / 顶部锚线差异化）+ 全页留白 `space-y-3→4`；删领导奖池「结算于 Nd」、算笔账「可交互」标签。
- **首页今日收益卡简化**：删底部「回本周期 · N 天」，底部收为单行（同侪均值 + 日收益）。
- **商城「手机 vs S1」对比卡弱化**：「640× 更多」实心橙胶囊 → 12% 淡底 chip + 字重回落、中间箭头橙→中性、光晕 0.6→0.4。
- **余额卡「添加设备」按钮降噪**：实心亮橙 + 发光 → 16% 淡底 tint + 橙字（去实心 / 去阴影），从抢镜回落为克制二级入口。
- **免费试用 banner 扁平化**：去前置图标、左文案右按钮、去卡框 + 上下留白。
- 各处连带清理对应死 i18n key（en/zh 同步）。

### ✏️ 提现 / 签到 / 注册改 NEX 经济（前端侧）

**概述**：积分经济整套换成 NEX 代币——提现摩擦、签到奖励、产出渠道全改 NEX。前端改动：

- **NEX 销毁式提现闸**：提现页积分进度条 → NEX 燃烧闸（每提 $100 燃烧 N NEX，余额 / 所需 + 充足提示）；提交原子扣 NEX + 失败回滚 + 记 USDT 提现单 + NEX 燃烧单；不足引导「签到攒 NEX / 去挖矿」。
- **签到改发 NEX**：每日 +2 NEX，连 7 天额外 +5；里程碑 Day3=+5 / Day7=+15 / Day21=+100 NEX。
- **产出收紧**：注册礼包 200→20、成就 50/100/500/1000→10/20/50/100、转盘 NEX 档下调；复投积分奖励删（复投只剩 35% APY + 培育奖 + Genesis 抽奖券）。设备挖矿日产保留为主水龙头。

**入口**：钱包 → 提现 / 每日签到 / 各产出页 ｜ **PRD**：前端 §9.3.2 / §9.8 / §13.1 / §13.4.1-2。

### 🗑️ 下线积分系统 + Premium 订阅 + NEX v2 锁仓（前端侧）

前端积分 store、提现积分门槛、签到 / 复投 / 成就的积分奖励全删；`/me/wallet/premium`、`/me/wallet/nex-v2-lock` 页 + 路由删，钱包 / 签到 / 复投页改 NEX。**PRD**：前端 §9.5a/§9.5b 删、§12.12 数据模型。

---

<!-- 新条目：在最上方当天日期下追加；同批纯 UI 合并为「视觉打磨集」一条。 -->
