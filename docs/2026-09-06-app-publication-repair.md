# App 发布检查修复（2026-09-06）

范围：正式 App `NX1.0-UniApp`，按用户授权只在 main 修复、完整验证后发布。未修改 PC、Java、业务配置；未执行真实购买、提现、领奖或客服转接。

## 问题、证据与修复

- 推荐课程及转人工客服文案内嵌在页面，未走三语词典：统一迁入响应式 i18n；保留转接确认、失败问题不被分享及敏感信息提醒语义。
- 4 个原生按钮违反 UniApp 控件规范：改为 view/text，保留 role、tabindex、忙碌禁用与点击守卫，复用 App 已安装的 Enter/Space 激活层。
- 推荐卡片字号档外：16 改为既有 15 档；删除已无消费面的 GEN 英文豁免；越南语开发者说明移除内部审批用语，不改变功能尚未开放的含义。
- 提现扫描误把 Vitest fixture 当生产消费点：仅排除 `.test.ts`，与现有 Vitest include 对齐，并检查易混淆生产文件名不会被排除。资金守卫、真实调用点集合与 14 条变异红测均保留。
- Genesis 刷新测试装载失败：共享 Vue stub 补齐 shallowReactive，两种方言一致。30/30 刷新缝实际触发，未增加不可达豁免。
- 设备槽位旧正则不接受 pendingDeactivate 释放槽位：改为执行真实策略，依据 DeviceKind 派生全部机型，覆盖 8×2×3=48 组行为，不修改业务策略。
- 质押 CAS fixture 漏 canOpen：补齐允许进入被测路径的测试状态，原有冲突、退款与可提额度还原断言不变。
- 接口台账缺少已实现的密码命令回执和 AI 人工转接：对照后端控制器登记精确路径。拆开嵌套路径模板以恢复精确扫描；保留 cursor 编码与原有鉴权，不登记宽泛接口豁免。

## 验证与发布要求

新增 UI、Vue stub、安全分页路径测试均由现有完整验证链运行；已有 a11y 行为门覆盖键盘激活和重复触发。定向复验通过：i18n、字号、提现 121 项、提现变异 14 项、刷新韧性 8 项、槽位 48 组合、质押 CAS 55 项、接口台账和目标集合元门。

独立只读初审/复审未发现功能退化或降低门禁。静态诊断不作为发布通过证据；必须在最终提交、干净工作树上跑 `npm run verify`，由原 pre-push 检查该提交的 full 记录。不得使用未验证推送豁免。最终运行记录见本地 `.verify-cache/last-run.json` 与对应 logs，远端 SHA 必须读回与本地 HEAD 相同。

## 回退与边界

原始 refs 与工作文件已在工作区外部 Git 发布备份中保留。修复前 App 提交为 d241412c；回退应通过明确授权的回退提交，不重置无关工作树。App 已按真实 5173 所属进程单独重启，PC、Java、MinIO、Gemma 保持运行。代码发布不等于真实世界第三方能力或全量线上业务验收；旧门已有范围提示保留，不更改既有黄灯基线。
