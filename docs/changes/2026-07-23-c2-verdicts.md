# C2 · 零-border 铁律 —— 裁决台账(main 回源裁决)

> 横切专项批次第 2 批。规范依据 `UI/规范/03-空间圆角边框分层规范.md` §3/§4:
> **「任何带背景色填充的卡片/板块一律零 border」**,border 只属透明容器(分组 hairline / empty-state 虚线);
> 玻璃 chrome(TabBar / Header / 浮层)明写不在此约束内。

## 0 · 口径先定,再上门(B1 完整性报告 §5.3 的要求)

B1 报「全站同类约 77 处」并注明「**77 处 = 规则没落地,不是 77 个 bug;定完口径再上哨兵**」。
本批先回源读规范定口径,再按口径扫 —— 结果是 **34 处 full 违例**(去重后约 15 个不同组件),
不是 77。差额来自 77 那次是按「源码里出现 border」粗数,含大量透明容器 hairline 与 chrome。

**判据(全部来自规范原文,不是我调的参)**:
- 违例 = 该元素**自身有可见填充**(bg alpha ≥0.03 或有渐变)**且有可见描边**。
- 分级:`full`(≥3 边 = 盒子描边)进硬门;`partial`(1-2 边 = 行分隔线)只列不拦。
- 三条内建豁免,判据均出自《03》:① chrome 身份(TabBar/Header/浮层/下拉刷新指示器)
  ② dashed/dotted(empty-state 虚线惯用法,填充卡不会用虚线)③ ≤16px 圆形「隔离描边环」
  (状态点用底色描边做分隔,是技法不是卡片描边 —— 主人在任务书里明确列的豁免)。

**为什么是运行时门**(承 C1 教训):静态 grep 只能钉「我已经想到的写法」——
border 可写成 `border:` / `border-top:` / 驼峰 `borderColor` / UnoCSS class / CSS 类 / 继承;
bg 可以是 color / gradient / token。用写法找角色必漏。本门只问渲染结果。

## 1 · 已修(12 处填充容器去描边)

| 组件 | 原状 | 判据 |
|---|---|---|
| `milestone-celebration.vue` `.ms-card` | 双径向渐变 + surface 底 + `--v5-border-strong` 四边 | 填充卡带描边 |
| `voucher-claim-sheet.vue` `.vcs-card` | `color-mix(brand 6%, surface-2)` 底 + brand 26% 四边 | 同上 |
| `staking/position-row.vue` 持仓卡 | `--v5-surface` 底 + `--v5-border` 四边 | 同上 |
| `staking/compound-calculator.vue` 卡 | `--v5-surface` 底 + `--v5-border` 四边 | 同上 |
| `staking/vault-row.vue` 档位 chip ×4 | soft bg tint + 四边描边 | 《03》§4「内嵌 chip/pill 用 soft tint,**禁加 border**」 |
| `staking/vault-row.vue` ribbon | 同上 | 同上;连带删掉已无消费者的 `borderColor` 死字段 |
| `team/team.vue` royaltyHero | 渐变 + surface 底 + **`--v5-brand-border` accent 描边** | 填充卡带描边 **且**「大块卡片禁 accent border」双违 |
| `team/team-ledger-card.vue` | 填充 + `--v5-border` 70% 四边 | 填充卡带描边 |
| `store/tradein-window-banner.vue` | brand-2 渐变底 + **brand-2 40% accent 描边** | 双违(同 royaltyHero) |
| `earn/compute-share-entry.vue` | `color-mix(tech-cyan 8%, surface)` 底 + tech-cyan 25% 四边 | 填充卡带描边 |
| `me/me.vue` Sign out | `--v5-surface` 底 + danger 25% 描边 | 改走 `nexion-design` 状态派生公式的 **soft-tint** 形态:`--v5-danger-soft` 底 + danger 实色文字 + 零 border |

**扫描结果 full 34 → 2**;`partial` 13 处经核对全是行分隔线 / 子页头底边,属规范认可的透明 hairline。

## 2 · 🔴 待主人拍板

### 待拍板 1 · 创世「金-曜石」域的两处描边(genesis hero 357×232 · Claim seat 吸底 CTA 358×54)

- **现状**:hero 是 `linear-gradient(#1B140A…)` 曜石底 + `1px solid rgba(212,175,90,0.35)` 金色描边;
  吸底 CTA 同族金边。组件文件头自称「.genesis-hero 设计例外」。
- **问题**:那句「设计例外」写于 **2026-07-09「原『有条件例外』一律收回」终裁之前**,按现行《03》§3 属违例。
- **为何不自主修**:删金边会改变旗舰品($11,999)主视觉身份 —— 金-曜石这套「奢华」语言正是靠
  暗底 + 金描边成立的,去掉描边后 hero 与普通暗卡难以区分。属**设计决策**不是机械修。
- **我的倾向**:**保留并正式登记为域级例外**(置信度 MED)。理由:与 canvas 海报、产品实拍图衬底同类
  —— 都是「自成体系、不参与全站分层语言」的孤岛;强行拉齐会损失产品线的高端识别度,收益不抵。
  若主人认为铁律不留口子,则改法是**加厚曜石底 + 内阴影**替代描边做边界,视觉成本比直接删边低。
- 当前已进 `docs/ZERO-BORDER-BASELINE.json` 逐条留痕,门只拦新增,不会静默扩散。

## 3 · 机制

`scripts/zero-border-gate.mjs`(已接 verify.sh):运行时扫 10 条路由 × 双主题,
自检 **19 条双向红测**(阳性:实底/tint 底/渐变底 + 四边框、实底 + 单边记 partial;
阴性:透明底 + 四边框、透明底虚线、实底无边、全透明描边占位、style:none、alpha<0.03 极淡底、
tabbar 玻璃砖、header chrome、dashed、隔离环;且「普通卡不被 chrome 豁免误放」反向锁)。
存量走棘轮基线 `docs/ZERO-BORDER-BASELINE.json`,只拦新增 full。

**门**:tsc **0** · verify **323 pass / 0 fail**(C2 前 321,+2)· 溢出 **新增 0** · 5 tab × 双主题 console **0**。
