# 实施拆解 · FEAT-WD01 提现信任通道

状态:**Signed — 主人已签字 2026-07-31,进子任务循环**(规格 `PRD/specs/FEAT-WD01-trust-payout-rails.md` 一次审完)
规格血缘:每个子任务的验收标准(AC)逐条继承规格 ② GWT,不另造口径。

**涉及两个工程**:`Nexion-uniapp`(用户端,dev :5173)· `admin-ops`(运营后台,dev :3012 · 主人已授权直接改)

---

## 执行纪律

- 🔴 **逐子任务闭环**:实现 → 增量机器门 → **独立 tester agent 黑盒验收** → pass 才打勾 → 回源三问。
- 🔴 **实现方 ≠ 验收方**:tester 只拿「本条 AC + 运行方式 + 已知脆性清单」,**不给实现叙述**(防锚定)。
- 🔴 **熔断**:同一子任务连续 2 轮 fail → 停下反思并报主人,不硬刷。
- 🔴 每条新不变量**必配哨兵 + 红测**(判据失效=空集全过,红测证明它真能拦)。
- 已知脆性清单(tester 必读):dev server 在 5173(探不通先 curl,vite 绑 IPv6 会假 down)· 前端验证用 `?nx_device=off` 绕设备外壳 · uni storage 注入必须 `{type:'object',data:X}` 包装 · 中断必杀 headless chromium · admin 走查需先登录。

**依赖顺序**:T1 → T2 → (T3 ∥ T4) → T5 → T6 → T7 → T8。T3/T4 可并行(一个动后台、一个动前端费用函数,不碰同一文件)。

---

## T1 · 后台:D5 新增两个可配参数

**范围文件**:`admin-ops/app/components/domain-views/d-tabs/d5-params.tsx` · `admin-ops/lib/admin/d-client.ts`

**做什么**:新增「小额免审线」(`smallAmountThresholdUsd`,默认 50,范围 0–500)与「到账时效」(`payoutSlaHours`,默认 24,范围 1–168)两个可写参数,沿用 D5 既有写入范式(版本号 + 幂等键 + 理由 + 放大方向判定 + 覆盖率红线校验)。

**AC**(继承规格 FEAT-WD01c ② 与 §2 参数表)
- [x] 两个参数在 D5 页面可见、可改,范围越界时提交按钮置灰并给出原因
- [x] 「小额免审线」调大 = 放大流出方向 → 走超管确认 + A2 提案(与既有 `dailyLimitCount` 同规则)
- [x] 「到账时效」调小 = 放大方向;调大 = 收紧
- [x] 改动写审计(操作人 / 时间 / 旧值 → 新值 / 理由)
- [x] 契约测试不红(现有 `tests/d5-withdrawal-limits-closure-contract.test.mjs`)

**测试指令**:`cd admin-ops && npm run dev`(:3012)→ 登录 → `/finance/params` → 改值 → 看确认弹窗与审计
**敏感度**:🔴 钱 + 权限 → tester 之外加 1 个 code-review agent
**tester 报告位**:`docs/changes/2026-07-31-trust-payout-rails-t1-test.md`
**回源三问**:① 仍服务「新用户第一笔能当天走完」目标 —— 是,两参数是快车道的开关与时长。② 有偏差 —— 有,首轮漏了「渲染文案只披露一道闸」与「页面描述两个渲染源只改一处」,属**实现错**(规格 ⑤/⑦ 本已要求),已修并各焊哨兵。③ 下一子任务计划仍成立 —— 是。
**验收**:独立验收 2 轮(首轮 FAIL 2 项 → 修复 → 复验 PASS);哨兵 9 条,复验方 5 次定点红测均只红对应条。

---

## T2 · 后台:「提现冷却」改名「到账审查窗口」

**范围文件**:`admin-ops/app/components/domain-views/d-tabs/d5-params.tsx`(只读行)· `h-tabs/h1-phase.tsx`(列名)· `e3-manual.tsx` 等运营手册 · `lib/admin/glossary.ts`(如有该词条)

**做什么**:仅改**显示名**,`cooldownDays` 字段键**保持不变**(改键会破坏后端契约与既有审计记录)。

**AC**(继承规格 FEAT-WD01c ② 异常5)
- [x] D5 只读行、H1 列名、运营手册三处显示名一次改齐,无残留「提现冷却」
- [x] 字段键 `cooldownDays` 未改,后端契约与既有测试不红
- [x] 历史审计记录不被追溯改写
- [x] 新增哨兵:全域 grep「提现冷却」= 0(红测:塞回一处应 FAIL)

**测试指令**:`grep -rn "提现冷却" admin-ops/app admin-ops/lib` 应为 0
**敏感度**:低(纯文案)
**tester 报告位**:`...-t2-test.md`
**回源三问**:① 服务「消除运营误读」目标 —— 是。② 无偏差,字段键未动、历史审计未追溯改写。③ 计划成立。
**验收**:随 T1 同批复验 PASS;哨兵剥三种注释后仍拦得住(行注释假绿口子已堵,红测证)。

---

## T3 · 前端:费用函数补网络费

**范围文件**:`Nexion-uniapp/src/store/nex-faucet.ts`(`computeWithdrawFee`)· `src/store/config-types.ts`(费率配置字段)· `src/pages/me/wallet-withdraw.vue`(明细展示)

**做什么**:把费用公式从 `grossFee = 金额 × 惩罚费率` 改为 `grossFee = networkFee + 金额 × 惩罚费率`,其中 `networkFee = clamp(金额 × 费率, Min, Max)`。费用明细区分行展示网络费与惩罚费。

**AC**(继承规格 FEAT-WD01c ② 全部 5 条)
- [x] 提 $100 / 惩罚 20% / 网络费率 1% 下限 $1 → 总费 $21,与后端等式误差 < 0.0001
- [x] 提 $10 → 网络费按下限取 $1(不是 $0.1)
- [x] 提 $10,000 → 网络费按上限封顶
- [x] NEX 抵扣作用于**总手续费**(含网络费),抵扣后实扣不为负
- [x] 费率配置拉不到 → **禁止下单** + 「费率更新中」+ 重试出口,**绝不回退写死值**
- [x] 明细区四态齐(默认 / 未输入金额 / 加载骨架 / 拉取失败)
- [x] 新增哨兵:费用等式不变量(红测:去掉 networkFee 项应 FAIL)

**测试指令**:5173 → `/pages/me/wallet-withdraw` → 输入上述三组金额核对明细
**敏感度**:🔴 钱 → 加 code-review agent
**tester 报告位**:`...-t3-test.md`
**回源三问**:① 仍服务「用户看到的费=实扣的费」目标 —— 是,总费两笔相加已与后端等式对齐。② 有偏差 —— 有,5 轮验收共 13 条必修,根因两类:(a) 只跑静态检查没开浏览器(加载态死 UI/NaN/悬空指代全是开页面即见);(b) 同类「漏一处」犯了 6 次(明细区→NEX面板→矛盾文案第二处→warnings box→未输入金额态→中文分隔符)。均属实现错,已修并各配哨兵。③ 下一子任务计划成立。
**验收**:5 轮独立验收(浏览器实景为准);末轮四态 4/4 人可见、坏配置全页 NaN=0、恢复完整性经字节级独立核实。
**遗留(主人 2026-07-31 拍板 A,记为待办不阻塞)**:费率校验器的**输入面**仍可被塞防御性默认值架空(独立验收编号 NEW-2);本轮已按验收建议把校验挪到信任边界(config store 的 `feeConfigValid`)大幅收窄,彻底闭合需端到端测试。

---

## T4 · 前端:小额免审快车道

**范围文件**:`Nexion-uniapp/src/store/withdrawal-eligibility.ts` · `src/store/config-types.ts` · `src/mock/platform-config.ts`

**做什么**:金额 ≤ 小额线时,跳过「首提必审」与「新地址 hold」两道闸的判定;其余风控闸(冻结簇 / 共用地址 / 风险分 / 大额账龄)照常裁决。记录 `waivedGates` 供审计与客服解释。

**AC**(继承规格 FEAT-WD01a ② 全部 6 条)
- [x] 新用户提 $30(≤$50)→ 路由 `pass`,单据落 `submitted`
- [x] 同一新用户提 $80(>$50)→ 路由 `manual`,不免审
- [x] 命中冻结簇 / 共用地址 / 风险分 ≥0.7 的账户提 $30 → **免审不生效**,风控照常裁决
- [x] 小额线设为 0 → 快车道整体关闭,回落原路径
- [x] `waivedGates` 记录被免的闸名
- [x] 新增哨兵:免审不得越过风控裁决(红测:让免审覆盖风控闸应 FAIL)

**测试指令**:5173 → 注入不同风控状态的账户 → 提现页提交对应金额 → 核路由与理由文案
**敏感度**:🔴 钱 + 风控 → 加 code-review agent
**tester 报告位**:`...-t4-test.md`
**回源三问**:① 仍服务「新用户第一笔小额能当天走完」目标 —— 是;六道风控闸逐个实测照常裁决,免审只免两道冷启动闸。② 有偏差 —— 有,哨兵方法论错了三次:结构断言→行为断言→端到端入口。终极诊断(验收员原话):**只要哨兵的入口比生产的入口低一层,那一层差额就永远是攻击面**。③ 计划成立。
**验收**:5 轮独立验收;34 万组行为等价证明重构零回归;12 种攻击(别名洗白/控制流搬迁/上游架空/时间炸弹/恒定维度×3/取值域盲区×2/加工层撒谎等)全部被抓。
**遗留(主人拍板 A,记为待办不阻塞)**:外壳 14 个转发点仍是未覆盖面(S1-S4/H/T2 实测可注入),彻底闭合需在 node 里模拟 pinia 测真正的生产入口 evaluateWithdrawal。

---

## T5 · 前端:每日提现笔数限制

**范围文件**:`Nexion-uniapp/src/store/app.ts`(`submitWithdrawal`)· `src/store/withdrawal-eligibility.ts` · `src/pages/me/wallet-withdraw.vue`

**做什么**:按账号 + 平台时区自然日计数;达上限时**不建单、不扣款**,提交按钮置灰并给下次可提时间。计数以**落盘值**为准(并发面,与既有并发透支门同源思路)。

**AC**(继承规格 FEAT-WD01b ② 异常 3/4/5)
- [x] 今日已达上限 → 提交按钮置灰 + 说明文案 + 下次可提时刻
- [x] 超限时**不建单不扣款**(余额与单据数均不变)
- [x] 两个标签页并发提交第 2 笔 → 以落盘计数为准,第二笔被拒(第 4 轮复验 40/40 PASS)
- [x] 后台把上限从 1 调到 3 → 当日立即可再提 2 笔(实时生效)
- [x] 23:59 与 00:01 分属两天,第 2 笔放行
- [x] 页面现有「每日限额 1 笔/日」文案从**空头承诺**变为**真实现**(改为从配置插值,不写死)
- [x] 新增哨兵:超限不建单(红测:去掉判定应 FAIL)

⚠️ 文案口径修订(首轮验收 F2/F4 + code review MEDIUM-1):原 AC 写的「明日 0 点重置」不成立 ——
平台日曾按 UTC 切,越南用户重置在当地 07:00;且用户本地 00:00–08:00 时段「明日」二字必然说错。
已改:平台日按**越南 UTC+7** 切,文案给**绝对时刻**「MM-DD HH:mm 后可再提」。

**测试指令**:5173 → 连续提交两笔 → 第二笔应被拒且余额不变
**敏感度**:🔴 钱 → 加 code-review agent
**tester 报告位**:`-t5-test.md`(FAIL 1)· `-t5-retest.md`(FAIL 2)· `-t5-retest2.md`(FAIL 1)·
`-t5-retest3.md`(FAIL 1)· **`-t5-retest4.md`(PASS)**
**敏感度处置**:code-review agent 已跑(1 HIGH + 3 MEDIUM 全修)。

**四轮验收 → 四次重做,每次都被实测证伪一个错误假设**:

| 轮 | 裁决 | 被证伪的假设 | 实际根因 | 修法 |
|---|---|---|---|---|
| 1 | FAIL(1) | 「建单成功后 +1 就够」 | 查与写之间隔着 600ms 风控评估,两页各自读到「今天还没提过」(3/3 复现) | 改**先占后建** |
| 2 | FAIL(2) | 「同步的读-改-写是原子的」 | Chrome 给独立标签页**不同渲染进程**,localStorage 传播异步,~1ms 内都读到旧值(12/12) | 改**写入后回读验令牌**(不依赖读写原子性) |
| — | — | 「消毒挡住 NaN / 负数就够了」 | 荒谬大值(1e9)反方向把当天提现**锁死**(12 注入 2 中) | 消毒**对称化**:非安全整数 / 负数 / >10000 一律当 0 |
| 3 | FAIL(1) | 「落盘一次就落上了」 | 落盘要先「读最新再合并」,那次**读最新跨进程同样是旧值**;败者整份覆盖,扣款连单一起没(9/40) | 加落盘后**按单号收敛重放** |
| 4 | **PASS** | 「重放读 user.value 就行」 | 合并结果会 adopt 回内存,把本页刚提交的状态**回滚**;重放读到的是回滚后的旧值 | 留**本地权威副本** + 重放前**先换基线**(不换则差分为 0,又写回旧值) |

**第 4 轮实测**:40/40 正好扣一笔;少扣 0、**多扣 0**(四个正交通道分别判,含写入轨迹瞬时最低值);
上轮的失败起手式在轨迹里照样发生 3 次,3 次全被重放救回 —— 不是运气。
单人提现 931ms(重放一次即收敛,3 次上限从未跑满);三标签页 / window.open / 连续两笔 / 坏数据 16 项全绿。
统计上界:0/40 只能把失败率压到 95% 上界 7.2%,不能证明绝对为 0;22.5% 那个量级已排除(p≈3.7e-5)。

**遗留(登记不修,已写进 PAY 规格 §5.1)**:
① 账单行并发丢失(两页 20% / 三页 80%)—— 同一个「整份快照后写覆盖先写」根因,不在 mock 层收口;
② 前端多标签页账本机制在充值 / 购机 / 奖励入账同样有同源风险。
两条均随用户侧接口落地由服务端事务替代。

**回源三问**:① 仍服务「新用户第一笔小额能当天走完」目标 —— 是;限额与并发面是它的护栏,不是阻碍。
② 有偏差 —— 有,**同一条不变量连续错判四次**,根因是一条共同的认知错误:
**把「本地存储的读-改-写」当成原子操作**。四次的正确解法是同一形状 ——
不依赖读写原子性,改为「各写各的 + 唯一凭据 + 等传播收敛后回读验证」。
纯函数哨兵**证明不了跨进程行为**,这一层只能靠真·多标签页实测;已写进 P-071 与 memory。
③ 下一子任务计划仍成立 —— 是。

---

## T6 · 前端:到账推进路径

**范围文件**:`Nexion-uniapp/src/store/app.ts`(状态推进)· `src/App.vue`(轮询接线)· `src/store/types.ts`

**做什么**:到达 `estimatedCompletion` 后,把 **`pass` 路由**单据推进到 `confirmed` 并记 `confirmedAt`。缺口一次性补齐且幂等。人工 / 冻结 / 延迟路由**永不**自动推进。

**AC**(继承规格 FEAT-WD01b ② 阳光 + 异常 1/2)
- [x] `submitted` 单据到点后推进到 `confirmed`,记录实际到账时间
- [x] 关 App 三天再打开 → 缺口一次性补齐,**不重复推进、不重复记账**
- [x] `review-pending` 单据过了时间**不推进**,保持人工审核态
- [x] 到账时间公式:正常 = 提交 + 到账时效;命中大额审查 = 提交 + 审查窗口(取更晚者)
- [x] 新增哨兵:非 pass 路由不得自动推进(红测:放开路由判定应 FAIL)+ 幂等不重复推进

⚠️ **不变量收窄(需主人知会)**:SPEC-7 原文是「client 零推进,状态一律服务端权威」,
verify 里有对应哨兵。本子任务的 AC 要求 mock 到点自动到账,两者冲突。处理方式:
**收窄而不是废除** —— 只允许**一条**推进路径(到达建单时就已承诺的 `estimatedCompletion` 后,
把 `pass` 路由推进到 `confirmed`),人工 / 延迟 / 冻结 / 异常终态永不推进;
追踪页仍然只展示不推进(原哨兵保留);推进判定单源在 `withdrawal-arrival-core.advanceArrival`,
verify 新增三条哨兵守「入口唯一 + App 层驱动 + 恰有一处置 confirmed」。
接真后端时整块删掉,状态改由服务端推送。

**测试指令**:5173 → 注入一张 `estimatedCompletion` 已过期的 `submitted` 单 → 打开追踪页应见已到账
**敏感度**:🔴 钱 + 状态机 → 加 code-review agent
**tester 报告位**:`...-t6-test.md`
**回源三问**:① 仍服务「新用户第一笔当天走完 + 次日看到钱到账」目标 —— 是。② 有偏差 —— 有,与既有 SPEC-7「client 零推进」不变量冲突,处理为**收窄不废除**(只放开一条:建单时已承诺的到点时刻 → pass 路由 → confirmed),已知会主人并配三条哨兵守「入口唯一 / App 层驱动 / 全项目恰有一处置 confirmed(== 1 不是 <= 1)」。③ 计划成立。
**验收**:独立验收 7/7 PASS —— 9 种持有态过点 5 天状态不动、余额分毫未变;confirmedAt 记到点那刻而非打开那刻;停在追踪页 1.5s 不推进、跨过轮询才推进(反证只开首页也补齐);时效 24→2 实测跟变。

---

## T7 · 前端:追踪页终态渲染

**范围文件**:`Nexion-uniapp/src/pages/me/wallet-withdraw-tracking.vue`

**做什么**:补 `confirmed` 终态的步进条走满与实际到账时间显示;补规格 ⑤ 的四态与 ⑥ 的点击流(含「再提一笔」在超限时置灰)。

**AC**(继承规格 FEAT-WD01b ⑤⑥)
- [x] `confirmed` → 步进条 5 步走满 + 显示实际到账时间
- [x] 无提现记录 → 空状态引导 + 「去提现」CTA(禁白屏)
- [x] 加载中 → 步进条骨架(非通用转圈)
- [x] 人工审核态 → 停在第 1 步 + 「审核中,不会自动放款」+ 客服入口
- [x] 步进条不渲染为按钮样式(避免死按钮误解)
- [x] 「再提一笔」在今日超限时置灰并说明

**测试指令**:5173 → 分别注入 5 种状态的单据 → 逐个核渲染
**敏感度**:中(展示面)
**tester 报告位**:`...-t7-test.md`
**回源三问**:① 服务「用户能看清钱到哪了」目标 —— 是。② 有偏差 —— 有,初版把加载骨架挂在整页(空状态用户要白等 600ms 才看到「暂无提现」),自查时改为「确有单据才骨架」。③ 计划成立。
**验收**:独立验收 8/8 PASS,含最难抓的加载骨架态实测抓到。

---

## T8 · 双端文案 i18n 三语

**范围文件**:`Nexion-uniapp/src/i18n/messages/{zh,en,vi}.ts`

**做什么**:规格 ⑤ 各态文案落三语,键同序镜像。禁工程名词 / 字段名 / 错误码;每个拦截态必带「下一步」。

**AC**
- [x] 新增键在 zh/en/vi 三文件同序存在(`i18n-key-mirror` 门过)
- [x] 无硬编码中英文残留
- [x] 每条拦截文案都给了下一步(超小额线 → 改提多少;超限 → 何时重置;审核中 → 客服入口)
- [x] 文案不含「首提必审」「风控路由」等工程词

**测试指令**:切换三语走查提现页与追踪页全部状态
**敏感度**:低
**tester 报告位**:`...-t8-test.md`
**回源三问**:① 服务「三语用户都看得懂、每个拦截都有下一步」目标 —— 是。
② 有偏差 —— 有,把「预计到账」那条文案从写死改成插值时**只改了两个消费者中的一个**,
标题把占位符原样直出(zh/en/vi × 2 态 = 6 处全中),而 type-check / key-mirror / 既有哨兵全绿
—— 只在渲染后的 DOM 里现形。属「改一处≠改全部」。
已修,并**新焊机器门** `selfcheck-i18n-interp`(扫「带占位符文案未过 fmt 就直出」,已接 verify)。
③ 计划成立。
**验收**:D-1 修复后复验 PASS —— 7 面 × 3 语 = 21 组全页 DOM 扫描占位符命中 0(上轮同一扫描命中 6)。
**哨兵加固**:验收方**实测构造出盲区** —— 工程常见的 namespace 别名写法
(先 `const w = computed(() => t.value.某namespace)`,再 `w.value.某key`)整条穿过,
哨兵全绿而 DOM 里真漏占位符,30 处别名引用全在覆盖外(是空门不是活漏,存量泄漏 0)。
已堵:扫描面 409→449 处,纳入别名声明 30 / 别名引用 40,候选 key 改**三语取并集**
(占位符名可在三语间不同,只读 zh 会漏);另加两条「空门探针」断言——
别名样本掉到 0 即判失效。红测重现该盲区可精确点名到行。

---

## T9 · 结构收口:提现单据 单条 → 列表(主人 2026-08-01 拍板「一切按真接后端处理」后追加)

**为什么加这一条**:audit 的 P0 里有两条(第二笔顶掉第一笔 / 在途闸把用户永久锁死)是**同一个根因**——
`latestWithdrawal` 只存最新一条,与真后端 `GET /api/withdrawals` 返回列表不同构,违反铁律
「Mock 必须 100% 真后台结构」。主人拍板后重新定位:不是 mock 里怎么变通,是**模型本身错了**。

**做了什么**:快照 `withdrawals: Withdrawal[]`(读盘自动升级老数据)· 合并层按单号取并集 + 状态 rank 单调 ·
store 列表为源真理、`latestWithdrawal` 变派生 · 建单追加 · 到账推进全表扫并返回「推进了哪几笔」·
单槽闸整条删除 · 新增列表级派生 `inFlightWithdrawals` / `primaryWithdrawal`。

**AC**
- [x] 多笔并存,钱正好扣两笔之和,第一笔不被顶掉
- [x] 各自到点各自到账(不是只推最新那笔)
- [x] 老格式快照升级不丢单
- [x] 人工审核单不再把用户永久锁死
- [x] 人工 / 冻结 / 延迟单过点永不自动到账
- [x] 跨端两笔都保留
- [x] 账单按**单号**逐笔结算(不问「最新一笔是谁」)
- [x] 钱包入口与追踪页显示**主单**(优先在途),在途单不被已到账的挤掉
- [x] 换绑闸问整张在途列表

**tester 报告位**:`...-t9-listmodel-test.md`(首轮 FAIL 3 条 → 全修 → 见下)
**验收**:独立验收首轮判 FAIL(3 条),**全部是同一根因的三种形态**——模型层改对了,
三个消费者仍用「只问最新一笔」的老问法去问列表级问题(账单结错单 / 钱包入口消失 / 换绑闸被架空),
且**三条全部逃过了当时的 362 道机器门**。已全修并补 4 条「列表级消费者门」+ 5 路红测。

**回源三问**:① 仍服务「新用户第一笔当天走完 + 看得到钱到哪了」目标 —— 是,且把两个 P0 从根上消掉。
② 有偏差 —— 有,**改完模型没扫消费者**:列表化是「上游数据形态改变」,下游所有「只问最新一笔」
的地方都是隐式耦合点,我只改了模型和最直接的两处。教训与 P-071/072 同族:改一处 ≠ 改全部,
而这次的「全部」是**用旧问法提问的所有地方**。已把它焊成机器门(列表级问题不得读 latestWithdrawal)。
③ 下一步计划成立。


---

## T10 · 前端:快车道**双向告知** + 首审提示矛盾修复(走查发现后追加)

**为什么有这个子任务**:T4 把能力做出来了,走查却发现**页面一个字都没告诉用户** ——
输 $30 什么都不说,输 $80 只说「需人工确认」而不说「小一点就能立刻处理」。
判定层全绿而产品目的(新用户第一笔小额成功)完全落空。规格 ⑤⑥ 本已要求,属**实现漏**。

**范围文件**:`src/pages/me/wallet-withdraw.vue` · `src/lib/risk-reason-text.ts` ·
`src/i18n/messages/{zh,en,vi}.ts` · `scripts/selfcheck-fastlane.mjs`

**AC**
- [x] 小额且**这笔真能立刻走**时,正向横幅说明并列出免掉的闸(判据 `waivedGates` + `route === "pass"`,不是 `fastLaneApplied`)
- [x] 超线时在审核横幅内给**可点**的「改为 $N 提现 →」,点击即把金额设为小额线
- [x] 该 CTA **仅当把小额线代进同一判定函数得出 `pass`** 时出现(风控闸命中不给假希望)
- [x] 「首次提现审核 N 小时内」改为按 `riskReasons` 显示,不再常显;时长从 `payoutSlaHours` 插值
- [x] 三语(zh/en/vi)齐备,占位符 `{g}` / `{h}` 对齐,无裸美元符号残留
- [x] 新增接线门 8 条 + 逐条红测 8/8 命中**对应**门(不是随便红一条)

**运行时证据**(5173 实景,非 grep):

| 场景 | 正向横幅 | 审核横幅 | 降额 CTA | 首审提示 |
|---|---|---|---|---|
| A 冷启动 $30 | ✅ 列出**两道**免闸 | — | — | 不显示 ✅ |
| B 冷启动 $80 | — | ✅ | ✅「改为 $50 提现 →」 | 显示 ✅ |
| C 点 CTA 后 | ✅ | 消失 ✅ | 消失 ✅ | 不显示 ✅ |
| D 老用户+老地址 $30/$80 | 不显示 ✅ | — | 不显示 ✅ | 不显示 ✅ |
| E 共用地址闸 + $80 | — | ✅ | **不显示** ✅ | 显示 |
| E2 共用地址闸 + $30 | **不显示** ✅ | ✅ | — | — |

三语实景各跑一遍:占位符 / NaN / undefined 残留 0;console error 0。

**回源三问**:① 仍服务「新用户第一笔小额能当天走完」目标 —— 是,这一条正是让用户**知道**并**做得到**。
② 有偏差 —— 有两处,都是实景走查抓的,机器门抓不到:(a) 判据一度用 `fastLaneApplied`,
老用户会看到空的「已免去:」;(b) 免了冷启动闸就喊「立即处理」,共用地址闸还拦着 —— **假承诺**,
已收紧为 `route === "pass"`。③ 计划成立。

**新坑归档**:`PORT-PITFALLS.md` P-075「能力跑通了 ≠ 用户知道」;memory `feedback_silent_capability_must_be_told`。

---


---

## T11 · nexion-audit 五层 + 修复(2026-08-01)

**怎么跑的**:模式 B 手动五层,7 个独立 agent 并行(3 个逐单元 + 2 个横切 + 1 个哨兵自审 + 1 个 completeness critic)。
派单前先做墨菲前置,自己抓到并修掉一个靶子(账单数组「恒倒序」这个不变量在读盘路径上没被代码保证,只是巧合)。
审计期间冻结被审文件,等 7 份全部回报才动手。

**结果**:confirmed P0 = 9,逐条回源核实后全部修复;P1 修了 3 条,其余登记进任务 #8。
**其中多条出在本轮自己的修复上** —— 假配置项、哨兵对中文失明、账单倒推口径、CTA 判据、报价闸漏挂。

**AC**
- [x] 9 个 P0 全部修复,每条回源核实过(不采信 agent 结论)
- [x] 族级根治而非逐个打补丁:同一概念只留一份判据 / 提交输入统一冻快照 / 拦截判据单源 / 数字只认单源
- [x] 机器门重写:新建真解析模块 + 重写 slacopy(36)+ 升级 fastlane(107)/ rebind(39)/ feegate 结构锚定
- [x] 红测 15/15 逐项命中对应门,每条注入只破坏一个合取项,形态取自真实事故现场
- [x] verify.sh all 364/0 · vue-tsc 0 错 · i18n mirror PASS
- [x] 实景复验:披露 §04 数字接配置 / 日限已用完不给假 CTA / 冷启动 0 正常报价 / 账单余额列不再编造
- [x] 新坑归档 PORT-PITFALLS P-077,memory 两条,审计阀如实记录(未修的 P1 逐条列出,不谎报)

**回源三问**:① 仍服务「新用户第一笔小额能当天走完」目标 —— 是,且这一轮把「说得出口但兑现不了」的部分全清了。
② 有偏差 —— 有,而且偏差主要在**我自己上一轮的修复**里:为 P0 建的门对该 P0 敞开、红测靠错的理由变绿、
新增配置项其实没有配置面。三条都已归层焊死。③ 计划成立。

## 收尾门(全部子任务打勾后)

- [ ] 前端 `npm run type-check` 0 错 + `bash scripts/verify.sh all` 全绿
- [ ] 后台契约测试与 verify 全绿
- [ ] 独立走查 agent 端到端跑一遍真实用户路径(新用户 → 充值 → 小额提现 → 次日到账)
- [ ] `nexion-audit` 五层修到 P0=P1=0
- [ ] `done-review` 6 维矩阵
- [ ] PRD 同步(前端 §9.3 / 后台 D5)+ 产品更新日志
- [ ] 进化闭环:本轮新坑记 `PORT-PITFALLS`,能机器测的升哨兵
