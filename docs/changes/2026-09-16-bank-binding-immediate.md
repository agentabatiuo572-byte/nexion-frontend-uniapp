# 银行卡绑定立即生效与额外核验移除

2026-09-16 用户确认：首次绑卡和换卡都立即生效，取消额外的外部账户/归属核验前置条件。随后用户补充取消 7 天换卡限制，并要求换卡短信校验。

- App 依据服务端绑定结果及通道状态展示可提现状态，移除重新核验入口和 24 小时等待提示，中英越三语同步。
- 既有仍在等待期的绑定由后端按实际绑定时间返回生效时间；不生成或展示伪造的核验成功记录。
- 换卡不再等待 7 天，使用账号绑定手机号短信确认；验证码 5 分钟有效、60 秒重发间隔，页面只在内存保留验证码，离开页面后清除，临时切到短信应用再返回时保留。首次绑卡无短信。
- 未决报价/在途单换卡限制、报价与收款账户版本一致性、原单恢复及到账/退款证据继续生效。
- 后端和 PC 同步调整；App 不自行覆盖通道开关或推断已到账。

验证范围：绑定回执与隐私解析、即时生效和服务端布尔门禁、换卡短信、过期/失败/身份切换、丢响应恢复、取消/旧单查询、三语言页面与完整 verify。支付操作仅使用隔离测试，不发起真实提现或发送测试短信。
