# T4 验收档 — runtime 探针重试(pkg/zj)

## 独立 tester 证伪(验收独立性铁律;派单未含期望结论)
- 结论:P0=0 · P1=1 · P2=10;十发变异九发被门逮住,打穿一发(M5 注释诱饵掩护解包)。
- 有利证据(tester 实测):恒败退出码原样传播且计 fail;env 前缀真传变量;AUTH02 en/zh 循环无串标;16 处重定向全含 2>&1;机器判定面(.verify-exit.code / trap 哨兵)不受输出污染;重跑无状态残留(新进程+新浏览器+Date.now 唯一手机号);census 两条假阳性剔除属实。
- 全部发现的处置:P1-1、P2-1/2/3/4/9/10① 已修(`1bc3465`);P2-5 采纳入族(16 点);P2-6/7 census v2 改口径;P2-8 与「计数门不守新增」记账不处置(census 有账)。

## 红测 v3(修复轮后,仓形副本,变异先证落地)
基线绿 16/16 + 真登记簿 0 字节 · 仅注释诱饵→仍绿 · 诱饵+真解包→红 15≠16 · 废重跑臂→红② · 文件缺失→红 0≠16(fail-closed)。

## D4 三轮回归(最终代码 `1bc3465`,同机串行)
run2/3/4 = 445 pass / 0 fail / 0 skip,退出码 0/0/0;零真实重试、零假警报块。修复前基线 run1(`91438cf`)= 445/0/0 但含 selftest 演习弹假警报块(P1-1 的案发证据,已修)。

## Done-when 回测
- [x] D1 恒败不洗绿(红测① + tester E7a)
- [x] D2 首败后成=绿+⚠+计数+留痕+首败存档(selftest② + tester E7b/h + 修复后 v3)
- [x] D3 接线 16、解包/废臂/诱饵组合全红(红测 v3)
- [x] D4 三轮零未解释红(run2/3/4 全绿全勾)
