# T5 复验轮 4 · 提现并发面(FEAT-WD01b)

- 日期:2026-07-31
- 角色:独立验收方(实现方≠验收方),第 4 次复验
- 方式:Playwright 真页面实测。结论全部来自运行时证据(raw localStorage 直读 / `submitWithdrawal` 真实返回值 / `localStorage.setItem` 写入轨迹),**不采信「代码里写了」**
- 上轮报告:`docs/changes/2026-07-31-trust-payout-rails-t5-retest3.md`(判 FAIL 1 条:并发建单后钱没扣,40 轮 9 中 = 22.5%)
- 环境:复用已在跑的 dev server `http://localhost:5173`(node PID 32008,未重启未杀);全部访问带 `?nx_device=off`

## 大白话结论

| # | 验什么 | 结果 | 一句话 |
|---|--------|------|--------|
| **主目标** | 并发建单后钱有没有正好扣一笔 | **PASS** | **40 轮 40 中,一笔不多一笔不少**。上轮那条资金 P0 **修好了** |
| 主目标·少扣方向 | 会不会不扣 | **PASS** | 0 / 40(上轮 9 / 40) |
| 主目标·多扣方向 | 会不会扣两次 | **PASS** | 0 / 40。而且是**四个通道分别判**的:余额、可提现桶、账单行、写入轨迹里的瞬时值,全都只扣一笔 |
| 主目标·决定性证据 | 是真修好了还是这轮运气好 | **修好了** | 上轮那个「胜者写对 → 败者 1ms 后抹掉」的现场,这轮**照样发生了 3 次**,但 3 次全被补写救回(轨迹见第 2 节) |
| 1 | 单人正常提现 | **PASS** | 6/6 正好少 1 笔、单号落盘;耗时 **均值 931ms**(上轮 928ms,几乎没变,不是预期的变慢) |
| 2 | 三标签页 / window.open 并发 | **PASS** | 各 5/5 只建 1 单、只扣 1 笔 |
| 3 | 连续两笔(上限 3) | **PASS** | 4/4 余额正好少 2 笔、两单号都在、计数 = 2 |
| 4 | 坏数据 16 项 + 上限 1→3 免刷新 | **PASS** | 限额失效 0 例、当天锁死 0 例;上限项 9/9 全绿 |
| 5 | 附带发现「钱扣对了但账单行没落盘」 | **仍在,登记** | 实现方未改,确认还在:两标签页 **8/40**,三标签页 / window.open **各 4/5**。不计新 FAIL |
| 6 | 败者笼统提示 | **仍在,登记** | 「当前暂不能提交这笔提现。」未改;window.open 5/5、三标签页 7/10。不计新 FAIL |
| 哨兵 | `selfcheck-fastlane.mjs` | **PASS** | **82 pass / 0 fail**;自选 3 处注入全部变红;cp 还原后 sha 一致、回绿 |

**要主人拍板的**:资金 P0 已确认修复,**并且没有往「多扣」方向翻车**(这是本轮最该盯的风险,四个通道全查过)。剩下两条登记项(账单行丢失、败者笼统提示)是实现方本轮没动的存量项,其中账单行丢失在三标签页场景占比高达 80%,是否本轮处理请主人定。**只报不修**。

---

## 1. 主目标:并发建单后钱有没有正好扣一笔 —— **PASS(40 / 40)**

判据(两个方向都判):建单数恒 = 1;落盘余额**正好少 1 笔**(25.00)。少扣 = 0 笔是上轮那条 P0;多扣 ≥ 2 笔是本轮修法(「最多重放 3 次」)万一判据写错会翻的方向。

读法沿用上轮:**raw localStorage 直读,不初始化任何 store**(零回写污染)。提交后**+10s 与 +18s 各读一次**(主人要求 ≥8s;两次读用来分辨「暂时没收敛」和「永久」)。

### 逐轮明细(B1 = 批次 1 独占无探针,B2 = 批次 2 带 setItem 轨迹探针)

| 轮 | 建单数 | 单号 | 落盘余额 前→+10s→+18s | 扣了几笔 | 落盘最新单号 | 提现账单条数 | 账单含本单号 | 计数 | 调用时差 |
|---|---|---|---|---|---|---|---|---|---|
| B1-1 | 1 | …7263 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B1-2 | 1 | …9183 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.1ms |
| B1-3 | 1 | …2811 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.3ms |
| B1-4 | 1 | …2766 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.6ms |
| B1-5 | 1 | …4066 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | −0.1ms |
| B1-6 | 1 | …6403 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.1ms |
| B1-7 | 1 | …5070 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B1-8 | 1 | …1803 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.2ms |
| B1-9 | 1 | …3043 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.7ms |
| B1-10 | 1 | …3041 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B1-11 | 1 | …4323 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B1-12 | 1 | …1329 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0.2ms |
| B1-13 | 1 | …4717 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −1.0ms |
| B1-14 | 1 | …3830 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.7ms |
| B1-15 | 1 | …5486 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.7ms |
| B1-16 | 1 | …9621 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | 0.1ms |
| B1-17 | 1 | …6670 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.2ms |
| B1-18 | 1 | …5741 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | 0.1ms |
| B1-19 | 1 | …8851 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.2ms |
| B1-20 | 1 | …6220 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | −0.1ms |
| B2-1 | 1 | …8719 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | 0.1ms |
| B2-2 | 1 | …5550 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.1ms |
| B2-3 | 1 | …8115 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0.1ms |
| B2-4 | 1 | …8092 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0.1ms |
| B2-5 | 1 | …2171 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.3ms |
| B2-6 | 1 | …1540 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | 0.6ms |
| B2-7 | 1 | …9156 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0.2ms |
| B2-8 | 1 | …4426 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B2-9 | 1 | …7969 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.1ms |
| B2-10 | 1 | …1265 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | 0ms |
| B2-11 | 1 | …1298 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B2-12 | 1 | …9741 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 1 | ⚠️缺 | c=1 | −0.1ms |
| B2-13 | 1 | …8387 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B2-14 | 1 | …1552 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.3ms |
| B2-15 | 1 | …6085 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.8ms |
| B2-16 | 1 | …8397 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.6ms |
| B2-17 | 1 | …1376 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |
| B2-18 | 1 | …4568 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −1.1ms |
| B2-19 | 1 | …3211 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | −0.1ms |
| B2-20 | 1 | …3487 | 24856.56 → 24831.56 → 24831.56 | 1 | ✅ | 0 → 3 | ✅ | c=1 | 0ms |

### 合并统计

```
40 轮:建单数≠1 = 0        🔴 少扣 = 0        🔴🔴 多扣 = 0        🔴🔴 非整数扣 = 0
      落盘最新单号 = null → 0        单号对不上 → 0
      落盘计数 40/40 恒 {dayIndex:20665, count:1}
      +10s 与 +18s 读数不同 → 0(结果一落定就不再变)
      扣了几笔分布 {"1":40}          调用时差 |≤1.1ms|(绝大多数 ≤0.3ms)
      胜者分布 {B:32, A:8}           pageErrors 全 40 轮为空
```

### 多扣方向:四个通道分别判,全过

主人点名「多扣比少扣更严重」,所以不只看余额一个口径:

| 通道 | 判据 | 结果 |
|---|---|---|
| USDT 余额 | `(前 − 后) / 25 ≥ 2` | **0 / 40** |
| 可提现桶 `withdrawableUsdt` | 同上,正交字段(余额对了桶多扣也算 bug) | **0 / 20**(批次 2 全测,分布 `{"1":20}`) |
| 非整数扣款 | `扣了几笔` 偏离整数 > 0.001 | **0 / 40** |
| 写入轨迹里的**瞬时**值 | 任何一次落盘写入的余额低于「正好扣一笔」半笔以上 | **0 / 20**(批次 2 全 54 条/轮写入逐条查,最低值恒 = 24831.56) |
| 账单行重复 | 同一单号写出两条 USDT 提现行 / 出现重复 bill id | **0 / 40**(见第 6 节 harness 自证伪) |

第 4 条尤其关键:**即使最终收敛正确,只要中途写出过「扣两笔」的落盘,也会被这条抓到**——40 轮里一次都没有。原因在轨迹里看得很清楚:补写在第 1 次尝试就命中单号并 `break`,3 次上限从没跑满过。

### 统计说服力

上轮失败率 22.5%(9/40)。若真实失败率仍是 22.5%,这轮 40 轮全绿的概率 = 0.775⁴⁰ ≈ **3.7×10⁻⁵**。
反过来给出界:0/40 对应失败率的 95% 置信上界 ≈ **7.2%** —— 也就是说「还剩个位数百分比的残余概率」还不能被 40 轮排除,但「22.5% 那个量级的 bug」已经被排除得很干净。

---

## 2. 决定性证据:上轮的失败现场这轮**照样发生**,但被救回了

这一节回答墨菲清单上最要命的一问:**40 轮全绿,是真修好了,还是这轮竞争根本没打到那个点?**

我拦 `Storage.prototype.setItem` 记录批次 2 每一轮对 `nexgrid-account-cloud-v1` 的**每一次**写入,再机器扫描「首次写对之后有没有被写回旧值」。结果:

```
20 轮里「写对后又被写回旧值」(=上轮的失败起手式)发生: 3 轮 [6, 12, 13]
其中最终救回: 3 轮 —— 3/3
```

三条完整轨迹(`t=0` = 胜者 `submitWithdrawal` 调用时刻):

**轮 13(胜者 B)—— 与上轮失败轨迹逐字同构:**

```
A@-0ms    bal=24856.56  无单
B@-0ms    bal=24856.56  无单
B@155ms   bal=24831.56  单在     ← 胜者落盘:正确
A@155ms   bal=24856.56  无单     ← 败者同刻整份覆盖:钱和单一起被抹掉(上轮到这里就永久丢了)
B@312ms   bal=24831.56  单在     ← ✅ 胜者补写写回**正确值**(上轮这一步写的是旧值)
B@315ms   bal=24831.56  单在
A@445ms   bal=24831.56  单在     ← 败者下一次 tick 也读到了正确值,不再回退
...(其后每秒 tick 恒 bal=24831.56 单在,共 54 条写入,末次 B@17308ms 仍正确)
```

**轮 6(胜者 A):**

```
A@156ms   bal=24831.56  单在     ← 胜者落盘:正确
B@156ms   bal=24856.56  无单     ← 败者覆盖
B@313ms   bal=24831.56  单在     ← ✅ 救回
B@319ms   bal=24831.56  单在
A@447ms   bal=24831.56  单在
```

**轮 12(胜者 A):**

```
A@155ms   bal=24831.56  单在
B@155ms   bal=24856.56  无单     ← 败者覆盖
B@327ms   bal=24831.56  单在     ← ✅ 救回
B@330ms   bal=24831.56  单在
A@468ms   bal=24831.56  单在
```

对照上轮同一位置的失败轨迹(原文):

```
B@154ms  bal=24831.56  单在
A@155ms  bal=24856.56  无单
B@297ms  bal=24856.56  无单     ← 🔴 上轮:补写写回的是**旧值**
```

**同一个起手式、同一个时间尺度,补写那一下从「写旧值」变成了「写正确值」。** 这就是修复在运行时的直接体现,不是「这轮没打到」。

补充两个从轨迹读出来的事实:

- 补写**只发生一次就收敛**(第 1 次尝试即命中单号 break),所以「最多 3 次」这个上限在实测中从未跑满 —— 这也是没出现多扣的机制层原因。
- 收敛后两页每秒的 tick 一路写正确值,17 秒内 54 次写入无一回退。

---

## 3. 墨菲:先把「是我测绿了」逐个证伪

| 怀疑 | 对照实验 | 结果 |
|---|---|---|
| 竞争窗口这轮根本没打到? | 逐轮记两次 `submitWithdrawal` 真实调用时刻;并机器扫描轨迹找「覆盖」现场 | 40 轮时差 \|≤1.1ms\|;**3 轮抓到完整覆盖现场** → 打到了,而且是同一个点 |
| dev server 还在跑**旧代码**(HMR 没更新),我测的是幻觉? | 直接 `curl http://localhost:5173/src/store/app.ts` 取运行中模块,grep 重放循环 | HTTP 200 / 135650 B,内含 `for (let attempt = 0; attempt < 3; attempt++)` 与 `stored?.latestWithdrawal?.id === id` → **跑的就是修复后的代码** |
| 读太早,还没收敛就读绿了? | 每轮 +10s 与 +18s 各读一次 | 40 轮**两次读数完全一致**,0 例变化 → 不是「读早了刚好对」 |
| 只看余额一个口径,多扣藏在别处? | 加 3 个正交通道:可提现桶 / 非整数 / 写入轨迹瞬时最低值 | 全 0(见第 1 节表) |
| 背景负载稀释了复现率? | 批次 1、批次 2 **均独占跑**(上轮已证实并行会稀释) | 两批各 20 轮独占,均 0 失败 |
| 探针本身把结果搅了? | 批次 1 **不带任何 setItem 探针**,批次 2 才带 | 两批结果一致(各 20/20)→ 探针无影响 |
| 我读错了 key / 账号行? | 同一套 raw 直读 | 40/40 读出真实单号与 24831.56 → 读法没问题 |
| 平台日边界跨了导致计数重置假绿? | 逐轮读落盘计数 | 40/40 恒 `{dayIndex:20665, count:1}`,测试全程在同一平台日内(边界 08-01 02:00) |

---

## 4. 回归项

### 1)单人正常提现 —— PASS

```
6 次:耗时 [922,935,926,935,933,937] ms   最小 922  最大 937  均值 931
     余额 24856.56 → 24831.56(6/6 正好少 1 笔)
     落盘最新单号 6/6 与返回单号一致;落盘账单含本单号 6/6;落盘计数 {dayIndex:20665, count:1}
     pageErrors 全空
```

**耗时报数**:上轮 928ms → 本轮 **931ms(+3ms)**。主人预期「这轮会更长(最坏约 1.4 秒)」,**实测没有变长**。原因在第 2 节的轨迹里:重放第 1 次尝试就命中单号 break,3 次上限从未跑满,所以最坏 1.4s 是理论上界而非实测值;6 次采样的 p100 是 937ms,仍在「点了立刻有反应」区间。

### 2)三标签页 / 同进程标签页(window.open)—— PASS

```
triple  5 轮:建单数≠1 = 0  🔴少扣 0  🔴🔴多扣 0  单号对不上 0   扣了几笔分布 {"1":5}
             三页调用时差 [0, ≤0.1ms, 0]
winopen 5 轮:建单数≠1 = 0  🔴少扣 0  🔴🔴多扣 0  单号对不上 0   扣了几笔分布 {"1":5}
```

### 3)连续两笔(上限调到 3,一笔一笔顺序提)—— PASS

```
4 轮:建单数全 = 2   总扣分布 {"2":4}   计数全 = 2   两单号都在 4/4   落盘账单含两单号 4/4
      余额 24856.56 → 24831.56 → 24806.56(正好少 2 笔)
      落盘最新单号 = 第 2 笔
```

### 4a)坏数据 16 项 —— PASS

```
共 16 例;限额失效 0 例;当天锁死 0 例;pageErrors 全空
```

`1e9` / `Number.MAX_VALUE` / 小数 / 负数 / null / 字符串 / `dayIndex` 各种荒谬值 / 整行非对象 / 整表 garbage / `claimToken` 被塞成对象 —— 全部按「读不到」处理(`todayCountFrom` 恒 0),既不放行超额也不锁死当天,与上轮一致无回归。

### 4b)上限 1→3 免刷新生效 —— PASS(9/9)

```
PASS  L.a 上限1、已提1笔 → 第2笔置灰      {"aria":"true","label":"…今日提现次数已用完,08-01 00:00 后可再提。"}
PASS  L.b 上限 1→3 免刷新即解灰           {"aria":"false","bg":"rgb(158, 220, 29)"}
PASS  L.c 第2、3笔放行(3 个不同单号)      ["WD-…-4528","WD-…-4217","WD-…-8670"]
PASS  L.d 落盘计数=3                      {"dayIndex":20665,"count":3,"claimToken":"ms8qyt63-snzcygfv"}
PASS  L.e 余额正好少 3 笔(25+26+27=78)   {"before":24856.56,"after":24778.56,"delta":78}
PASS  L.f 落盘最新单号 == 第3笔
PASS  L.g 第4笔被拦(CTA 灰 + 无新单)      {"blocked":true,"calls":3}
PASS  L.h 第4笔后余额不变                 24778.56 → 24778.56
PASS  L.i 第4笔后计数仍=3
```

(L.a 显示 `00:00` 而非 `02:00`,是这条用例把浏览器时区设成 `Asia/Ho_Chi_Minh` 所致,与上轮完全一致,非回归。)

---

## 5. 登记项(实现方本轮未改,按主人要求只登记,不计 FAIL)

### 5-1「钱扣对了但账单行没落盘」—— 仍在

先说清结构,免得误判成多扣:**一笔提现设计上写两条账单行** —— `-25 USDT`(principal,`pending`)+ `-15 NEX`(手续费,`posted`),两条共用同一个单号 ref;另有一条种子历史单 `WD-20260516-3284 / -20 USDT` 恒在。丢的时候**两条一起丢**,落盘只剩那条种子单。

| 场景 | 本次单号的账单行落盘了吗 | 占比 |
|---|---|---|
| 两标签页并发(批次 1+2) | 缺 8 / 40 | **20%** |
| 三标签页并发 | 缺 4 / 5 | **80%** |
| window.open 并发 | 缺 4 / 5 | **80%** |
| 单人提现 | 缺 0 / 6 | 0% |
| 连续两笔 | 缺 0 / 4 | 0% |

上轮记录为 5/20 = 25%(两标签页),本轮 20%,**同一量级、未改善**。新增信息:**并发页数越多丢得越狠**,三页场景 80%。严重度仍低于 P0(不产生凭空的钱,只是这笔在账单里查不到),但用户视角是「钱扣了、单在、账单里没有这笔流水」。

### 5-2 败者笼统提示 —— 仍在

| 提示原文 | 出现 | 判断 |
|---|---|---|
| 「今日提现次数已用完,08-01 02:00 后可再提。」 | 两标签页 **40/40**、三标签页 3/10 | ✅ 与实际相符,给了绝对时刻 |
| 「当前暂不能提交这笔提现。」 | **window.open 5/5**、**三标签页 7/10** | ⚠️ MEDIUM 遗留:没说原因、没给下一步 |

实现方**未改**这一条,确认仍存在。与上轮相比有个变化值得登记:三标签页场景里笼统提示的占比从上轮 1/10 升到 **7/10** —— 并发页越多,败者越容易走 UI 预判分支(点击时重新评估资格就发现额度没了,`submitWithdrawal` 一次都没被调用),于是看到这句没信息量的话。建议接到同一句「已用完 + 绝对时刻」。

---

## 6. 哨兵 —— PASS

**基线**:`node scripts/selfcheck-fastlane.mjs` → **82 pass / 0 fail**(exit 0),与主人给的期望一致。

自选 3 处注入(全部落在本轮修复面),每次都先自证「真落盘」(新文本在 + 旧文本没了 + sha 变了),避免注入没生效的假绿:

| # | 注入 | 注入自证 | 哨兵结果 | 抓到的断言 |
|---|---|---|---|---|
| R1 | 去掉「重放前先把落盘现状认作基线」(`adoptAccountSnapshot(stored)`)—— 上轮实测的失败形态 | ✓✓✓ | exit=1 **81 / 1** | 🔴 重放前先把落盘现状认作基线 |
| R2 | 重放判据由**单号**换成余额比较(主人点名的多扣风险方向) | ✓✓✓ | exit=1 **80 / 2** | 🔴 落盘后按单号收敛重放 + 🔴 重放判据是单号 |
| R3 | 重放上限 3 → 1 | ✓✓✓ | exit=1 **80 / 2** | 🔴 落盘后按单号收敛重放 + 🔴 重放次数有上限 |

**3 / 3 全部变红。**

**还原纪律**:全程 `fs.copyFileSync`(等价 `cp`),**未使用 `git checkout`**(工作树有大量未提交改动,`git checkout` 会抹掉整轮工作)。逐文件 sha256 比对:

```
基线 sha            = c539a2813019…
每次注入后 cp 还原   → sha 一致 = true(3/3)
最终:src/store/app.ts                          c539a281…  == 备份 ✅
      src/store/withdraw-daily-count.ts         a60dd771…  == 备份 ✅
      src/store/withdrawal-eligibility-core.ts  fbc0e372…  == 备份 ✅
      src/store/withdrawal-eligibility.ts       c9fd6384…  == 备份 ✅
还原后哨兵: exit=0   82 pass / 0 fail
```

红测在**所有浏览器批次跑完之后**才执行 —— 注入会改源文件,Vite HMR 会把改动推给正在跑的页面,若与实测同时进行会污染实测结果。

---

## 7. Harness 自证伪记录

按纪律留档,以下是**我的探针问题,不是被测缺陷**:

1. **差点误报一条「多扣」P0**。本轮我新加了「本单号在账单里出现次数 ≥ 2 = 多扣」这条判据,批次 2 直接报 **16/20 命中**。按纪律先证伪再下结论:把落盘账单整行 dump 出来,并**用单人(无并发)提现做对照** —— 单人提现同样是 2 行(`-25 USDT` principal + `-15 NEX` 手续费,共用 ref),并发轮 46 条账单行 **id 全唯一(46/46)**。结论:**判据写错了,不是被测缺陷**。若不做这一步对照,本轮会错判一条本不存在的资金 P0。
2. 沿用上轮已修正的两个坑:账单 raw 形状是 `.bills` 不是 `.items`;`$onAction` 拦不到 `persistAccountSnapshot`(setup store 内部闭包调用不走 action 代理),必须拦 `Storage.prototype.setItem`。

沿用并复现的环境事实:必须用 `localhost` 不能用 `127.0.0.1`;`page.goto` 只改 hash 不重载页面(每次 boot 带不同 query);注入 uni storage 要 `{type:'object',data:X}` 包装;uni 产物是 `<uni-text>`;`submitWithdrawal` 是 async 必须 await;toast 会自动消失、要 50ms 轮询 `ui.toasts` 累积。

**收尾**:已删本轮自建 png、无 `.playwright-mcp/` 产物;本轮起的 headless chromium 已随进程退出回收;**未动** 5173 的 dev server(node PID 32008)与主人自己的 Chrome。

---

裁决: PASS

> 说明:上轮那条资金 P0(并发建单后钱没扣)**已修复** —— 40 轮 40 中正好扣一笔,且上轮的失败现场在本轮复现 3 次、3 次全被补写救回;主人重点盯的**「多扣」方向四个通道全 0**。被指定的 6 项回归与哨兵红测全部 PASS。两条登记项(账单行未落盘、败者笼统提示)是实现方本轮未动的存量项,按主人要求登记、不计入 FAIL 数。
