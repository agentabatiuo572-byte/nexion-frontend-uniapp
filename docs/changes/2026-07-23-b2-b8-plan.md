# B2–B8 · 85 个子页按 11 册规范对齐(分批计划)

> 工作线 ④ uniapp · 定级 L · 状态 **✅ 全批收口(B2–B8)**
> 字号债务 **1235 → 8**(降 1227,剩下 8 处是 emoji 当图标用,判定不动)

## 前置:横切三批已把三个维度清干净

C3(触控 ≥44 + 按下反馈)、C4(文案 i18n)、C5(空状态体系)是**全站**横切,已收口。
所以 B2–B8 不再重复这三件事,只做剩下的:**字号阶梯 · 行高 · 圆角阶梯 · 间距 8pt grid**。

## 真实工作量(不是拍脑袋,来自机器门基线)

`docs/VALUE-LADDER-BASELINE.json` 记着字号债务:**1235 处 / 150 文件**不在《02》14 档合法集
`{56,44,36,34,26,20,15,13,12}`。这是 B0 定的债务台账,棘轮只许降不许涨。

| 批 | 范围 | 字号债务 |
|---|---|---:|
| B2 | 钱包群(wallet* / topup / receipt) | 183 |
| B3 | 商店订单(store/*) | 103 |
| B4 | team(team/*) | 213 |
| B5 | genesis + staking | 144 |
| B6 | onboarding + auth(login/register/session/ref) | 93 |
| B7 | me 其余 | 250 |
| B8 | 杂项(trust / daily / developer / market / globe / events / compute-share …) | 249 |

圆角债务:离散值 `2 2.5 3 4 6 7 8 9 10 14 18 22` 需上阶梯。

迁移映射表:`docs/TYPO-MIGRATION-MAP.md`(主人已过目)。

## 🔴 每批必走的四步(B1 的教训直接固化成流程)

1. **改前采溢出基线** `node scripts/overflow-probe.mjs --out <baseline>`
   —— 字号只会变大不会变小(11.5→12 / 10.5→12 / 13.5→13 有升有降),**撑破容器是主要风险**,
   B1 迁移时溢出探针实抓 4 次真回归(live-feed 截断、排名卡撑破、me 菜单副标题…)。
2. **按映射表迁移**,改字号**必同步补该档行高**(表里每档都写了 字号/行高/字重)。
   🔴 判定线不能机械套:`12.5px` 要看用途 —— 需用户「理解/决策/确认/阅读」→13,纯状态/时间/短标签→12。
3. **溢出 diff 判无新增**;有新增就地修(缩边距 / 加 truncate / 换档),不靠"看着还行"放过。
4. **机器门 + 实景**:vue-tsc 0 · verify 全绿(含 value-ladder 棘轮必须**降**)· 改动页双主题截图。

## 🔴 B1 踩过、这几批要提前防的

- **同一把尺子量自己**:B1 时迁移 codemod 与字号哨兵共用一个只认 kebab-case 的正则,
  漏了 camelCase `fontSize:"11.5px"` 与 UnoCSS `text-[11px]`,结果「迁移完成」是假的(实际只做了 67%)。
  哨兵已改三写法覆盖,但**每批仍要用哨兵实测数字下降**,不看"我改了 N 处"的自述。
- **改字号会连带撑高父容器**:C3 里把 genesis hero 内的 pill 从 34 抬到 44,hero 高度 232→242,
  连带让零-border 登记表的 size 失配报假新增。改尺寸后**顺手核对各基线里 pin 了尺寸的条目**。
- **孤字断行**:字号变大后中文换行点变化,《02》禁末行单字 —— 迁移后要复查长文案段落。

## 进度

- [x] **B2 钱包群** —— 192 处 → 剩 1 处(那 1 处是 `⚠️` emoji 当图标用,判为图形尺寸不动)
  - 机械映射 140 处(48→56 · 30→34 · 24→26 · 18→20 · 13.5→13 · 11.5/11/10.5/10→12)
  - 语义判定 51 处:28→26(金额是数值不是页面标题)· 14→15/13(主信息 vs 说明)·
    12.5→13(钱包域费用/明细/状态都过「需理解决策」判定线)· 12.5→12(纯辅助 1 处)
  - 🔴 顺带修**全站共享**的子页顶栏标题 17→20:《02》14 档无顶栏专档,先量后改
    (标题盒 28 vs 顶栏 56 放得下),影响全部 85 个子页
  - 哨兵实测:字号债务 **1235 → 1040**(降 195)· 溢出新增 0 · verify 328 pass
  - commit `c062f88`(前置机械批 `d28dd70`)

- [x] **B3 商店订单** —— 101 处 → **剩 0**
  - 机械 79 处 + 语义 22 处; 在两个文件里**同名不同义**(bundle 是 eyebrow 小标题、
    order-detail 是订单状态主信息),按「文件+常量名」而非只按常量名判 → 前者留 12、后者升 15
  - 22px 倍数徽章 → 20(徽章是短标签不是标题,不升 26)· 阶段状态词 12.5 → 12(纯状态)
  - 哨兵实测:债务 1040 → **939** · 溢出 diff(改动页真实对照)新增 0 · verify 328 pass
- [x] **B4 team** —— 213 → 1(captionStyle 归 12 · 输入框/提交按钮归 15)
- [x] **B5 genesis+staking** —— 144 → 0(emoji 图标整行跳过 · $ 符号 22→20 不跟主数字升 26)
- [x] **B6 onboarding+auth** —— 93 → 0(登录/注册主标题 28→34;320 窄屏专测不截断)
- [x] **B7 me 其余** —— 242 → 0(labelStyle 归 body.s 13;统计磁贴溢出**改文案根因**不回退字号)
- [x] **B8 杂项** —— 275 → 8(剩下 8 处全是 emoji 图标;顺带修探针把「truncate 生效」误报成溢出)
