# 交接书恢复单源 —— 指针瘦身 + U-4 撞号重编 U-19 + 指针门

主人 2026-08-14 指令:两份交接书漂移。约定(本文件旧尾注 + 后台仓正文)= 正文单份维护在后台仓
`nexion-ops-console/docs/PRD/HANDOFF-后端与待接手事项_2026-08-07.md`,本仓 `docs/HANDOFF-后端与待接手事项.md`
只放指针/索引;实况 = 12 条 U-x 带全文长在本仓索引里(「正文章节」列写着「本文件(新增)」),
且撞号:本仓 U-4「提现日限计数口径」 vs 正文 U-4「publicStats 投影」是**两个不同主题共用一个编号**。
索引反向也在漂:正文 08-13 新增的 Q-13 / E-6 没回流进本仓索引,正文自己的 U-4(publicStats)索引里也没有 ——
这正是撞号的成因:取号的人在本仓看不见正文已用的号。

## 方向裁决:A(搬回后台仓,恢复约定),没选 B(承认现状换权威)

- 正文是**跨仓总账**(〇三件事 / 一 12 条需求 / 二 B-x+U-x / 三 Q-x / 五 E-x / 六 转派批活 / 七约定),
  还有 specs 快照目录靠相对链接挂在它旁边,开场白就是「你从 GitHub 拉到这份文档时」——给后端同事拉
  `nexion-ops-console` 仓设计的交接面;
- 消费面全部指向后台仓:PLAN 根《HANDOFF-待办台账总览》§六、specs-snapshot README、本仓 08-11 变更卡;
- 两侧都在写(admin 侧有 g9~g13 五个 handoff 包 + Q-13/E-6),B 只是把漂移压力换个方向,还要整本书搬家。

## 做了什么

1. **12 条正文原样迁回**后台仓 §2.2a(文字未改写):U-5/U-6/U-7/U-8/U-11/U-13/U-14/U-15/U-16/U-17/U-18/U-19。
2. **撞号重编**:本仓旧 U-4(日限口径)→ **U-19**(全工作区原零占用);正文 U-4 = publicStats 不动。
   引用面:`wallet-withdraw.vue` 两处注释 U-4→U-19;08-11~13 变更卡是带日期历史**不改**,由两份文件
   头部的改号说明兜底(「变更卡里的 U-4 = 现在的 U-19」)。U-12 历史跳号,从未使用,非丢条。
3. **本文件降回纯指针 + 一行式索引**(每条 ≤300 字符),并补上此前缺行的 U-4(publicStats)/ Q-13 / E-6。
4. **焊门** `scripts/handoff-pointer-gate.mjs`,挂 `verify.sh` 静态段(conflict_marker_gate 旁,不依赖 dev server):
   ① 表格行 ≤300 字符;② 禁「本文件(新增」;③ 索引 ID 在正文可寻(指针不许指空);
   ④ 反向:正文加粗 **U-x** 必须全在索引(Q-13/E-6 式漏行);⑤ 正控两道(索引行数下限 + 正文提取量下限)。
   后台仓缺席(临时 worktree / 独立 checkout)时 ③④⑤b 显式 SKIP,①②⑤a 照跑,PASS 行写明跑了哪半。
   门站在本仓,因为写入压力在本仓(12 条全是本仓会话写的),后台仓的 verify 管不到本仓的提交。

## 条目守恒(机器核对,scratchpad conserve-check)

旧两份 ID 全集 45 项(旧指针 20 + 旧正文 25,含重叠)逐一在新正文找到,U-x 同时在新索引找到,零丢失;
12 条迁移正文 normalize 空白后**逐字包含**于新正文(最长 U-11 987 字)。
新索引 23 个 ID = U-1..U-11 + U-13..U-19(18)+ Q-7/Q-11/Q-12/Q-13 + E-6;新正文 37 个 ID(含 B-x 之外全部)。

## 双向红测(逐条隔离,先证变异落盘,期望钉注入内容,备份还原不走 git checkout)

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| 基线1 | 跨仓接上(env 指向新正文) | PASS 且带「双向集合相等」 | ✅ |
| 基线2 | 正文路径不可达 | PASS 但显式打 SKIP | ✅ |
| M1 | 追加 320 字正文型长行(合法 ID) | 只有「行超长」判红 | ✅ |
| M2 | 追加带「本文件(新增」标记的短行 | 判红且点名标记 | ✅ |
| M3 | 索引追加 U-98(正文没有) | 判红且点名 U-98 | ✅ |
| M4 | 正文副本追加 **U-97**(索引没有) | 反向判红且点名 U-97 | ✅ |
| M5 | 清空索引全部 ID 行 | 正控 5a 判红(不许假绿) | ✅ |
| M6 | 正文加粗约定剥掉(`**U-` → `U-`) | 正控 5b 判红(提取机制坏≠绿) | ✅ |
| 终态 | 全部还原 | 回绿 | ✅ |

## 没解决的(如实)

- 反向镜像判据依赖「正文表格 ID 加粗 **U-x**」的写法约定,新条目不加粗时反向半边看不见,由正控 5b
  兜住下限(<10 判红),但 10~N 之间的不加粗漏网仍靠人;Q-x/E-x 不做双向(索引只收用户端相关者),
  未来正文新增 Q-14+ 是否进索引仍靠约定。
- 后台仓那侧没有对称的门(admin verify 不核此文件);当前写入压力全在本仓侧,先焊这半。
