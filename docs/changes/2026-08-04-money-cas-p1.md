# P1 涉钱 / 配额 store 接乐观并发(CAS)— 跨标签页竞态双记账修复 · 红测留痕(2026-08-04)

存量 P1 二期。一期(`docs/changes/2026-08-04-staking-cas-redtest.md`)把共用能力
`writeAccountRowCas` 立起来并只接了质押;那份文档末尾的「后续:还有哪些 store 该接 CAS」
表列出的 **P1 档六个 store**,本轮全部接完。

判据未变:**该 store 的行承载资金 / 资格,且存在「同一笔只能消费一次」的语义**。

| store | 修前的洞 |
|---|---|
| `deposits.ts` | 入金单状态推进 + 到账入账,两端并发推进 = 同一笔充值入账两次 |
| `voucher.ts` | 代金券 claim 账本,同一张券两个标签页各领一次 |
| `nex-faucet.ts` | 每日签到 / 里程碑 / streak saver,覆盖式写 = 一天领两次 |
| `daily-powerup.ts` | 连胜增益一次性激活,覆盖 = 白送一档 |
| `lucky-spin.ts` | 每日免费票 + bonus 票,覆盖 = 一张票抽两次奖 |
| `withdraw-daily-count.ts` | 提现日限额计数,覆盖 = 限额白设 |

根因与一期同一条:H5 端 uni storage 就是 localStorage,同源多标签页共享同一份,而全仓没有
任何 storage 事件重新水合这些 store —— 窗口**不是几毫秒的传播延迟,而是「两个标签页只要都
打开过,状态就永久不同步」**。而 `usdtBalance` / `nexBalance` 在 `account-cloud.ts` 的
`ADDITIVE_NUMBER_KEYS` 里(按增量三路合并)→ 两次入账都记 = **真·双花**。

## 修法:一个共用提交器,六个 store 全路径走它

`account-scoped-storage.ts` 新增 `createAccountRowCommit({ tableKey, parse, snapshot, sync })`,
返回 `{ bind, commit, accountKey }`。`commit(apply)` 就是质押那个 `commit()` 的共用引擎:

1. 基准取**磁盘最新**行,不是本标签页可能已经陈旧几小时的内存副本;
2. `apply` 在新鲜状态上**重新校验前置条件** —— 别处已经领走 / 推进过的返回 `null`,调用方拿到
   `ok:false`,绝不会第二次入账(这是双花的根);
3. 带 `rev` 做 CAS 落盘;`rev` 被推进过说明 1→3 之间又被插了一脚,重跑一轮(有界 3 次)。

失败仍分两类:`conflict:true` = 期间被别处改过(页面弹「刚在别处更新过」),
`conflict:false` = 本来就不该成交(不存在 / 已领 / 未到期,既有语义原样保留)。

**为什么抽成共用件而不是每个 store 抄一份**:重试上界、conflict 归因、「失败也要把磁盘最新态
刷进内存」这三条都是**错了不会报错、只会静默双花**的细节;抄 6 份 = 6 个各自长歪的机会。

**`staking.ts` 本轮一个字没动**:它的机器门 ⑥ 按行文 pin 了 `readSnapshot(boundKey)` 等实现
细节,重构它等于同轮改判据;且当时它正被并发会话改着。代价是它保留自己那份内联 `commit()`
(一份重复),换来不动别人在飞的工作 + 不自改判据。已在共用件注释里写明。

### 顺带修掉三个被本修复放大的隐患

- **入账顺序**:`settleCredited` / `settleBankIntent` 原先是「先 `recordDeposit` 动钱、再 patch
  状态」。两端各拿陈旧内存副本判状态门 → 两次入账都过。现在统一「先把状态 CAS 落盘,过了才
  动钱」(与 `submitCardPayment` 既有铁律同形);动钱失败则把单据回滚,宁可「无单无钱」也不留
  「有单无钱」。
- **`settleBankIntent` 原先没有任何状态门**(全靠三个调用方各自把关自己那条边),补了
  `credited` 终态门 —— 这是银行轨双花的闸。
- **单号铸造**:`mintDepositId` 原先只对**内存**账本查重。CAS 之前撞号被 last-write-wins 掩盖
  (其中一笔本来就会被顶掉),现在两笔都留得住,撞号就成真事故(`depositId` 是三轨共享主键,
  `find()` 永远命中第一笔)。改成对**入参那份磁盘最新账本**查重 —— 与质押 `stk-${Date.now()}`
  那条同型。

### 对外签名

`conflict` 一律是**新增可选字段**。原本就返回对象的(`signIn`)直接加;原本返回 `boolean` 的
四个(`voucher.claim` / `dailyPowerUp.claim` / `useSaver` / `claimMilestone`)、返回
`string|null` 的 `luckySpin.spin`、返回 `boolean` 的 `cancelBankIntent` 改成
`{ ok, …, conflict? }` —— 否则页面无从分辨「已在别处处理」与「点了没反应」。

🔴 **这一改带一个陷阱**:返回对象后 `if (store.claim(x))` **恒真**。六处调用点全部改成读 `.ok`,
并由机器门 ⑥ 逐处 pin(见下面 R13)。其中 `daily.vue` 的 `claimMilestone` 原本就**丢弃返回值**
后无条件发奖 —— 本轮修掉,否则接了闸也照样白发。

### 🔴 诚实边界(与一期同口径)

localStorage 没有锁,「比 rev」与「写回」终究是两步,亚毫秒级插队窗口**无法在客户端消除**。
本机制根治的是真正会发生的那个窗口 ——「状态永久陈旧」;残余窗口靠真后端单事务。

`withdraw-daily-count.ts` 是**两道互补的闸**,谁也替不了谁,故令牌那套一个字没删:

- **CAS 治「写覆盖」**:同进程 / 传播已收敛的并发,后写的拿旧版本号被挡下并**重判额度**;
- **CAS 治不了「读陈旧」**:跨渲染进程时两边都读到写入前的版本号,各自 CAS 都过 ——
  那一格仍然只能靠「写后等传播收敛 + 回读验令牌」分胜负(原注释里 12 轮 12 中的实测结论)。

## 向后兼容

`writeAccountRow` **一个字节没动**,未接 CAS 的调用方行为完全不变(不读 rev、不写 rev、依旧
last-write-wins),机器门 ④ 逐项复验并结构断言「老写法函数体内 0 处 rev / CAS 引用」。
老行(无 `rev`)天然从 0 起步,**零迁移**。

爆炸半径由 `selfcheck-staking-cas.mjs` ④ 的**登记名单**守:接 CAS 的 store 必须恰好是册上那
7 个(6 个本轮 + staking),多一个未登记的就红。

## 机器门

`scripts/selfcheck-money-cas.mjs`(**108 pass / 0 fail**),已焊进 `scripts/verify.sh` 末段。
结构断言跑在剥注释后的正主源码上;行为断言 esbuild 载**真 store + 真 storage 层**跑真代码,
两个 store 实例 = 两个标签页共享一份 JSON 序列化的假 storage(与 localStorage 同语义:跨标签页
拿不到同一个对象引用)。

样本:6 个 P1 store · 16 组跨标签页固定靶 · 2 个 store 实例共享 1 份 storage · 3 语 i18n。

🔴 **stub 边界**:只有 `deposits` 的 `app` / `bills` / `fx` 三个**跨 store 组合方**换成可观测的
假账本 —— 它们不是被测对象,而双花的判据恰恰是「假账本上余额动了几次 / `recordDeposit` 被调了
几次」。`deposits` 自身、`deposits-core`、`account-scoped-storage` 全是真代码;其余五个 store
一个依赖都没 stub。

### 顺带跟进的两处既有门(不改判据,只跟上被测对象的新形态)

- `verify.sh` 的 P2-8「store is account-scoped」哨兵原先 grep `writeAccountRow`;四个转换后的
  store 改走 `createAccountRowCommit`,模式放宽成 `writeAccountRow|createAccountRowCommit`
  (**意图不变**:落盘按账号分行)。
- `selfcheck-withdraw-freeze.mjs` 自带 `storage-stub`,缺 `createAccountRowCommit` 导致
  bundle 直接失败。补上,但实现是**抛错**而非假对象 —— 本门只用 `nex-faucet` 的两个纯函数、
  从不实例化那个 store,真被调到说明用法变了,炸出来比静默跑偏好。

### 正交维度:绕过 commit 的第二种走法

「0 处裸 `writeAccountRow`」查的是**存在的对不对**,查不到「该走 commit 的有没有绕过去」。
真正的绕法不是再写一次盘,而是**直接改内存 ref 根本不落盘** —— 页面当场看着对,刷新即丢,
而且完全绕开了前置条件复核。补了一条判据:每个持久化 ref 在全文只许被赋值 2 次(提交器的
`sync` 回调 + `bindAccount`),多一次就是有人在 commit 之外动了它(红测 R15)。

复核结果:五个 store 的 16 个持久化 ref 全部合规;`lucky-spin` 的 `open/phase/lastWonPrizeId/
wheelAngle` 是会话态(本就不落盘),`deposits` 的 `chainChannelEnabled/bankAccounts` 是 server
配置非用户态(本就不入账号分行),两类都不在名单里。

## 红测:15 个合取项逐个隔离,15/15 命中

驱动脚本按合取项逐个破坏 → 断言对应固定靶必红 → **`cp` 备份还原(禁 `git checkout`,工作树有
并发 agent 的未提交改动)** → sha256 byte-identical 校验 → 复跑复绿。
注入靶点命中数 ≠ 1 或注入没落盘 → **立刻终止,绝不在未生效的注入上判红绿**(实际触发过一次:
多行锚点用了 `\n` 而源文件是 CRLF,命中 0 次 → 驱动按规则中止,改成随文件行尾自适应后重跑)。

| 编号 | 破坏的合取项 | 期望靶 | 结果 |
|---|---|---|---|
| R1 | 共用提交器:基准取**磁盘最新**行(退回拿内存副本) | ①dep 陈旧标签页再核销被拒 | exit=1,红 24 条 |
| R2 | 共用提交器:带 rev 做 CAS(退回纯覆盖式落盘) | ①b dep 插队被挡下 | exit=1,红 19 条 |
| R3 | 共用提交器:冲突归因(`raced` 恒 false) | ③dep conflict=true | exit=1,红 6 条 |
| R4 | 共用提交器:失败时把磁盘最新态刷进内存 | ①dep A 的内存态被刷新 | exit=1,红 2 条 |
| R5 | deposits `settleCredited`:「先落状态、过了才动钱」的顺序 | ①dep 余额只增一次 | exit=1,红 6 条 |
| R6 | deposits `settleCredited`:入账资格在磁盘最新记录上复核 | ①dep 陈旧再核销被拒 | exit=1,红 6 条 |
| R7 | deposits `settleBankIntent`:`credited` 终态门 | ①bank 只入账一次 | exit=1,红 1 条 |
| R8 | voucher `claim`:「是否已领」跑在磁盘最新账本上 | ①vou 同一张券只领一次 | exit=1,红 6 条 |
| R9 | nex-faucet `claimMilestone`:里程碑资格跑在磁盘最新状态上 | ①fau Day-7 只记一次 | exit=1,红 3 条 |
| R10 | daily-powerup `claim`:已领判定跑在磁盘最新状态上 | ①pow 只激活一次 | exit=1,红 3 条 |
| R11 | lucky-spin `spin`:今日免费票存量跑在磁盘最新状态上 | ①spin 票只花一次 | exit=1,红 2 条 |
| R12 | withdraw `claimWithdrawSlot`:`expectedRev` 取基准那一刻的版本(改成写前现读 = 退回覆盖式) | ①b wd count=2 | exit=1,红 2 条 |
| R13 | 页面接线:`daily.vue` 必须先看 `claimMilestone` 的 `ok` 才发奖 | ⑥ daily.vue 读到了 ok | exit=1,红 1 条 |
| R14 | voucher:`watch` 旁路保持删除(复活 = 绕过 CAS 的第二条落盘路) | ⑥ watch 旁路已删除 | exit=1,红 1 条 |
| R15 | 持久化 ref 只在 `sync` + `bindAccount` 被赋值(绕过 commit 直接改内存) | ⑥ 持久化 ref 赋值处数 | exit=1,红 3 条 |

R14 第一次注入只加了 `watch(...)` 调用没加 import,模块直接崩(exit=1 但 0 条具名 FAIL)——
**判红靠的是「炸了」而不是「哨兵抓到了」,证不出哨兵有效**,故改成 import + 调用一起注入、
store 仍能正常载入,这才真正证明了那条结构哨兵抓得住旁路复活。

还原后 8 个源文件均 sha256 byte-identical,复跑 108 pass / 0 fail。

## 全量门

`npm run type-check` **0 错**;`bash scripts/verify.sh` **404 pass / 0 fail**
(dev server 身份已核:`http://localhost:5173/src/store/voucher.ts` 取回的是本工程改动后的源码,
不是并发 worktree 的 —— 见 `worktree-dev-server-port-collision` 那条坑)。

## 后续:剩下的 P2 / P3 档

一期表里的 P2(`event-quest` / `quest` / `weekly-quest` / `achievements` / `orders` /
`exchange-v3`)与 P3(偏好展示类)本轮**未接**,避免继续扩大爆炸半径。接的时候直接复用
`createAccountRowCommit`,每批各自带固定靶 + 红测;接完记得同步
`selfcheck-staking-cas.mjs` ④ 的登记名单,否则那道门会红。
