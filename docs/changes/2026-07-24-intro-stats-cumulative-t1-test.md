# Onboarding Intro 页 stats 胶囊「累计口径」改动 — T1 独立黑盒验收报告

- 日期:2026-07-24
- 验收人:独立黑盒 tester agent(未读 intro.vue 实现,结论全部来自运行时观察)
- 被测:`http://localhost:5173/?nx_device=off#/pages/onboarding/intro`(dev server 5173,未重启)
- 工具:Playwright MCP(headless Chromium);语言切换 = 注入 `nexgrid-locale-v1`(uni 包装格式 `{type:'object',data:{code,userSet:true}}`)+ `location.reload()`

## 总判定:PASS(7/7 全过)

| AC | 结果 | 证据(运行时观察) |
|----|------|------|
| 1 文案(zh) | **PASS** | innerText 含「台设备在线」「累计已发放」;全页 `今日已付` 命中 0。实测全文:`28,432 / 台设备在线 / $127,438,965 / 累计已发放` |
| 2 数值口径 | **PASS** | 金额 $127,438,965(zh 首测)/ $127,439,024(en)/ $127,439,017(vi)——均为 $1.27 亿量级且带非整零头,非百万级日流水 |
| 3 跳动校准 | **PASS** | 连续采样 26 次 / 25.1 秒(zh 态)。金额从 127,439,051 → 127,439,260,共 14 步,步长序列 `18,16,9,21,19,15,9,17,10,15,14,16,15,15` — 全部落在 $9~21;节奏 ≈ 每 1.8s 一步。设备数同窗序列在 28,431↔28,433 间双向波动:观察到 **-1**(28,432→28,431 @ 第 8s)与多次 +1/-1 交替,非单调猛涨 → 按 AC 双向性直接 pass |
| 4 英文态 | **PASS** | 注入 en 后 innerText 含 `devices online`、`total paid out`;`/paid today/i` 命中 0 |
| 5 console | **PASS** | 整段会话(zh/en/vi 三态加载 + 25s 停留)console **error=0,warning=0**(仅 2 条 info) |
| 6 窄屏 375×812 | **PASS** | zh 态截图:胶囊为**单行**一体式(`● 28,432 台设备在线  $127,439,492 累计已发放`),居中于 CTA 上方。DOM 实测:`document.scrollWidth=375`(无横向溢出);胶囊容器 x=27.5, right=347.5(视口内);两段 label 同行(y=546)且包围盒不相交(不重叠) |
| 7 越南语 | **PASS** | vi 态 innerText 含 `thiết bị trực tuyến` 与 `tổng đã chi trả`;375px 下胶囊内**换行为两行**(两段 y=525/546),容器 right=351 ≤ 375,`scrollWidth=375` 无溢出。截图确认两行居中呈现干净 |

## 关键观察数值

- 设备数各次刷新落点:28,434 / 28,432 / 28,433 / 28,430 / 28,427 — 全部 28,4xx。
- 金额跨刷新会重定基(如 127,439,024 → 刷新后 127,438,965),会话内只涨不跌;AC3 管辖的是会话内行为,跨刷新重定基不违反本次 AC。
- 采样明细(zh,26 点):金额去重后 15 个值,相邻差均在 [9,21];设备数序列 `28432×8, 28431×2, 28432×5, 28433×4, 28432×2, 28433×2, 28432×3`。

## 环境假阴排除记录(先证伪环境再判 fail)

1. **同 URL goto 不触发真刷新(hash 路由)**:首次注入 zh 后用 `browser_navigate` 回同一 URL,页面仍是英文——注入值实测还在 localStorage 且格式与 app 自写 key 逐字符一致,判定为 same-document 导航未重跑 app 启动;改用 `location.reload()` 后 zh 立即生效。若当时直接判 AC1 fail 即为假阴。
2. **uni 存储包装格式**:全程按 `{type:'object',data:X}` 注入(与 `nexgrid-device-id-v1` 等 app 自写值格式一致),未踩裸值坑。

## 范围外观察(不计入本次判定)

- 条款行文字与链接之间缺空格:vi 态 DOM 实测 `"…đồng ý với"` 文本节点与 `"Điều khoản"` 链接包围盒 gap = **0px**(en 态 innerText 同样呈 `theTerms` 粘连;zh 无空格需求不受影响)。疑似存量问题,与本次 stats 改动无关,建议另行处理。
- zh 态 375px 下主标题「NexGrid,让算力流动。」折行出现孤字尾行「动。」(截图观察)。属标题排版,非 stats 胶囊,与本次 AC 无关,提请留意项目「禁孤字断行」规则。

## 清理

- headless Chromium 已关闭,孤儿进程已清;临时截图(intro-zh-375.png / intro-vi-375.png)与 `.playwright-mcp/` 快照已删除,证据以本报告文字+数值保留。

## T1b 追验(hero 区 `text-wrap: pretty` 防孤字,2026-07-24 同日 HMR 后)

| 项 | 结果 | 证据(运行时观察,375×812 · zh 态) |
|----|------|------|
| 1 主标题末行防孤字 | **PASS** | 逐字符 Range 实测折行:`NexGrid,让算力` / `流动。` — 末行「流动。」= 2 个汉字 + 句号(≥2 字)。改前为孤字「动。」。computed `text-wrap: pretty` 已生效,`CSS.supports('text-wrap','pretty')` = true |
| 2 stats 胶囊无回归 | **PASS** | 胶囊仍单行(两段 label 同行),`docScrollWidth=375` 无溢出,右缘 333.5 ≤ 375;金额 8s 采样 127,439,113 → 134 → 145 → 175 → 194,仍在缓涨且步长与 T1 口径一致 |
| 3 console | **PASS** | 本轮加载 + 停留 error=0,warning=0 |

**T1b 判定:PASS。** 主标题末行实际内容:「流动。」

## T1c 追验(累计金额改「时间锚派生」,2026-07-24 同日 HMR 后)

| 项 | 结果 | 证据(运行时观察,375×812 · zh 态) |
|----|------|------|
| 1 单调不倒退 | **PASS** | A=127,571,572(打开)→ B=127,571,714(停留 18.1s)→ C=127,571,987(reload)→ D=127,572,162(再 reload)。A<B<C<D 严格递增,跨刷新零倒退(对照 T1 老行为:跨刷新曾 -59) |
| 2 新步长恒定 | **PASS** | 18.1s 窗内 10 步,步长序列 `14,15,14,14,14,14,15,14,14,14` — 恒定 $14~15($14.216/tick 的整数进位),全部落在 $14±1,不再是 $9~21 随机;节奏 ≈1.81s/步。日速率外推:窗内 $677,097/日,A→D 全程(74.6s)$683,747/日,与声称 $682,368/日 偏差 0.2% ≈ $0.68M ✓ |
| 3 设备数+console+胶囊 | **PASS** | 设备数窗内 28,430↔28,432 双向波动(+1 与 -1 均观察到),reload 落点 28,434 / 28,432,全为 28,4xx;console error=0,warning=0(含两次 reload);胶囊仍单行、scrollWidth=375 无溢出(B 点与 D 点两次实测) |

**T1c 判定:PASS。**
备注:T1b→T1c 基线从 127,439,xxx 跳至 127,571,xxx(+13.2 万),为公式从「随机重定基」切到「anchor+速率派生」的一次性基线迁移,属预期改动产物,非运行时倒退;切换后的可观察行为(单调/恒速/双向抖动)全部符合新口径。
