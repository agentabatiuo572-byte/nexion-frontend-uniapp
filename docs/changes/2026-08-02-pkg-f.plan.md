# 实施拆解 · FEAT-TRIAL02 无卡领取试用与到期转化(包 F)

状态:规格 **Signed(主人 2026-07-31)**,本拆解就绪待开工。规格:`PRD/specs/FEAT-TRIAL02-cardless-trial.md`
规格血缘:每个子任务 AC 逐条继承规格 ② GWT + ④ 状态机 + ⑥ 点击流,不另造口径。
工程:`Nexion-uniapp`(dev :5173)单仓。

## 现状结论(拆解依据)

- 现行 `src/store/free-trial.ts` 是「绑卡→到期自动扣款」SaaS 模型:`idle→active→grace→extended→{redeemed|failed|cancelled}`,带 `cardTokenId`/`scheduledChargeAt`/`mockChargeAttempt`/高质量延期(extension)三件套。规格状态机 `none→active→grace→ended` + `converted` 与之结构性不同 → 重铸而非打补丁。
- 领取链路耦合三处(与规格 ⑦ 对齐):① `trial-claim-sheet.vue` onClaim → navTo `wallet-cards-new?trial=1` 绑卡;② `trial.vue` onLoad `?trial=1` 自动 `startWithCard`;③ `wallet-cards.vue` 解绑拦截 + `trial-unbind-retention-sheet` 挽留。
- 转化路径现有两条:trial.vue 页内 `handleRedeem` 直购编排(debit+账单+addDevice 直插)、App.vue `handleAutoRedeem` 自动扣款。规格 ⑥ 要求转化走**购机结算页**(checkout),自动扣款废止。
- checkout.vue 注释明言「trial-offset lives in the trial redeem flow」且 voucher 有 `stackWithTrial` 配置(`src/mock/vouchers.ts:51`)——接入抵扣后该开关必须真实生效。
- 消费面 31 文件分三类:**旧枚举字面量类**(App.vue / trial.vue / me.vue:237 / devices.vue:180 / wallet-cards.vue:99 / trial-ghost-slot:130 / trial-promo-banner:44 / trial-hero-banner:114 / trial-countdown-hero)、**trialReservesSlotNow 类**(checkout/order-detail/download/tradein-sheets/earn 三件/use-device-eligibility/refresh——helper 语义不变则零改动)、**claim-sheet 触发类**(hero/promo/entry/chassis——仅链路终点变)。
- 首日任务绑卡文案(`dayOneTaskBindCard`/`t_bind_bank_card`)三语均已是中性「绑定银行卡」,试用耦合只在 `?trial=1` 披露块;任务本体保留。
- i18n 为 **en/zh/vi 三语**镜像;`trialExtension` 整命名空间、`cards.trialDisclose*`、trial 命名空间的 boundCard*/autoDebit/urgency 自动扣款系文案随改版删换。
- 既有机器门在读试用面:`check-device-yield-parity.mjs`(读 trial-config `shadowDaily*`/`trialProductId`,**保留字段勿动**)、verify SPEC-2 槽位哨兵(trialReservesSlotNow 模式)、P2-8 account-scope、`trial-check.mjs`(claim sheet auto-push 走查)、verify:1288 trial.vue debit-guard(F5 迁移后需同步)。

## 执行纪律

- 🔴 逐子任务闭环:实现 → 增量机器门(type-check + verify)→ **独立 tester agent 黑盒验收** → pass 才打勾 → 回源三问。实现方 ≠ 验收方;tester 只拿「AC + 运行方式 + 脆性清单」,不给实现叙述。
- 🔴 熔断:同一子任务连续 2 轮 fail → 停下反思报主人,不硬刷。
- 🔴 每条新不变量必配哨兵 + 红测(按合取项逐个隔离红测;还原用 cp 备份,禁 git checkout)。
- 脆性清单(tester 必读):dev :5173(vite 绑 IPv6,先 curl 探)· H5 验证加 `?nx_device=off` · uni storage 注入必须 `{type:'object',data:X}` 包装 · 中断必杀 headless chromium · 行尾 CRLF,grep 用 `\r?`。
- 墨菲前置(动手前当它一定崩去验):① 存量 persisted 行旧枚举渗入新机器(僵死/白屏);② checkout 接入改坏普通购买(voucher/trade-in/phase 门回归);③ tsc 抓不到的 string-widened 残留(wallet-cards `as readonly string[]` cast、devices.vue `string[].includes`)→ 只有 grep 门拦得住;④ 三语漏一语;⑤ grace 语义已变「停摆」但 ghost-slot/promo 文案仍说在产出;⑥ `trial-check.mjs`/chrome-baseline 走查断言被改坏。

依赖顺序:F1 → F2 → F3 → F4 → F5 → F6 → F7 → F8(F6 可在 F1 后任意时点并行;F3/F4 都动 trial.vue,不并行)。

---

## F1 [机制] 试用状态机重铸(store 层)

**范围文件**:`src/store/free-trial.ts` · `src/store/trial-config.ts`

**做什么**:按规格 ③④ 重铸状态机为 `none|active|grace|ended|converted`。
- 删字段:`cardTokenId`、`scheduledChargeAt`、`extendedEndsAt`、`extensionGranted`、`failReason`;保 `startedAt/activeEndsAt(→expiresAt 语义)/graceEndsAt/finishedAt/shadowFrozenAt*`。
- 删动作:`startWithCard`、`redeemEarly`、`acceptExtension`、`declineExtension`、`markChargeFailed`、`mockChargeAttempt`、`isHighQualityEligible`。
- 新动作:`start()`(无入参;非 eligible 即 no-op 幂等,规格 异常3)、`convert()`(仅 `active|grace` → `converted` 终态,禁回退)、`eligibility(): {ok, reason?}`(reason 枚举:已购机/冷却中/未开放/进行中,规格 异常2 供 UI 给具体原因)。`cancel("explicit")` 保留(状态机 `active→ended`;"unbind" 理由删除)。
- `poll`:`active→grace`(冻结影子,既有逻辑)、`grace→ended`(**只改状态,零扣款零下单**,规格 ④ 禁止动作)。`ended` 后影子不再累积(liveShadow* 对 ended 返 0)。
- 资格:`converted`(已购机)**永久不可再领**;`ended` 沿用既有 cooldownDays 冷却;`phaseOpen` 沿用。
- hydrate 迁移(规格 异常6,存量键 `nexgrid-trial-accounts-v1` 不换):`idle→none`、`extended→grace`(graceEndsAt 取 max(graceEndsAt, extendedEndsAt))、`redeemed→converted`、`failed|cancelled→ended`(保 finishedAt 使冷却钟有效);legacy 行 `cardTokenId≠null` 且仍 active/grace → 置 `legacyCardMigrated: true`(F4 页面明示规则已变)。
- `SLOT_RESERVING_STATUSES = ["active","grace"]`(仅去 extended,槽位语义不变)。
- trial-config:删 `autoChargeAtEnd`/`chargeFailRate`/`extensionDays`/`highQualityThresholdUSD`;**保** `shadowDaily*`/`trialProductId`/`trialPriceUSD`/`trialOffsetCapUSD`/`discountRate|CapUSD`/`cooldownDays`/`phaseOpen`/autoPush*(device-yield-parity 门在读,试用参数组单源,规格 ⑦ 不新增数值)。

**AC**(继承规格 ③④ + 异常3/6)
- [ ] 新枚举五值,`converted` 终态禁回退;`grace→ended` 转换全程无任何 debit/账单/设备写入
- [ ] `start()` 连调两次仅产生一份试用(第二次 no-op,状态与时间戳不变)
- [ ] 五种 legacy status 各构造一条持久行,hydrate 后映射正确、冷却钟不丢、无 crash
- [ ] `eligibility()` 对 已购机/冷却/未开放 各返回具体 reason
- [ ] 旧枚举字面量全站清零:`grep -rn "\"idle\"\|\"redeemed\"\|\"extended\"" src` 中试用上下文命中 = 0(逐文件核 me.vue:237 / devices.vue:180 / wallet-cards.vue:99 / ghost-slot:130 / promo-banner:44 / hero-banner:114 / countdown-hero——wallet-cards 的 `as readonly string[]` cast 与 devices 的 `string[].includes` **tsc 抓不到,必须 grep**)
- [ ] `npm run type-check` 0 错;device-yield-parity 门不红

**测试指令**:node 直调 store 单元断言(Vitest 不在此工程,tester 用 Playwright 注入 storage 构造五档 legacy 行 → 刷新走查)
**敏感度**:🔴 状态机+资金前提 → tester 外加 1 个 code-review agent
**tester 报告位**:`docs/changes/2026-08-02-pkg-f-t1-test.md`

---

## F2 [机制] App.vue 到期编排重铸

**范围文件**:`src/App.vue`(132–293 试用段)

**做什么**:删 `handleAutoRedeem` 整函数、extension 触发块(175–189)、`markChargeFailed` 分支;poll 分支改新枚举:`→grace` 提示「已停止产出,抵扣金可用至 {时刻}」;`→ended` 提示「抵扣金已失效」。urgency 双推保留改文案:active 剩 24h(既有);grace 失效前 24h/1h 改为「抵扣金 X 后失效」——**无任何「自动扣款/auto-purchase」语义**(规格 ④ 始终给明确时间与下一步)。

**AC**(继承规格 ② 异常1 + ④)
- [ ] 把 graceEndsAt 拨到过去 → poll 后状态 ended、余额/账单/设备数全部**不变**(零自动扣款零自动下单的运行时证明)
- [ ] grace 内 urgency 推送出现且文案为抵扣金失效倒计时;ended 后不再推
- [ ] `grep -n "handleAutoRedeem\|markChargeFailed\|TrialExtensionSheet" src/App.vue` = 0
- [ ] SPEC-2 哨兵 `tickOrders(trialReservesSlotNow() ? 1 : 0)` 不被误删(verify 绿)

**测试指令**:5173 → Playwright 注入 active 试用行,拨时间过 expiresAt/graceEndsAt 各一档,断言余额与状态
**敏感度**:🔴 钱(负向证明)→ 加 code-review agent
**tester 报告位**:`...-t2-test.md`

---

## F3 [机制] 领取链路去绑卡

**范围文件**:`src/components/trial-claim-sheet.vue` · `src/pages/me/trial.vue`(仅 onLoad 段)· `src/pages/me/wallet-cards-new.vue`

**做什么**:claim sheet `onClaim` 改为:eligibility 不通过 → toast 具体原因;通过 → 按钮 loading 防连点 → `freeTrial.start()` → 成功 toast + 关半屏留在当前页;失败 → **半屏内 inline 错误 + 重试**(不静默,规格 ⑤)。删 trial.vue onLoad `?trial=1` 自动启动段;删 wallet-cards-new `?trial=1` 披露块 + `isTrialBinding` 全部逻辑与样式(绑卡页回归纯商城支付用途)。

**AC**(继承规格 ② 阳光路径-领取 + 异常2/3 + ⑥)
- [ ] 领取全程零跳转绑卡页,确认后试用直接运行:倒计时开始、首页/设备页出现试用机位
- [ ] 领取链路(入口→半屏→成功态)运行时文案无「银行卡/卡/支付方式/card」字样
- [ ] 不可领时按钮置灰 + 具体原因(已购机→「去看看升级」句式;冷却→剩余时间),禁泛化报错
- [ ] 半屏连点 5 次仅一份试用、一份影子累积
- [ ] `grep -rn "trial=1" src` = 0;`grep -n "isTrialBinding\|trialDisclose" src/pages/me/wallet-cards-new.vue` = 0
- [ ] `trial-check.mjs`(auto-push 走查)仍绿

**测试指令**:5173 清 storage → Home 等 auto-push → 领取全流程真点;再构造 converted/冷却行各验置灰原因
**敏感度**:中
**tester 报告位**:`...-t3-test.md`

---

## F4 [机制] 试用页多态改版(停摆 / 终态 / CTA 重指向)

**范围文件**:`src/pages/me/trial.vue` · `src/components/me/trial-countdown-hero.vue` · `src/components/trial-ghost-slot.vue` · `src/components/trial-promo-banner.vue`

**做什么**:按规格 ⑤ 五态重排:
- active 默认态:删 boundCard 行整块;保 倒计时+影子日产+已累积抵扣金;主 CTA「立即购买(可抵 $X)」→ `navTo /pages/store/checkout?product=stellarbox-s1&trial=1`;**删页内 `handleRedeem` 直购编排**(迁 F5)。
- grace 停摆态:设备灰显 +「已停止产出」+ 抵扣金数额 + 失效时刻 +「用抵扣金购买」CTA(同上跳 checkout trial 模式)。
- ended 终态:失效时间与原因明示 + 普通「去购买」(无抵扣,不带 trial=1)。
- converted 终态:已购机 → 去设备页链接。
- countdown-hero 删 extended 视觉分支(ribbon/CTA/totalMs 里的 extensionDays);grace 改停摆视觉(灰显)。
- ghost-slot / promo-banner:grace 下文案对齐「已停止产出」,不再宣称在产出。
- 新增「查看抵扣规则」入口 → 规则说明半屏(购前抵扣/不可提现/购后余额/失效时间,规格 ⑥)。
- `legacyCardMigrated` 为真时页面明示「规则已变更:到期不再自动扣款」一行(规格 异常6)。
- 加载态:试用数据即 store 同步读,骨架仅 <300ms 场景按规格免展示——不做假骨架。

**AC**(继承规格 ② 阳光路径-到期转化 + 异常1/6 + ⑤⑥)
- [ ] 五态(未领取/active/grace/ended/converted)Playwright 各注入一档,截图齐、console error=0
- [ ] active/grace 主 CTA 真点后落地 checkout 且携 trial 上下文;ended 的「去购买」落地 checkout 无抵扣行
- [ ] grace 页面同屏可见:灰显设备、已停止产出、抵扣金额、失效时刻(缺一 FAIL)
- [ ] `grep -n "handleRedeem\|redeemEarly\|boundCard" src/pages/me/trial.vue` = 0
- [ ] 规则半屏可开可关,内容含不可提现与失效时间

**测试指令**:5173 → 注入五档状态行分别走查 `/pages/me/trial`
**敏感度**:🔴 转化主链 UI → 走查加困惑点分级
**tester 报告位**:`...-t4-test.md`

---

## F5 [机制] checkout 抵扣金结算接入 + 转化闭环

**范围文件**:`src/pages/store/checkout.vue`(为主)· `src/store/free-trial.ts`(仅消费 F1 的 convert)

**做什么**:checkout 增加 trial 转化模式(条件:`?trial=1` **且** freeTrial.status ∈ {active,grace} **且** productId === trialProductId,条件不满足静默回落普通模式):
- 金额行:促销折扣行(既有 `computeDiscountedPrice`)+「试用抵扣金」行(既有 `computeTrialOffset`,单源勿双写);应付 = 价 − 折扣 − 抵扣,下限 0。
- 异常4:抵扣 ≥ 应付 → 应付显示 $0 但**仍走显式确认下单**,页面明示不退差额。
- 支付成功(订单落盘同笔):`freeTrial.convert()` + remainder 入余额 + NEX 入账 + 对应账单条目(对齐既有 memo 风格);设备由**既有订单流**生成(不再 `app.addDevice` 直插,试用机即正式购机)。
- voucher 叠加:`stackWithTrial=false` 的券在 trial 模式下不出现/不可用(`src/mock/vouchers.ts` 既有配置真实生效);checkout 顶部旧注释「trial-offset lives in the trial redeem flow」同步改写。
- trade-in 与试用理论不相交(试用用户无旧机),不做特殊处理,现状门保持。
- verify 同步:1288 行 debit-guard 哨兵若 pin 了 trial.vue 的 debit 模式,随迁移改指 checkout;SPEC-2 哨兵不动。

**AC**(继承规格 ② 阳光路径-到期转化 + 异常4 + ⑥⑦)
- [ ] active 试用(影子 $21)进 checkout:subtotal 649 − 促销 − 21 抵扣,行行可见,合计与手算一致(误差 <0.01)
- [ ] 抵扣 ≥ 应付构造档:应付 $0、确认步仍在、明示不退差额;确认后订单生成
- [ ] 支付成功后:trial=converted、remainder/NEX 入账、账单齐、设备经订单流出现、试用页转 converted 态;**再次进 checkout?trial=1 不再出抵扣行**(convert 终态防重复抵扣)
- [ ] `stackWithTrial=false` 券在 trial 单不可用;`=true` 券可叠加且金额行正确
- [ ] 普通购买(无 trial=1)全流程回归:voucher/trade-in/phase 门/卡支付四态与改前一致
- [ ] ended 后进入 = 无抵扣行普通结算

**测试指令**:5173 → 从试用页 CTA 真点进 checkout 走完支付;另跑一单普通 S1 购买对照
**敏感度**:🔴🔴 钱+订单状态机 → tester 外加 code-review + 对抗 skeptic 各 1
**tester 报告位**:`...-t5-test.md`

---

## F6 [机械] 解绑挽留全删(卡 ↔ 试用彻底解耦)

**范围文件**:`src/pages/me/wallet-cards.vue` · `src/components/trial-unbind-retention-sheet.vue`(删)· `src/store/trial-unbind-retention-sheet.ts`(删)· `src/components/app-chassis.vue`(卸挂载点)

**做什么**:删除挽留半屏组件与 store(**先 Move 到 `.trash/<时间戳>/`**);wallet-cards 删试用拦截分支(99–113),所有卡一律走既有纯 destructive confirm 分支;chassis 卸载该 sheet。

**AC**(继承规格 ② 异常5)
- [ ] 试用 active 时解绑任意卡:无挽留弹层、试用状态不变、卡正常删除
- [ ] `grep -rn "trial-unbind\|TrialUnbindSheet\|TRIAL_HOLDING_STATUSES" src` = 0
- [ ] 非试用卡删除路径回归不变(confirm → remove → toast)

**测试指令**:5173 → 注入 active 试用 + 两张卡 → `/pages/me/wallet-cards` 真点解绑
**敏感度**:低(删代码,保留分支已存在)
**tester 报告位**:`...-t6-test.md`

---

## F7 [机械] i18n 三语文案收口

**范围文件**:`src/i18n/messages/en.ts` · `zh.ts` · `vi.ts`

**做什么**:三语同序镜像地——
- 删:`trialExtension` 整命名空间;`cards.trialDisclose*` 五键;trial 命名空间自动扣款系键(`boundCardFooter/boundCardManage/toastAutoDebitFailed/urgency1h(旧义)/terminalFailedTitle|Desc/countdownExtendedRibbon|Cta` 等,以 F2–F5 实际消费为准清孤儿键)。
- 增:停摆态/失效说明/规则半屏/checkout 抵扣行/领取半屏改版所需新键(F3–F5 落地时已加,本任务做三语齐与顺序核)。
- 改:绑卡入口价值表述面向商城支付(`cards.newTitle` 副文案、首日任务如有 desc);`terminalCancelledDesc` 等冷却文案去「绑卡再申请」句式。
- 红线:禁「模拟/演示/不会真扣款」;禁「绑卡后开始试用/自动扣款/Auto-charge/Auto-purchase」残留(试用域)。

**AC**(继承规格 ⑦)
- [ ] `node scripts/i18n-key-mirror.mjs` 绿(三语 key 树同序)
- [ ] 切 en/zh/vi 各走一遍试用五态 + 领取半屏 + checkout 抵扣行,无 key 裸奔、无英文硬编码
- [ ] 旧文案指纹 = 0:`grep -rn "Auto-charge\|Auto-purchase\|自动扣款\|绑卡后开始试用" src/i18n` = 0(vi 对应词同扫)

**测试指令**:key-mirror 脚本 + 三语实景截图
**敏感度**:低
**tester 报告位**:`...-t7-test.md`

---

## F8 [机械] 哨兵 + 红测收口

**范围文件**:`scripts/verify.sh`(新增 FEAT-TRIAL02 哨兵段)

**做什么**:焊「去绑卡不回潮」机器门,并证明门真的会拦:
- 哨兵 A(源码指纹清零):`startWithCard|cardTokenId|autoChargeAtEnd|chargeFailRate|scheduledChargeAt|trialDisclose|trialExtension|trial=1` 在 `src/` 命中 = 0。
- 哨兵 B(i18n 旧文案清零):同 F7 指纹,三语文件。
- 哨兵 C(状态机不变量):free-trial.ts 存在 `convert` 且 poll 的 grace 分支不含 `debitBalance|addDevice|bills.add` 调用。
- 每个合取项**单独**红测:逐项塞回一处最小注入(取自本次真实事故形态,如塞回一行 `startWithCard`)→ verify 必 FAIL → cp 还原 → 复跑绿。PASS 行打印样本量(防空集假绿)。
- 既有门回归清单:device-yield-parity / trial-check.mjs / SPEC-2 槽位 8 条 / P2-8 / chrome-baseline 的 trial-sheet 预置(键名未变应仍有效)。

**AC**
- [ ] `bash scripts/verify.sh` 全绿且新哨兵段 PASS 行带命中数/样本量
- [ ] 红测记录逐合取项留痕(注入→FAIL 证据→还原→绿)
- [ ] 既有门清单逐项跑过,无一因本包变红

**测试指令**:`bash scripts/verify.sh`;红测按合取项循环
**敏感度**:中(门本身要防假绿)
**tester 报告位**:`...-t8-test.md`

---

## 收尾(全包完成门)

机器门(type-check + verify 全绿)→ 每子任务独立 tester 已 pass → 实景门(独立走查 agent 过五态+领取+转化全链,三语)→ `nexion-audit` 审计门 → done-review 6 维 → PRD sync 问询(`nexion-prd-sync`,免费试用章节按新状态机改写)→ `docs/前端产品更新日志.md` 追加(✏️ 试用去绑卡 + 到期停摆转化)。
