# 兑换成交快照 + 创世购买重入守卫 — 红测记录(2026-08-04)

存量 2×P1,同一个根:**动钱的入口没有重入守卫、跨 await 读活值**。

| # | 缺陷 | 位置 |
|---|---|---|
| P1-① | 确认后仍读活值 + 无重入守卫 | `src/pages/me/wallet-exchange.vue` `handleConfirm` |
| P1-② | 购买半屏无重入守卫、提交按钮无 disabled 态 | `src/components/genesis/purchase-sheet.vue` `handlePurchase` |

机器门:`scripts/selfcheck-exchange-genesis-guard.mjs`(43 断言),已焊进 `scripts/verify.sh`
(`exchange_genesis_guard_gate`,排在 result 汇总行**之前** —— 排在之后的话失败不计入 fail 计数、
脚本还会以最后一条命令的退出码返回 0,等于假绿)。

判据方法:结构断言跑在**剥注释后的正主源码**上;行为断言把 `handleConfirm` / `handlePurchase`
**原文抠出来注入执行**(`grabBlock` + esbuild transform + `new Function`),不抄判据副本。
抠不到代码块(函数被改名/删除)时直接抛错终止 —— 见 R7。

---

## 一、机器门红测(逐合取项隔离)

驱动:`<scratchpad>/fix-L1/redtest.mjs`。每例:注入 → 跑门 → 记红榜 → `cp` 备份还原 → 校验
sha256 byte-identical → 复跑必须回到 43 pass / 0 fail。**还原一律用 `cp`,禁 `git checkout`**
(本仓有并发 agent,`git checkout` 抹掉的是整轮未提交工作)。注入未生效(命中数不符 / sha 未变)
立即抛 `INJECTION-NOT-APPLIED` 终止,不许静默算过。

| # | 合取项 | 注入形态 | 结果 | 判定 |
|---|---|---|---|---|
| R1 | ① 确认后汇率复验 | 复验条件永远为假(结构还在、语义死掉)—— 等同「拿快照汇率复验快照报价」那类恒真判据 | 红 2 条,全部落在本合取项(`C① 确认后汇率 0.085→0.09 → 零资金动…` `C① 拒单走「报价已过期」并说明未扣款…`);其余 41 条仍绿 | ✅ 断言载荷成立 |
| R2 | ② 按快照成交 | 扣款入参改回活读 `fromAmount.value`(原缺陷形态) | 红 6 条,全部落在本合取项(`A② 首个 await 之后 0 处裸读活值…` `A② 全部 5 个动钱/计数入口的入参都是快照…` `C② 按快照成交…` `C② 方向未被篡改…` `C② 日限按被闸校验过的那个数计入 8.5…` `C② swap 记录与账单三处金额都是快照…`);其余 37 条仍绿 | ✅ 断言载荷成立 |
| R3 | ③ 兑换重入守卫 | 删掉 `handleConfirm` 的重入守卫(原缺陷形态:全文件无守卫) | 红 4 条,全部落在本合取项(`A① 重入守卫排在最前…` `C③ 连点 3 次只成交 1 次…` `C③ 余额只被扣一次…` `C③ 日限计数只累加一次…`);其余 39 条仍绿 | ✅ 断言载荷成立 |
| R4 | ④ 创世重入守卫 | 删掉 `handlePurchase` 的重入守卫(原缺陷形态:全程同步无守卫) | 红 4 条,全部落在本合取项(`B④ 重入守卫排在最前…` `C④ 双击/三击只买 1 台…` `C④ 只扣一台的钱…` `C④ 成功 toast 只弹 1 次…`);其余 39 条仍绿 | ✅ 断言载荷成立 |
| R5 | ④b 失败路径必解锁(防永久锁死) | `finally` 里不再复位 —— 正是 `checkout.vue` 模块级 `let` 那条踩坑路径的后果 | 红 4 条,全部落在本合取项(`B④ 复位在 finally,且只有真成交才继续持锁…` `C④ 余额不足 → …守卫已解锁…` `C④ 铸造失败 → 已扣款原路退回 + 守卫解锁…` `C④ 失败后重试真的能成…`);其余 39 条仍绿 | ✅ 断言载荷成立 |
| R6 | ⑤ 提交按钮 disabled 派生 | 按钮填充写死 brand(在途也一直亮着,视觉上仍邀请再点一次) | 红 1 条(`B⑤ 提交按钮绑 disabled 态…`);其余 42 条仍绿 | ✅ 断言载荷成立 |
| R7 | 🔴 判据自身 | 把正主函数改名 —— 抠不到代码块时哨兵必须炸 | exit=1,报「源码里找不到 `async function handleConfirm()`」 | ✅ 判据自身可被红测,不会「没找到 = 全过」 |

**7/7 成立;每例还原后 sha256 与备份一致,复跑 43 pass / 0 fail。**

### 🔴 R2 第一轮把我自己的哨兵测红了 —— 判据被就地改强

首轮 R2 注入后,`A② 首个 await 之后 0 处裸读活值` **没红**。原因是我把豁免写成了「这一行里
只要出现 `snap.` 就整行放过」,而注入后的那行是

```js
const succ = snap.direction === "usdt2nex" ? app.debitBalance(snap.fromAmount) : app.debitNex(fromAmount.value);
```

—— 同一行里既有快照又有活值,靠 `snap.direction` 就把活值一起赦免了。**混着写的那一行恰恰是
最危险的形态**,却正好从这道门底下走过去。改法:把「确认后复验」两个**准许的表达式**先从行里
抠掉,剩下任何活值一律违例;顺手把 `rate.value` 也纳入扫描口径(它只允许出现在复验那一处)。
改完 R2 稳定红 6 条。

## 二、运行时红测(接线门:门判得对 ≠ 门真接上了)

驱动:`<scratchpad>/fix-L1/rt-redtest.mjs` / `rt-a.mjs`,真浏览器(`localhost:5173`,`?nx_device=off`)。

| # | 摘掉的东西 | 真浏览器里发生了什么 | 判定 |
|---|---|---|---|
| RT-A | 兑换页重入守卫 | ② 靶转红;现场「叠出的确认弹窗数 **1**(应 0)」—— 第二条完整兑换链已排队并弹出第二个确认框,其 `.nx-mask` 随后拦截了全站点击(脚本在后续步骤被这层遮罩卡死) | ✅ 运行时靶载荷成立 |
| RT-B | 创世半屏重入守卫 | ③ 靶转红;真金白银扣了 **$23,998**(两台)而不是 $11,999 | ✅ 运行时靶载荷成立 |

还原后复跑:① ② ③ 全绿,console error 0,两文件 byte-identical。

**RT-A 附带结论**:摘掉守卫后 CTA 的 `aria-disabled` 仍然是 `true`,点击照样穿透执行 ——
**aria/置灰只是告知,不是闸**;真正拦住第二次的是那句 `if (submitting.value) return;`。
两者都要有,但不能拿视觉当守卫。

## 二之二、并发改动后的判据跟进(postMoneyBill 收口)

创世购买链随后被收口到 `postMoneyBill`(扣款 ⊗ 记账原子提交,`src/lib/money-receipt.ts`,
非本轮作者)。本门的 B/C④ 段随之更新 —— 重入守卫本身一字未动,只改判据与固定靶:

- `B④ 上锁点在第一次动钱之前`:原写死 `app.debitBalance(cost)`,收口后 `indexOf` 返回 −1。
  这次恰好判假、红了,但「找不到就当没有」正是哨兵假绿的经典形态。改为扫**一组**资金原语
  取最早命中,并且**一个都扫不到就判失败**(空集不放行)。
- `C④` fixture:`makeApp` 补 `captureMoney` / `restoreMoney` 与 `withdrawableUsdt` 维度;
  `postMoneyBill` **跑真实现**(从 money-receipt.ts 原文抠出注入),不写替身 —— 写了替身,
  「只扣一次」就变成在跟自己写的假扣款对账。
- 铸造失败靶按新语义改写:从「0 行账单」改为「1 扣 + 1 反向冲正,净和 0,且余额与
  **可提额度**都还原」;新增「收据落盘失败 → 资金精确还原 · 零铸造 · 零账单 · 守卫解锁」。

| # | 靶 | 注入形态 | 结果 | 判定 |
|---|---|---|---|---|
| RB1 | B④ 上锁点判据仍有牙 | 删掉 `purchasing.value = true` | 红 5 条,全部同一合取项(守卫)：B④ + 4 条 C④ 双击行为 | ✅ |
| RB2 | B④ 空集不放行 | `captureMoney` / `postMoneyBill` 同时改名 → 命中 0 | 红 1 条(`命中 0 个`)且脚本抛错终止,未静默放行 | ✅ |
| RC1 | C④ 收据落盘失败靶 | 收据写失败后不还原资金 | 红 1 条 | ✅ |
| RC2 | 桩里的 `withdrawableUsdt` clamp 到底能不能分辨对错 | 冲正入参 `{ restoreTo: before }` → `{}`(退化成裸 `creditBalance`) | 红 1 条,现场 `usdt=50000 withdrawable=40001` —— **总余额看着完全还原了,只有可提额度被悄悄压低** | ✅ |

4/4 成立;每例 `cp` 还原后 sha256 byte-identical,复跑 59 pass / 0 fail。

🔴 RC2 是这轮最值得记的一条:**桩不建 clamp,裸 `creditBalance` 与 `restoreTo` 在门下长得一模一样**
(总余额都回到 50000),门看起来验了、其实分辨不出对错。`makeApp` 的 `debitBalance` 必须照
`app.ts` 同款把 `withdrawableUsdt` clamp 到扣款后余额,这条断言才有牙。

## 二之三、收口到 postMoneyBills 后的判据跟进 + 两腿原子性

兑换成交链随后也被收口到 `postMoneyBills`(N 条分录**一次** `bills.addMany` 落盘,
非本轮作者)。跟进三处,重入守卫与快照纪律本身一字未动:

- **A② 改判性质,不再点名函数**。原判据点名 `app.debitBalance(snap.fromAmount)` 等四个裸原语,
  收口后它们在页面里不再出现。改为:扫**一组**资金原语(含 `postMoneyBills`)、**一个都扫不到
  直接判失败**,再断言一进一出两腿的金额+币种与日限计数**全部取自 `snap.*`**。
- **注入作用域修复**:`postMoneyBill`(单数)现在只是 `postMoneyBills` 的壳,只注入单数函数体会
  在运行时炸 `postMoneyBills is not defined`。复用已有的 `buildPostMoneyBills` 一起注入同一闭包。
- **新增 C⑦ 两腿原子性**(此前无人守):兑换是复式一进一出,收据落不了盘时若只还原一侧,
  用户就会「NEX 少了、USDT 没多」而账单页查无此单。

| # | 靶 | 注入形态 | 结果 | 判定 |
|---|---|---|---|---|
| RD1 | A② 空集不放行 | 成交处 `postMoneyBills` 改名 → 6 个原语命中 0 | 红 1 条(`命中 0 个`),**不是静默绿** | ✅ |
| RD2 | A② 入参必须取自快照 | 出账腿改回活读 `fromAmount.value` | 红 6 条,全部同族(A② 两条 + C② 四条行为靶) | ✅ |
| RD3 | C⑦ 两腿原子性 | `addMany` 失败分支不还原资金 | 红 2 条(C⑦ + C④,共用同一个 `restoreMoney`) | ✅ |

3/3 成立;`cp` 还原后 sha256 byte-identical,复跑 64 pass / 0 fail。

🔴 **这轮的元教训(比 clamp 那条更一般)**:判据可以比它所判的形态**活得更久**。
跨 owner 只是让它提前暴露成死锁;单 owner 下它表现为**慢性静默失效** —— 判据钉死一个函数名,
重构后扫不到 → 没有违规 → 绿,更难发现。持久修法三件套:判**一组**原语 + **扫不到就判失败**
+ 断言**性质**(入参是不是快照)而非**具体调用了哪个函数**。

## 三、边界声明

本次只收口**单标签页内**的快照漂移与重入。台账另有一条独立缺陷
`src/store/exchange-v3.ts:116`(额度计数器的**跨标签页竞态**,可绕过每日 $50 / KYC $100 阈值)
**不在本次范围**,未修复。因此不得声称「兑换页并发问题已全部解决」。
