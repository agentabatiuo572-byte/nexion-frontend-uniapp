# 2026-09-05：本轮六项审计缺陷修复

范围：正式 UniApp 与 Java 后端。PC 配置及业务数据未修改，未执行购买、提现、领取奖励、内容发布或 A2 审批。

## 修复结果

1. 商城首屏与升级卡共用实际升级结果；无目标时不展示收益倍数。移除固定模型能力承诺，改为预估收益说明。
2. 商品卡和详情页的中、英、越三语库存文案改为当前剩余库存，不再声称“本周剩余”。
3. 提现未填写金额或未绑定地址时显示具体前置条件，不再错误停留于“加载中”；服务端资格检查仍不可绕过。
4. 工单详情请求返回前复核账号、运行范围及请求代际；过期响应拒绝返回。页面切换账号时清除详情模式和输入，成功/异常/已读操作前检查账号范围。
5. Java 领导池读取 PC F1 的 team.ui.F.vrank.leadership.topN，正式 App 契约校验并使用该字段。此字段仅控制集中度展示，不改变结算金额。
6. 领导池使用响应式时钟，在服务端结算时间边界重新读取。隐藏/卸载停止时钟、取消排队回调并使在途结果失效；隐藏页不因账号或运行状态变化发起读取。错误仍走显式重试，不无限轮询。

## 验证

- 已先运行新增回归测试并复现失败，再修改实现。
- 最终 App Vitest：354 文件，1,688 项通过。
- App vue-tsc 类型检查及 H5 构建通过。
- 定向覆盖率：payout-clock.ts 的语句、分支、函数、行覆盖率均 100%；它与 store-upgrade.ts 合计行覆盖率 100%、分支覆盖率 93.33%。这不是全仓覆盖率。
- 后端 6 个相关测试类共 141 项通过：AppTeamInsightsServiceTest、OpsTeamServiceTest、LeadershipPoolConfigGuardTest、LeadershipPoolProjectionTest、LeadershipPoolServiceTest、LeadershipPoolSettleSchedulerTest。
- 两路独立对抗复审通过。生命周期复审提出隐藏页读取问题，新增红→绿测试后修复并复审。
- 重启后真实浏览器使用既有登录会话回读：商城 Pro v2 日收益 18 → Rack P1 日收益 45，首屏和升级卡均 2.5×；商品分别显示库存剩余 3 件、30 件；提现空金额显示“请输入提现金额”，按钮禁用。
- 工单切号竞态、Top N 非默认配置及跨结算时钟以隔离测试/源码交叉审查验证；未通过真实换号、修改奖池规则或实际结算验证。

## 运行状态

仅重启受影响的正式 App（5173，PID 12052）与 Java（8110，PID 6184）。PC（3002，PID 38688）、MinIO（9000，PID 5636）、GemmaRAG（8010，PID 16976）未重启，健康检查均正常。端口项目归属已验证。

App → Java 法律条款代理读取 HTTP 200、code 0；PC 无登录凭据访问会话代理 HTTP 401，符合预期。NexionLocalRuntimeWatchdog 为 Running。以上均为本次验证时点状态。

## 明确保留的范围

- 隐私协议未发布、领导池比例未配置：不擅自发布或设定业务值。
- 独立单笔/每日累计提现金额上限仍为能力缺口；没有把次数或余额比例冒充金额上限。
- 开发者文档及未开放接口能力未发布/实现，本次没有扩大为 OpenAPI 建设任务。
- 现有混合工作区改动均保留。未创建分支、提交或推送；仓库主线限制超过 5 文件的直接提交，并要求精确提交的干净工作区全量 verify，不能将本地修复测试等同于发布门禁通过。

审查质量自评：初审 97、复审 99。分数不代表 App 全功能验收。
