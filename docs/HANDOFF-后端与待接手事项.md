# 后端与待接手事项 —— 正文在后台仓

<!-- STALE-TERMS-OK: U-17 要请后端确认试用转化端点用的是新名还是卡时代旧名,必须把旧名原样写出来才能问。 -->

> **正文不在这里。** 交接书统一维护在后台仓,避免两份副本各自往前漂:
>
> ## `nexion-ops-console` 仓 → `docs/PRD/HANDOFF-后端与待接手事项_2026-08-07.md`
>
> 本文件只作指针 + 列出**与本仓(用户端)直接相关**的条目索引,细节去正文看。

## 与用户端直接相关的待办(索引)

| 编号 | 一句话 | 正文章节 |
|---|---|---|
| **U-1** | 🔴 `GEO_*` 地区拒绝码后端发不发?**13 个界面的守卫已放好,但今天一次都不会触发** —— 全仓没有代码产生这类错误 | 二 2.2 / 〇 第 2 条 |
| **U-2** | 🔴 `/api/stakes/` 与 `/api/staking/` 谁是真路径?本仓接口客户端 4 处用前者,PRD 与哨兵台账用后者 | 〇 第 1 条 / 三 Q-1 |
| **U-3** | 本仓错误映射函数缺单元测试(`src/api/geo-policy-error.test.ts` 当前线上不存在) | 二 2.2 |
| **Q-7** | 6 个动钱页面的地区拒绝**落点**:前端按「拒绝在扣款落地之前」接线并据此显示「余额未变动」。**若真后端可能在扣款之后拒绝,这句话就是错的**,必须一起改 | 三 Q-7 |
| **Q-11** | **原生 App 端**(非 H5)冷启动登录守卫的「读不到当前页」窗口从未被验证——H5 侧已实测修复(hash 兜底+常开重试轮询),原生端无 hash 可兜底,需真机构建验证 | 三 Q-11 |
| **U-4** | 🔴 **每日提现笔数上限的计数口径必须两边一致**。客户端(2026-08-11 起)按 `GET /api/withdrawals/policy` 的 `dailyLimitCount` 预检,笔数从**本地提现单列表**按平台日(UTC+7)现算,**已驳回 / 已退款 / 上链失败的单照样占额度**(数的是发起次数)。服务端若按别的口径(只算成功单、按 UTC 切日、按自然月…),会出现「客户端说不能提、服务端其实允许」——用户被自己的 App 挡住。**请后端确认口径,不一致以服务端为准并回来改客户端** | 本文件(新增,PRD §9.3 待补) |
| **U-5** | 🔴 **没有 `GET /api/withdrawals` 列表端点**。`app.withdrawals` 是本地持久化镜像(每次 `POST /api/withdrawals` 成功后把返回单据存进账号快照),换设备 / 清缓存后为空 → **日限预检会重新从 0 算**(服务端仍会正确拦截,只是预检失灵)。根治要么补列表端点,要么在 policy 响应里带上 `dailyUsedCount` | 本文件(新增) |
| **U-8** | 🔴🔴 **`dailyLimitCount` 下发 0 会打死整个提现页,不是「关闭日限」**。客户端契约要求它是 **≥1 的整数**,值为 0 / null / 小数会让**整份 policy 解析失败** → 最低额、费率、可用网络一起没了 → 页面显示「费率更新中,已暂停提交」。而「不限笔数」恰恰是运营最自然会用 0 表达的意思。**请后端确认**:① 0 是否合法、语义是什么;② 若要表达「不限笔数」用什么(建议省略该字段或用一个明确的大值),③ 关闭提现通道请走 J1 闸而不是把日限配成 0。已用本地假后端在真页面上复现(策略三态真值表) | 本文件(新增) |
| **U-7** | 🔴 **提现单时间戳的类型是日限的隐性契约**。客户端按 `submittedAt` 是 **number(毫秒)** 现算今日笔数。后端若在列表/建单响应里下发**字符串或 ISO 时间戳**,当日单会被**静默计 0** —— 日限预检再次恒不触发,与本次修的缺陷同型、同样无声、且不会有任何门变红(客户端加消毒也救不了:消毒的结果同样是 0)。请在契约里把它钉成 number,或告知将用 ISO,我们改解析 | 本文件(新增) |
| **U-6** | 提现单的 `submittedAt` 是**客户端**落的时间戳(`toCanonicalWithdrawal` 默认参数),日限的平台日归属就按它算。客户端改时钟即可影响预检(服务端不受影响)。建单响应里带上服务端 `createdAt` 后应改用它 | 本文件(新增) |
| **U-9** | 🔴 **`GET /api/withdrawals/:id` 请多回两个字段:`terminalReason` + `retriable`**(2026-08-11/12 包 z7 落地,客户端已完整消费)。不发也不会坏(缺省 = 不显示),但失败提现在用户眼里只有一句「审核未通过」,没有原因,客服也查不到。`terminalReason` 用后台 D2「拒绝原因码」下拉**同一套码**(`RISK_HIT`/`ADDRESS_RISK`/`DATA_MISMATCH`/`USER_CANCELLED`/`OTHER`);`retriable` 是布尔,含义=「这个用户就**这一笔**能否重新发起」——⚠️ 客户端**不会**拿它去禁用「再提一笔」(那是账号级动作),只在该单详情里如实说明。**两个字段最好与终态状态同一个响应发**:单据一进终态就离开在途集、之后不再回查,晚发的补不回来 | 正文详见后台仓 `admin-ops/docs/PRD/HANDOFF-后端与待接手事项_2026-08-07.md` 的 U-9(本条为本仓指针) |
| **U-10** | 🔴 **`PENDING` / `TX_ORPHANED` / `DEAD` 三个状态码此前会让客户端整张单据的状态回读失败**(抛协议错 → 被静默吞 → 那一单永久停在「处理中」,换绑收款地址与下一笔提现连带永久拦死)。2026-08-12 已修(前者归 `submitted`,后两者归 `tx-failed`),并焊了逐值比对你那张状态表的机器门。⚠️ `PENDING` 出现在**建单回执**里时更重:客户端会在单据入表**之前**抛错 —— 你那边钱已经扣了,用户这边连单号都没有。**请回查:后端还会发哪些客户端没见过的状态码?** | 同上;门 = `scripts/withdraw-terminal-reason-parity.test.mjs` |
| **U-11** | 🔴🔴 **失败提现要不要退还已烧 NEX——契约已定名 `nexRefunded`(FEAT-WD01 §4.6),但线上没有任何入口能把它送到客户端**。用户勾抵扣时服务端烧了 N 枚 NEX 抵手续费;单子若进失败终态,链上确认从没发生,那笔费也就从没真付过。**客户端侧已完成**(包 z6:宽松解析 + 同单号 `+N NEX` 冲正分录 + remote 模式行为门 `selfcheck-withdraw-nex-refund.mjs`;包 z8,2026-08-12:`nexRefundedAt` 的契约表 + 宽松解析 + 落盘合并与金额**成对**保留 + 冲正行按**单条分录自己的事件时刻**落账 + 机器门 ⑬),判据锚服务端字段、绝不由「单据失败了」倒推。**卡在三件事**:① 后端要在失败终态的**同一事务**里退还并下发 `nexRefunded` + `nexRefundedAt`(**成对**,ISO-8601 字符串不许发裸 epoch;靶 T16–T18)。🔴 **少发 `nexRefundedAt` 不会报错,但账本上那条冲正的日期就成了「客户端得知的此刻」而不是退款真正发生的时刻** —— 月份多半仍对,精确日期是猜的;② **提现单状态回查入口**(见 U-5)——提交即拒是零副作用不烧 NEX,所以退还只可能发生在提交之后,而客户端现在拿不到提交之后的任何状态;③ 🔴 **回查响应必须真的带上这两个字段,并合并回单据**——并行包 z7 已把回查端点写完,但它有自己的响应类型与解析器,只回 5 个字段(`withdrawalNo`/`status`/`confirmedAt`/`terminalReason`/`retriable`),**不带 `nexRefunded`**,且 patch 只覆盖 4 个字段。**按现状两包合并后,这条冲正照样一次都不会触发**(与本条要修的缺陷完全同形)。判据已焊进 `selfcheck-withdraw-nex-refund.mjs` ⑪:本仓一旦出现状态回查面而响应不带该字段,当场判红。✅ **第③件 2026-08-12 已在客户端补齐**:两包真合并当天该门如期报红,现已把 `nexRefunded` + `nexRefundedAt` 加进回查响应的契约与解析器,并成对合并回单据(没给就保留旧值,不覆盖成空)。**①②两件仍在后端**:客户端已备好接料口,你那边发了就能用 | 本文件(新增) / PRD §4.6 |
| **U-13** | 🔴 **提现失败后的重试:同一笔意图必须复用同一把幂等键,请求体也必须逐字节相同**。客户端侧已完成(2026-08-12):键与整个请求体在**发出请求之前**落盘,失败后按「服务端定局了没有」分四档处置 —— 日限拒单 / 409 幂等冲突 → 键退役;确定拒绝 → 键退役并刷费率;**结果未知(超时 / 断网 / 5xx / 应答读不懂)→ 键与 body 原样留着,下次点提交原样重放**。**需要你那边保证三件事**:① 幂等键的去重窗口要**长于**用户可能的重试间隔(至少 24h),否则窗口一过重放就变成第二笔;② 同 key + 异 body 时返回 **409**(客户端认这个码,会告诉用户「这笔已在处理中」而不是让他重试);③ 🔴 **`policyVersion` 一旦进了请求体就是幂等键的一部分** —— 客户端为此专门冻结了它、并在「结果未知」那一档禁止刷新费率策略,就是为了不让后台的发布节奏把重放的 body 改掉 | 本文件(新增) |
| **U-16** | 🔴 **创世售罄 / 关闭 / 熔断 / 未开售时,不要下发「买创世」周任务** —— 这件事**只能由你那边做**,客户端已确认做不了。周任务改成服务端下发(实际端点是 **`GET /api/quests/state`**,不是 PRD 里写的 `/api/quests/weekly`,那个名字全仓已无实现——顺带请后端确认以哪个为准)之后,客户端原本那道闸(创世关闭时不派该任务)自然消失。后果:用户领到一个**不可能完成**的任务,一周都做不掉,而任务系统还在替一个已经关停的活动做推广。<br>🔴 **上一版这里写「若你拿不到该状态,我们在客户端加兜底过滤」——那句话现在撤回**。客户端识别一条任务是不是「买创世」,唯一句柄是 `questCode` / `name`,而 `questCode` 在后台 H3 是**运营手输的自由文本**(mission-create 表单只要求「编号英文唯一」),没有枚举也没有分类字段。按字符串猜的两个方向代价不对称:猜漏 = 回到今天;**猜错 = 把一条 `CLAIMABLE` 的创世任务藏掉,等于扣掉用户已经挣到的奖励**,比它想防的问题更坏。所以过滤留在派发端。<br>✅ **客户端 2026-08-13 已落地的部分**:同一套启发式只用来**报警**不用来过滤 —— 服务端派了 `PENDING` 的创世任务而本地创世闸判阻断时,打 `console.error`(运行时探针统一断言 app console error = 0,所以联调时这类违约会当场把门打红,你我都看得见)。另焊了一条反向钉:任务面日后新增「点进创世」的入口必须问闸。<br>🔴 **如果你那边确实拿不到创世开关**:别让我们在客户端猜,请改成在 `CanonicalQuest` 上加一个服务端自己判的可用性字段(例如 `blocked` / `blockedReason`),客户端按字段渲染即可——那是有契约的,字符串猜不是。 | 本文件;客户端侧 `src/lib/quest-genesis-tripwire.ts`,门 = `scripts/selfcheck-quest-genesis-tripwire.mjs` |
| **U-15** | 🔴🔴 **请在提现记录里回显 `idempotencyKey`** —— 一个字段,解开一个客户端自己解不开的死结。现状:客户端把幂等键发出去(header + body),但 `GET /api/withdrawals` 回来的 19 个字段里**没有它**。后果:一笔提交超时后,客户端保留了键与冻结请求体准备重放,但它**无法判断那笔到底落库没有** —— 服务端列表里就算有那一单,客户端也认不出「这一单就是我那笔」(只能靠金额+地址+时间模糊匹配,两笔相同金额就分不清)。于是那笔尝试进入一个**没有出口的状态**:重放可能永远失败(例如回执格式对不上、或本机模式不对),而客户端既不敢作废键(可能已出账 → 重提 = 二次出账),也无法确认它已成功。**回显这一个字段之后**,客户端拉一次列表就能精确认出来并收口,不用猜、也不用问用户。⚠️ 请注意这**不是**「加个按幂等键查的接口」那么大的事——键你那边本来就存着(去重要用),只要在提现记录的响应里带上即可。若确实不便回显,退而求其次:提供 `GET /api/withdrawals?idempotencyKey=xxx` | 本文件(新增) |
| **U-14** | 🔴 **你的仓是单体仓,我们这边是前端独立 checkout —— 你新写的跨仓测试在我们机器上跑不了**。你有 4 个契约测试按 `root = ../..`(其下同时有 `backend/` 与 `app/`)去读 Java 源:`funds-run-scoped-isolation` / `funds-server-sandbox` / `h8-run-scoped-referral-projection` / `behavior-analytics-active-route-catalog`。我们这边全盘搜不到 `AppWithdrawalService.java`,它们必然 ENOENT。**危害不是这 4 个红,而是它们崩掉之后排在后面的门一道都不跑** —— 整条链呈现「零断言失败 + 退出码 1」,看着像通过了。已从本仓门链摘出、给了具名出口 `npm run test:cross-repo`,并在契约登记表里登记为 excluded 写明原因(不会变成没人管的孤儿)。**你那边单体仓环境用那个出口跑即可**;若希望它们进本仓主链,需要把定位方式改成可配置(环境变量指向后端仓根,缺失时明确 skip 并计数,而不是崩) | 本文件(新增) |
| **U-17** | 🔴 **试用转化端点请确认名字:`POST /api/trial/convert` 还是 `POST /api/trial/redeem-early`?** 客户端这边已按前者为准。理由:根 PRD §9.11a.2 的表在 2026-08-02 无卡化(FEAT-TRIAL02)时把 `redeem-early` 那一行**改名**成了 `convert`,两行的结算公式逐字相同(`trialPriceUSD` − 早购促销 `min(rate, discountCapUSD)` − 收益抵扣 `min(accrued, trialOffsetCapUSD)`,`remainderUSD` 购后入余额,返回价格拆解);签字规格 FEAT-TRIAL02 里只有 `convert`,没有 redeem 概念。但 2026-07-28 / 08-12 两笔后端对接提交在接口层留的是**旧名**,且从没接过消费点 —— 我们已按「旧名 = 改名前的残留」删掉了那个方法(证据链见 `docs/changes/2026-08-13-trial-early-buy-adjudication.md`)。**如果你那边线上真的还在服务 `redeem-early`,请告诉我们**,我们按你的名字重接;⚠️ 注意两者响应形状不同:PRD 说这个端点回**价格拆解**,而被删的那个方法解析的是 state 信封(`authoritative/state/version/...`),两边对不上,所以就算名字对了也要重新定响应契约。<br>🔴 **另一件更要紧的**:服务端转化**整条链目前没接线** —— `free-trial.ts` 的 `convert()` 在真后台档直接 return false,结算页因此在真后台档走不完试用转化。这是客户端欠账,不是你的,但接线时需要你这边的 `convert` 契约(请求体 / 响应价格拆解字段名 / 幂等键)。 | 本文件(新增);客户端侧 `src/api/trial-api.ts` |
| **U-18** | 🔴 **兑换排队期间,用户的资金到底占没占住?** 客户端查不到答案(排队语义在服务端),但这个答案决定一个缺陷的优先级。现状:用户当日兑换触顶个人上限后可选择**排队到下一个平台日**成交(PRD §9.4.3),兑换页**会把排队单显示给用户**,但**没有任何撤销入口** —— `cancelRemoteOrder()` 写好了(幂等键 / 失败态俱全)却零调用,`POST /api/exchange/:id/cancel` 因此从没被真正走过。<br>· **若排队期间不占资金**:问题是「用户被锁在一笔明天成交、明天汇率未知的兑换上,撤不掉」——汇率风险,该修但不紧急。<br>· 🔴 **若排队期间占住/冻结了资金**:那就是**资金被锁且客户端无解锁路径**,优先级要往上提。<br>另请一并确认接撤销时的三件契约:①哪些状态可撤(客户端假设仅 `QUEUED`)②撤销后当日额度退回**当日**还是**次日** ③有没有撤销次数限制。客户端侧已备好调用,接上即可。 | 本文件(新增);客户端侧 `src/pages/me/wallet-exchange.vue`,卡 = `docs/changes/2026-08-14-card-dead-exchange-cancel.md` |
| **Q-12** | H5 **回前台的唯一触发源是一个浏览器事件**(独立审查读框架源码证实,无第二条路径)——浏览器「整页跳出→系统返回」的缓存恢复场景若不补发该事件,**全部轮询(采矿/结算/守卫)回前台不重启**。需真机/多浏览器矩阵专走这条路径(切标签页/切 App 必触发,测不出它) | 三 Q-12(细节:仓内 `docs/PORT-PITFALLS.md` P-058) |

## 这个仓的两条脾气(动手前必看)

1. **有些机器门会把业务函数的原文抓出来真跑**(`new Function` 注入)。改了那个函数却没同步喂新依赖,门会**崩溃而不是报红**,报错内容还和该门守的事毫无关系。
   动手前跑:`grep -l "new Function" scripts/*.mjs`,逐个看它抓哪个函数。⚠️ 一道门里可能有**多个注入环境**,要逐个核。
2. **注释里的接口地址有专门的门在盯**(`scripts/endpoint-citation-sentinel.mjs`)——注释不参与编译,写错了 tsc 抓不到。新增引用要同步登记台账并注明 PRD 出处。

> 维护约定:**不要把交接内容拷到这里**。要加新条目,加到后台仓的正文里,这里只补一行索引。
