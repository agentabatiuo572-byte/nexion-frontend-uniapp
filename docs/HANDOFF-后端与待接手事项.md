# 后端与待接手事项 —— 正文在后台仓

<!-- STALE-TERMS-OK: U-17 要请后端确认试用转化端点用的是新名还是卡时代旧名,必须把旧名原样写出来才能问。 -->

> 🔴 **本文件只是指针 + 一行式索引,不含条目正文。** 交接书唯一正文在后台仓:
>
> ## `nexion-ops-console` 仓 → `docs/PRD/HANDOFF-后端与待接手事项_2026-08-07.md`
>
> **判据(机器门 `scripts/handoff-pointer-gate.mjs`,挂 verify 门链)**:本文件表格行单行 ≤300 字符、
> 禁出现「本文件(新增」字样;索引 U-x 集合与正文 U-x 集合**相等**(双向核,任一侧漂移即红)。
> **要加新条目**:正文写后台仓(编号 = 正文现有最大号 +1,先查号防撞;U-12 历史跳号勿补用),再回这里补一行索引。
> ⚠️ 撞号更正(2026-08-14):本文件旧 U-4「提现日限计数口径」与正文 U-4「publicStats」撞号,已重编为 **U-19**;
> 本仓 2026-08-11~13 变更卡里写的 U-4 指的就是现在的 U-19。

## 与用户端直接相关的待办(索引)

| 编号 | 一句话 | 正文位置 |
|---|---|---|
| **U-1** | 🔴 `GEO_*` 地区拒绝码后端发不发?13 个界面守卫已放好,今天零触发(全仓无代码产生该错误) | 二 2.2 / 〇 第 2 条 |
| **U-2** | 🔴 `/api/stakes/` 与 `/api/staking/` 谁是真路径?客户端 4 处用前者,PRD 与台账用后者 | 〇 第 1 条 / 三 Q-1 |
| **U-3** | 错误映射函数缺单元测试(`src/api/geo-policy-error.test.ts` 线上不存在) | 二 2.2 |
| **U-4** | 🔴 `/api/config/platform` 的 `publicStats` 投影值由你定,前端只校验值域;请按可信诱人档下发 | 二 2.2 + U-4 详注 |
| **U-5** | 🔴 缺 `GET /api/withdrawals` 列表端点:换设备后日限预检与注销拦截双双失灵 | 二 2.2a |
| **U-6** | `submittedAt` 是客户端时间戳,改时钟可影响日限预检;建单响应带服务端 `createdAt` 后应改用 | 二 2.2a |
| **U-7** | 🔴 提现单时间戳必须是 number(毫秒);发 ISO 字符串则当日笔数静默计 0 | 二 2.2a |
| **U-8** | 🔴🔴 `dailyLimitCount` 下发 0 会打死整个提现页,不是「关闭日限」;0 的语义请确认 | 二 2.2a |
| **U-9** | 🔴 `GET /api/withdrawals/:id` 请多回 `terminalReason` + `retriable`,且与终态同一响应发 | 二 2.2 + U-9 详注 |
| **U-10** | 🔴 `PENDING`/`TX_ORPHANED`/`DEAD` 曾致单据回读静默失败(已修);还会发哪些未知状态码? | 二 2.2;门 = `withdraw-terminal-reason-parity` |
| **U-11** | 🔴🔴 失败提现退 NEX:`nexRefunded`+`nexRefundedAt` 须同事务成对下发;客户端接料口已备好,①② 在后端 | 二 2.2a / PRD §4.6 |
| **U-13** | 🔴 重试必须复用同一幂等键:去重窗口 ≥24h / 同 key 异 body 回 409 / `policyVersion` 进体即冻结 | 二 2.2a |
| **U-14** | 🔴 你仓 4 个跨仓契约测试在前端独立 checkout 必 ENOENT 且崩链,已隔离到 `npm run test:cross-repo` | 二 2.2a |
| **U-15** | 🔴🔴 提现记录请回显 `idempotencyKey`:解开「结果未知,不敢重放也不敢作废」的死结 | 二 2.2a |
| **U-16** | 🔴 创世售罄/关闭时别下发「买创世」周任务;过滤只能在派发端;端点名 `/api/quests/state` 请确认 | 二 2.2a |
| **U-17** | 🔴 试用转化端点定名:`convert` 还是 `redeem-early`?另:真后台档转化链客户端尚未接线 | 二 2.2a |
| **U-18** | 🔴 兑换排队期间资金占没占住?这决定「排队单无撤销入口」的优先级;撤销三件契约一并确认 | 二 2.2a |
| **U-19** | 🔴 (原 U-4)每日提现笔数的计数口径两边必须一致;不一致以服务端为准并回改客户端 | 二 2.2a |
| **U-20** | 🔴 待支付订单请下发 `paymentDeadline` + 支付指令(地址 / 网络 / 精确金额);现「待支付会话 + 浮动条」只在本地支付腿工作,取消接口与已转账后取消的对账口径待你定 | 二 2.2a;用户端 PRD §7.3.5 |
| **U-21** | 🔴 远端建单幂等键的耐久性不能只靠客户端本地存储:请按账号 + 意图在短窗口内幂等(同意图重复请求回既有单) | 二 2.2a |
| **Q-7** | 6 个动钱页地区拒绝按「扣款前拒绝」接线;若真后端可能扣款后拒,「余额未变动」文案连带要改 | 三 Q-7 |
| **Q-11** | 原生 App 端冷启动「读不到当前页」窗口未验证(H5 已修;原生无 hash 兜底),需真机构建 | 三 Q-11 |
| **Q-12** | H5 回前台唯一触发源是一个浏览器事件;缓存恢复场景不补发则全部轮询不重启,需真机矩阵专走 | 三 Q-12;细节 = 仓内 `docs/PORT-PITFALLS.md` P-058 |
| **Q-13** | 🔴 钱包余额以谁为准?`walletUsdt` 口径(含不含在途提现)定了,才知道删本地扣款腿还是停覆写 | 三 Q-13 |
| **E-6** | 🔴 资金操作失败后「没人补、也不告诉用户」是全站族(10 处确认),按族治不逐个补 | 五 E-6;卡 = 仓内 `docs/changes/2026-08-13-card-funds-failure-family.md` |

> U-12 从未使用(历史跳号,非丢条)。本索引只收「与用户端直接相关」的条目;后台侧 B-x 与其余 Q-x / E-x 直接看正文。

## 这个仓的两条脾气(动手前必看)

1. **有些机器门会把业务函数的原文抓出来真跑**(`new Function` 注入)。改了那个函数却没同步喂新依赖,门会**崩溃而不是报红**,报错内容还和该门守的事毫无关系。
   动手前跑:`grep -l "new Function" scripts/*.mjs`,逐个看它抓哪个函数。⚠️ 一道门里可能有**多个注入环境**,要逐个核。
2. **注释里的接口地址有专门的门在盯**(`scripts/endpoint-citation-sentinel.mjs`)——注释不参与编译,写错了 tsc 抓不到。新增引用要同步登记台账并注明 PRD 出处。

> 维护约定:**不要把交接内容写到这里** —— 正文只住后台仓,这里每条只有一行。
> 机器门 `scripts/handoff-pointer-gate.mjs`(随 `bash scripts/verify.sh` / `npm run test:legacy-suite` 跑)在盯:
> 行超长 / 「本文件(新增」字样 / U-x 双向集合不等,任一即红。
