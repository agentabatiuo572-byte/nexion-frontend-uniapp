# PORT-LEDGER — Next.js H5 → uni-app 迁移台账

> 单一事实来源：迁移进度 + 依赖版本锁 + 基建状态。每批收尾更新。
> 状态：`pending`（未开始）· `ported`（已写 .vue + tsc 0）· `verified`（自测+审查过）。
> 配套：`PORT-PITFALLS.md`（踩坑）· `nexion-uniapp-port` SKILL（流程）。

## 工程坐标

- 源：`D:\WORKS\PLAN\Nexion-prototype`（Next.js 16 + React 19 + Tailwind v4，80 页）
- 标的：`D:\WORKS\PLAN\Nexion-uniapp`（uni-app Vue3 + Vite + TS）
- 目标端：App(iOS+Android) + H5；全用 `.vue`（webview 渲染，不用 .nvue）
- H5 dev：`npm run dev:h5` → http://localhost:5173

## 依赖版本锁（踩坑后固定，勿随意升级）

| 包 | 版本 | 备注 |
|---|---|---|
| @dcloudio/* (uni) | 3.0.0-4080420251103001 | 模板自带；锁 vue 3.4.21 |
| vue | 3.4.21 | uni 生态锁定，勿升 3.5 |
| pinia | **2.2.8** | ≥2.3 要 vue 3.5，必须停在 2.2.8 + `--legacy-peer-deps`（P-003） |
| unocss | 66.7.0 | presetWind3 + presetAttributify；ESM-only → `type:module`（P-001/002） |
| vite | 5.2.8 | 模板自带 |

`package.json` 必须有 `"type": "module"`；`vite.config.ts` 必须做 uni CJS interop（P-002）。

## 基建状态（Batch 0）✅

| 模块 | 文件 | 状态 |
|---|---|---|
| 设计 token（108·双主题） | `src/styles/tokens.css` | verified |
| UnoCSS（任意值保 Tailwind 类） | `uno.config.ts` `vite.config.ts` | verified |
| i18n（94 namespace·en/zh 镜像） | `src/i18n/*` | verified（系统检测→zh） |
| locale store（uni storage 双端持久） | `src/store/locale.ts` | verified |
| Pinia + 全局原语（toast/confirm/netError） | `src/store/ui.ts` | verified |
| 全局 Host（GlobalUi） | `src/components/global-ui.vue` | verified |
| chassis（路由感知:tab pill+brand/title header+search+bell badge / 子页 back-mode 隐 tabbar） | `src/components/app-chassis.vue` | **verified ✅ 字段级重修**(P-036:Batch0 简化版→忠实原型 tab-bar.tsx/header.tsx,浮动磨砂 pill+active 渐变 brand pill+子页隐 5-tab,零页面改动) |
| 机制（verify/skill/台账/hooks） | `scripts/verify.sh` `docs/*` SKILL | verified |

## 页面迁移矩阵（80 页）

### MVP 核心漏斗（Batch 1 样板 + Batch 2 铺量）— 目标 ~18 页

| # | 源路由 | uni 页面 | 端 | 状态 |
|---|---|---|---|---|
| 1 | onboarding/intro | pages/onboarding/intro.vue | H5 ✓ | **verified** |
| 2 | onboarding/estimator | pages/onboarding/estimator.vue | H5 ✓ | **verified** |
| 3 | onboarding/connect | pages/onboarding/connect.vue | H5 ✓(tsc+intro渲染;calib/result逻辑同源待视觉补) | **verified** |
| 4 | register | pages/register/register.vue | H5 ✓(step1+sponsor确定性;step2/3逻辑同源tsc过) | **verified** |
| 5 | login | pages/login/login.vue | H5 ✓(password+otp mode 渲染+切换;reset+signIn timer+safeReturnTo 逻辑同源tsc过) | **verified** |
| 6 | (main) `/` home | pages/index/index | H5 ✓ | **✅ 完整 verified**(全 19 section,21 home 组件,Playwright 0 err 双语,11 sparkline 渲染。整个 mission-control.tsx 3157 行已拆迁) |
| 7 | store | pages/store/store | H5 ✓ | **✅ verified**(Playwright 0 err 双语,3 product 图 + Hero 640× + 算力阶梯 5 档 + vsPhone + 产品卡 Best Seller + locked gen2 全渲染) |
| 8 | store/[productId] | pages/store/detail?id= | H5 ✓ | **✅ verified**(Playwright 0 err 双语;ProductRender+live social proof+vsPhone+ROI stepper+specs+reviews+FAQ+sticky CTA,?id= 路由生效) |
| 9 | store/bundle | pages/store/bundle | H5 ✓ | **✅ verified**(Playwright 0 err 双语;套餐结算 + 折扣阶梯 2+/3+/4+ →5/8/12% + 空态 + 推荐商品) |
| 10 | store/checkout | pages/store/checkout | H5 ✓ | **✅ verified**(Playwright 0 err 双语;步骤条+合计$1299·1082%年化+4 支付方式,?product= 路由生效) |
| 11 | store/orders | pages/store/orders | H5 ✓ | **✅ verified**(Playwright 0 err 双语;空态「暂无订单·去逛商城」正确) |
| 12 | store/orders/[id] | pages/store/order-detail?id= | H5 ✓ | **✅ verified**(Playwright 0 err 双语;not-found 优雅降级「订单不存在」) |
| 13 | me | pages/me/me | H5 ✓ | **✅ verified**(Playwright 0 err 双语;profile+wallet卡+快捷操作+network+trial入口,60 SVG) |
| 14 | me/wallet | pages/me/wallet | H5 ✓ | **✅ verified**(Playwright 0 err 双语;USDT/NEX 余额+收益明细+交易记录) |
| 15 | me/wallet/topup | pages/me/wallet-topup | H5 ✓ | **✅ verified**(Playwright 0 err;多通道充值 USDT/BTC) |
| 16 | me/wallet/withdraw | pages/me/wallet-withdraw | H5 ✓ | **✅ verified**(Playwright 0 err 双语;KYC 合规门 MiCA/FATF+金额) |
| 17 | (main) earn | pages/earn/earn | H5 ✓ | **✅ verified**(Playwright 0 err 双语;PillTabs+Compute earned $247.87+正式/AI拆分+错过收益−$2.46/累计−$1153+TrialHero,47 SVG) |
| + | (net-new) onboarding/terms | pages/onboarding/terms.vue | H5 ✓ | **✅ verified**(净增页:源 intro 的 termsLink 是点不动的假链接→建真服务条款页[10 编号段,口吻对标 risk-disclosure 真平台/无 meta]+i18n terms 命名空间 en/zh 27 键镜像+intro termsLink 接 navigateTo+下划线 affordance;Playwright 双语 0 err,往返 nav 通,verify 15/15) |

> 动态段 `[id]` → uni `?id=` query（P：uni 无文件动态段）。
> net-new：onboarding/terms 是源原型没有的页。intro termsLink + register 条款脚注「同意服务条款和隐私政策」（i18n 拆 termsPrefix/termsServiceLink/termsAndPrivacy）均已接 navigateTo→terms，「隐私政策」无页保持 plain（不做假链接）。硬件衰减文案(terms s4 + risk-disclosure s2)按主人指令去具体数值改模糊。**全站 3 处条款入口全收口**：intro 脚注 + register 脚注 + ref/code footer「条款」均 navigateTo→terms（双语实景验）；所有「隐私政策/隐私」无页统一保持普通文本（ref/code 的隐私从 footerLinkStyle 降 ink-4，去假链接）。PRD 已同步(§3.3 + §4.7.5 + §15.3)。

### 其余页（全部 ✅ — 迁移收官）

market ✅ · **team 域全收口 ✅**(team/binary/agent/commissions + rank/leaderboard/leadership-pool/network/quota/tree/unilevel + rank-how/binary-how/commissions-how/unilevel-how/leadership-pool-how) · genesis(+holder/how-it-works/marketplace) ✅ ·
staking ✅ · daily ✅ · missions ✅ · events ✅ · learn ✅ · globe ✅ · developer ✅ · search ✅ ·
me 子页（**devices/goals/profile/security/kyc ✅** · wallet-bills/wallet-exchange/wallet-nex/wallet-cards/wallet-cards-new/receipts/proof/wrapped ✅ · wallet 全套 ✅ · **achievements/help/support/support-tickets/language/notifications/preferences/replay-tour/risk-disclosure/trial/wallet-premium/wallet-nex-v2-lock/wallet-repurchase/wallet-repurchase-how/wallet-exchange-how/wallet-withdraw-tracking ✅ 收官批**）· **trust/trust + trust/nex ✅** · **ref/[code]→ref/code?code= ✅** · **tx/[hash]→tx/hash?hash= ✅**

**🎉 迁移 COMPLETE：源 `app/(main)` 73 路由 + ref/login/register/onboarding 全部有对应 uni 页。** 完整枚举见原项目 `find app -name page.tsx`。

**2026-06-13 外部整改 gate 收口**：`Nexion-admin-prototype/docs/remediation/specs/SPEC-L3c01-uniapp-full-route-batch-audit.md` 已 verified。Next 80 路由全部映射到 UniApp 81 pages（唯一 net-new: `pages/onboarding/terms`）；`UNI-FR-01`~`UNI-FR-10` H5 runtime 在 `http://localhost:5173` 重新跑完 82/82 captured、0 error；front action sampler 86/86 sampled、0 `no-observable-change` / `click-target-missing` / `hash-only-no-content`。本仓 `bash scripts/verify.sh` 已修 WSL `curl.exe` fallback 并通过 **15/15**（H5 probe 为 `Home shell [200] /`）。

## 进度

- 基建：**8/8 ✅**
- MVP 漏斗：**verified 若干 + ported 若干**（onboarding + auth + home ✅；store 主列表/detail/checkout/orders/order-detail + me/wallet 全套 + **bundle + earn(本批)** ported 待实景）
- 全量：~16 verified + ~8 ported（+ 本批 store/bundle · earn/earn ported 待实景）
- **app store 引擎 ✅**（device-types + state + **完整 tick 保真** + App.vue tick 驱动 + receipts store + 依赖链 types/tasks/ai-clients/device-lifecycle/server-time/interrupt/receipt 全 cp）。device-CRUD:**addDevice/activateDevice/deactivateDevice/scheduleDeactivation ✅(me/devices 批补,点亮 orders device-spawn)** + submitWithdrawal/advanceWithdrawal/setPhoneRuntime ✅(前批)。剩 recycle/replace/moveToInventory(trade-in 三动作)+ _dev helpers 按 trade-in/replay-tour 页按需补。

### home(mission-control) 基础层 ✅（2026-06-04 本批）
- **基线修复**：`trial-config.ts` vue-tsc FAIL(TS2740, setup store 显式返回注解与 Ref 冲突 P-017)→ 去注解,恢复绿。
- **6 v3 store 移植 ✅**：`commission` · `v-rank`(+nextRankProgress/nextRankGap/V_RANKS) · `leadership-pool`(+V_VOTES) · `genesis`(+GENESIS_ROYALTY_RATE) · `points` · `stella`(+agentReplyForIndex)。zustand→Pinia,method→function(带括号调用),持久化用 hydrate+uni storage,非持久化裸 ref。
- **→ home 的 11 个 v3 store 依赖现已全部就绪**(free-trial/market/profile/trial-config/trial-claim-sheet 先前 + 上述 6 个)。
- **3 primitives + TrialGhostSlot ✅**：`card-stagger.vue`(framer staggerChildren→CSS nth-child,弃 nav-memory P-018) · `stella/stella-avatar.vue`(`<image>` + v5-stella-halo keyframe,资源 `static/img/marketing/stella-avatar.png` HTTP200 已验) · `composables/use-scroll-grow-progress.ts`(IntersectionObserver) · `trial-ghost-slot.vue`(live shadow earnings hero,内联 SVG,fmt 插值)。
- 新增 keyframe `v5-stella-halo` + `nx-card-stagger-in` 入 tokens.css。verify **9/9**(+新哨兵 `no defineStore setup return annotation`)。

### home ZONE-1 partial ✅(2026-06-04 本批 · Playwright 实景验证)
- **基础补全**：`selectPhoneDevice`(device-types.ts) · `useTicker`(composables,number 漂移+大跳重同步) · 3 coupon keyframe(v5-ticket-enter/shimmer/hb-pulse) · TechMoneyCard 6 i18n 键(源硬编码英文→`t.home.tech*`,en/zh 镜像,**双语已实景验**)。
- **3 section + coupon 迁移 ✅**：`home/greeting-header.vue`(时段问候+节点收益) · `home/tech-money-card.vue`(cross-stream today 大数+aurora+grid+drift dots+peer/payback,useTicker 驱动) · `trial-hero-banner.vue`(coupon,mask-image notch 实景切出,3D flip 入场经 useScrollGrowProgress)。
- **index.vue 组装**:`<AppChassis active=home><CardStagger>` 4 section。
- **Playwright 实景**(414×896 dark/zh):**0 console error**,$295.06=earnings 247.83(与源一致)+commission,coupon notch 正确,双语全生效。**抓到并修 P-019**(uni `<view>` template ref 返组件实例非 Element,IntersectionObserver 挂→经 $el 解析;tsc+哨兵都抓不到,只实景暴露)。
- **下一步**:ZONE-1 余 3 section(ConversionBanner 704 / DayOneQuestCard 306「100% v5 稿」/ LiveFeedCard 2584)→ ZONE 2-7(21 section,见 mission-control.tsx 行号图)。CSSProperties 标注 style 对象(避 StyleValue TS2322)。

### store 主列表页 ✅ ported(2026-06-04 本批 · tsc 0 + 哨兵 9/9,实景待父)
- **2 新 store**:`product-phase.ts`(PHASES 12 月表 + getMonthsSince/getPhaseForMonth/isPhaseReached 纯函数 + `useProductPhaseOverride` 持久 store,NODE_ENV guard 改 `import.meta.env.PROD` 避 App 端 `process` 未定义) · `orders.ts`(zustand persist→Pinia + uni storage,createOrder/advanceOrder/markActivated/cancelOrder/getById + tickOrders。`advanceOrder` 的 addDevice/activateDevice 经 `DeviceSpawnApp` 可选方法探测——app store 设备 CRUD 未补时优雅降级,补上自动点亮)。
- **复用**:`useApp`(devices/user.joinedAt) · `useScrollGrowProgress`($el 修复版,locked 卡进度条) · `toast`(notify) · `fmt` · 现有 `store` i18n namespace(secRecommended/secMoreTiers/secComingSoon/secNetworkLadder/tradeinWindow.*/locked*/comingSoonHeading 全已存在,直接用)。
- **1 mock**:`src/mock/products.ts`(6 tier PRODUCTS + annualRoiPct/getProduct/TRADEIN_UPGRADE_MAP,零 React 依赖直接 cp)。
- **1 composable**:`composables/use-product-phase.ts`(源 hook→`ComputedRef<PhaseParams>`,组合 user.joinedAt + override)。
- **page + 8 组件**:`pages/store/store.vue`(组装 + phase-gate 过滤 + mounted guard)+ `components/store/{section-header,store-hero,cluster-ladder,vs-phone-hero,tradein-window-banner,product-card,spec-pill,purchase-ticker,locked-product-card}.vue`。最大 product-card.vue(~560 行 < 800)含 ProductRender(photo `<image>` + ribbon + tier/legacy/cloud chip)。
- **i18n**:store namespace 补 60 键 ×2(en/zh 镜像,`heroEyebrow`…`pageFooter`,源硬编码英文→t.*)。
- **静态资源**:3 product png cp 进 `src/static/img/products/`(s1-v4/pro-v2/rack-p1-v2)。
- **pages.json**:+`pages/store/store`(navigationStyle custom)。
- **导航目标(未建页,均 fail no-op)**:`/pages/store/detail?id=`(卡片点击)·`/pages/store/checkout?product=`(Buy CTA)·`/pages/store/orders`(订单 chip)·`/pages/me/devices`(trade-in)。
- **默认渲染**:TradeinWindowBanner 默认隐藏(P1 phase + 仅 phone 设备,与源一致);gen2(Pro v2/Rack P2)默认进 locked「Coming soon」区(phase 未达)。
- 新坑 **P-020**(`importsNotUsedAsValues:error` 下纯类型 vue 导入须 `import type` 非 `import { type X }`),见 PITFALLS。

### 购买→订单生命周期 ✅ ported(本批 · tsc 0 + 哨兵 9/9,实景待父)
- **3 页 + 3 组件**:`pages/store/{checkout,orders,order-detail}.vue` + `components/store/{chain-payment,card-payment,checkout-row,order-detail-row}.vue`(均 < 800 行)。
- **checkout 状态机**:select-payment→confirm→pay-instructions(chain/card)→awaiting→confirmed→activating→live。React `useEffect([step])` 自动推进链 → Vue `watch(step)` + setTimeout 链(2400/1500/3000ms)。进入 `confirmed` 时**一次性**副作用:`app.debitBalance(price+卡费)`(不足则回 select-payment)→`orders.createOrder`→`bills.add`(signed -total),first-order 庆祝。`confirmingRef` 防双击守卫保留。framer AnimatePresence/spring → scoped CSS `@keyframes`(step-in/pop-in/spin/progress-fill,带 prefers-reduced-motion)。
- **跨 store 编排在 handler 组合**(store 互不 import):debit + createOrder + bill 三步在 watch 内顺序调,对齐源。读 `?product=` 或 `?id=`(onLoad)。
- **orders 列表**:`orders.orders` → 状态徽章(Clock/CheckCircle2/Server/Cpu/XCircle 内联 SVG)+ 空态 browse CTA + 卡片点击 navigateTo order-detail?id=。
- **order-detail**:`?id=` → hero 状态(color-mix tint 替源 `${color}10` hex 后缀)+ activated→Earn 跳转 + 订单摘要 + 4 段时间线(当前点 ring) + cancel(仅 placed,经 `confirm()` danger)。**3s tick** `orders.advanceOrder(id)`(组件用 onUnmounted 清,页面用 onUnload+onUnmounted)。
- **复用**:`orders.ts`(createOrder/advanceOrder/cancelOrder/timelineFor 全现成)·`bills.ts`(add)·`app.ts`(debitBalance/devices)·`ui.ts`(confirm/toast)·`products.ts`(getProduct/annualRoiPct)·现有 `store`/`orders` i18n namespace(co*/firstOrder*/orders.* 大量已存在)。
- **i18n**:store namespace 补 ~44 键 ×2(en/zh 镜像,`coStep*`/`coTotal`/`coContinue`/`coSendNetwork`/`coOrderPlaced`… 源硬编码英文→t.*)。orders.* 零新增(全现成)。
- **pages.json**:+3 页(checkout/orders/order-detail,navigationStyle custom)。
- **优雅降级(基建未到,父后续接线)**:① **trade-in intercept** 省略(`useTradeinSheet`/`useDeviceEligibility` 未迁,属 Trade-in Batch);② **card 表单**用 mock 单卡(`useCards` 未迁)——`/me/wallet/cards/new` 绑卡页未建,接真 cards store 时换回空态+绑卡链;③ `mockExternalAddress` 内联进 chain-payment(`wallet-pairing.ts` 未迁,仅借一函数不整迁 store)。
- **导航目标(未建页,均 fail no-op)**:`/pages/earn/earn`(activated→Earn)。
- 新坑 **P-021**(component 用 `onUnload` 不触发,定时器泄漏 → 必用 `onUnmounted`),见 PITFALLS。

### me / wallet 域 ✅ ported(本批 · tsc 0 + 哨兵 9/9,实景待父重启 dev)
- **4 页**:`pages/me/me.vue`(13 section profile+钱包卡+设备+网络+genesis+订单+4 设置组)·`pages/me/wallet.vue`(余额 hero + Earnings/Activity 列表 + NEX callout)·`pages/me/wallet-topup.vue`(常规充值 channel→链上 QR/卡表单 + KYC-Express ?kyc=1 四态机)·`pages/me/wallet-withdraw.vue`(KYC 门 + 金额/网络/地址 + 摘要 + 质押反话术 + 积分门 + 提交)。
- **20 组件**(均 < 800 行,`src/components/me/*`):section-header / profile-row / wallet-card(+wallet-action-btn)/ withdrawal-locked-warning / network-card / my-devices-entry / trial-entry / genesis-node-card(+stat-cell)/ orders-card / settings-card(+setting-row + theme-row)/ wallet-list-row / topup-card-form(+card-brand-badge)/ verify-row / complete-row / stake-alternative-card。
- **app store 新增 action**(扩 `src/store/app.ts`,无显式返回注解):`submitWithdrawal(amount,network,address)`(原子 debit,不足返 null,对齐源 Round-7 P0)+ `advanceWithdrawal()`(tracking 状态机推进)。`recordDeposit` 加 NaN/±Inf/>1e9 输入校验对齐源。**device-CRUD 未加**(这 4 页都不用——任务规则「IF a page uses them」)。
- **新 store**(zustand persist → Pinia + uni storage):`wallet-pairing.ts`(KYC 配对状态 + `mockExternalAddress`)·`theme.ts`(light/dark,H5 `#ifdef` 套 `setAttribute(data-theme)`)·`risk-disclosure.ts`(提现前置接受门)。
- **复用现成 store**:app(user/devices/earnings/balances + creditBalance/recordDeposit/submitWithdrawal)·bills(add)·receipts·orders·points(`pointsRequiredFor`/`signIn`/`spend`/`earn` 积分门)·profile(displayName)·free-trial(`status`/`canStart`/`trialReservesSlotNow`)·trial-claim-sheet(`show`)·trial-config(`config`)·v-rank(`nextRankGap`/`V_RANKS`)·genesis(`remaining()`/`soldSlots`)·commission(`totalUSDTLifetime()`)·locale·ui(confirm/toast)·device-types(`MAX_DEVICES`/`derivePromoUpgrade`)·use-product-phase composable。
- **跨 store 编排在 handler 组合**(store 互不 import):提现 = `points.spend` + `app.submitWithdrawal` + `bills.add`(debit 失败回退 points);KYC 完成 = `pairing.complete` + `app.creditBalance(1)` + `bills.add`(kyc);卡充值 = `app.recordDeposit` + `bills.add`(topup),对齐源 Round-12 P0。
- **i18n**:全 namespace **已存在零臆测**(me 49 键 / wallet / walletV3 含 stakeAlt / kycExpress.flow / myDevices / trial / orders 全现成,P-016 grep 验过)。**仅补 3 键 ×2**(en/zh 镜像):`me.signOutConfirmTitle`/`signOutConfirmMsg`/`signOutConfirmLabel`(源硬编码英文 "Sign out of this device?")。
- **pages.json**:+4 页(me/me · me/wallet · me/wallet-topup · me/wallet-withdraw,navigationStyle custom)。
- **优雅降级(基建未迁,设置行的次要展示值用忠实静态占位;行本身均 nav 链)**:security(2FA 状态)·achievements(计数)·notifications(未读 badge)·cards(银行卡数)四 store 未迁——2FA=off / 成就 0/6 / 未读 0 / 卡 hint。KYC 状态走**已迁** wallet-pairing(真值)。
- **导航目标(未建页,均 `fail:()=>{}` no-op)**:`/pages/me/profile` · `/pages/me/security` · `/pages/me/notifications` · `/pages/me/receipts` · `/pages/me/risk-disclosure`(提现风险门 redirect) · `/pages/me/language` · `/pages/me/preferences` · `/pages/me/replay-tour` · `/pages/me/help` · `/pages/me/support` · `/pages/me/devices` · `/pages/me/goals` · `/pages/me/achievements` · `/pages/me/wrapped` · `/pages/me/wallet-bills` · `/pages/me/wallet-exchange` · `/pages/me/wallet-nex` · `/pages/me/wallet-cards` · `/pages/me/wallet-repurchase` · `/pages/me/wallet-withdraw-tracking`(提现后跳) · `/pages/team/team` · `/pages/team/rank` · `/pages/missions/missions` · `/pages/daily/daily` · `/pages/events/events` · `/pages/learn/learn` · `/pages/staking/staking` · `/pages/genesis/genesis` · `/pages/trust/trust` · `/pages/developer/developer` · `/pages/login/login`(登出 reLaunch)。
- 新坑 **P-023**(scoped keyframe 不跨组件,共享动画放 tokens.css)+ **P-024**(filter 后联合不收窄,赋窄 ref 须 narrow 函数),见 PITFALLS。tokens.css 全局补 `@keyframes spin` + `nx-step-in` + `.nx-step-in`。

### earn 标签页 + bundle ✅ ported(本批 · tsc 0 + 哨兵 11/11 + per-file curl-compile,实景待父重启 dev)
- **2 页**:`pages/store/bundle.vue`(多商品折扣 builder,AppChassis active=store + 自带 back header)·`pages/earn/earn.vue`(Earn tab,AppChassis active=earn + CardStagger;PillTabs + TotalEarnedCard 内联,7 组件组装)。
- **7 earn 组件**(`src/components/earn/*`,均 < 800 行):`device-card-pc.vue`(~430 行,源 626;header+lifecycle chip+phone 5 态[reconnecting/no-charger/no-network/waiting/task]+背景模式 battery/network demo 切换+locked-tasks loss-ad+双币收益;长按 480ms 菜单 touchstart/end;framer 进度条→CSS width transition)·`missed-income-banner.vue`(loss-aversion,双进度条,phone 条 scroll-grow 经 $el)·`device-lifecycle-banner.vue`(车队效率,动态 accent 色)·`task-lock-cumulative-banner.vue`(premium 任务锁,phase-keyed)·`market-board.vue`(价格指数 6 行 sparkline+设备排名 5 档+网络态)·`empty-slots-hint.vue`(6 槽位 hero 转化卡,aurora+10 粒子 drift+槽位网格 filled/trial/empty)·`task-center.vue`(Current/History tab,locked teasers,完成列表)。
- **新 store/util**:`cart.ts`(bundle 购物车,pinia + uni storage 持久,`BUNDLE_DISCOUNT_TIERS`/`bundleDiscountForCount`)·`task-lock.ts`(纯函数 `getTaskLockSummary`/`getInitialTaskLockSummary`,读 product-phase + server-time)。
- **app store 新增 action**:`setPhoneRuntime(id, patch)`(扩 `src/store/app.ts`,无显式返回注解;phone-only 切 isCharging/isWifiConnected/batteryLevel,device-card demo 切换用,对齐源)。types.ts 早已声明该接口,此前未实现。
- **复用现成 store**:app(devices/earnings/user/`setPhoneRuntime`)·device-types(`MAX_DEVICES`/`derivePromoUpgrade`)·device-lifecycle(`getLifecycleSummary`/`getNetworkMonthlyLoss`/`isDegradable`)·interrupt(`interruptInfo`/`INTERRUPT_MAX_RETRIES`)·free-trial(`trialReservesSlotNow`)·product-phase·server-time·products mock(`PRODUCTS`/`getProduct`)·tasks mock(`getLockedTeasers`)·ui(toast)·composables use-scroll-grow-progress。**复用现成 trial-hero-banner + trial-ghost-slot 组件**(earn 直接 import,未重写)。
- **i18n**:全 namespace **已存在零臆测零新增**(earn 60+ 键 / market / bundle 全套 / taskHistory / home.slotsHint* / trial.slotTag·ghostBadge 全现成,P-016 grep 逐键验过)。
- **pages.json**:+`pages/store/bundle` + `pages/earn/earn`(navigationStyle custom)。
- **范围决策(YAGNI,对齐已发布 uni 约定)**:① **EmptySlotsHint CTA** 直接 nav `/pages/store/store`——源开 slot-action-sheet 但 uni mock 无 inactive 设备(makeInitialDevices 只 seed active phone),sheet 唯一可达分支就是 go-store,故略 sheet+`activateDevice`(对齐已发布 add-device-row 模式);接库存激活流时换回 sheet。② **TaskCenter 收据 icon 略**——`ReceiptModal`(源 534 行独立大件)未迁,不渲染死按钮,迁 receipt-modal.vue 时接回。
- **导航目标(未建页,均 `fail:()=>{}` no-op)**:`/pages/store/detail?id=`(market 排名/locked-task unlock/upgrade)·`/pages/store/store`(missed-income/empty-slots/task-lock 早 phase/task-center teaser CTA)·`/pages/me/devices`(lifecycle/device 长按 trade-in/task-lock 晚 phase)·`/pages/me/trial`(trial-ghost buy)。
- 新坑 **P-026**(裸插值 `<view>{{x}}</view>` App 端不渲染,H5/tsc/curl 全绿,须裹 `<text>`),已转 verify.sh 哨兵 `no bare {{ }} directly in <view>`。

### market + team 域 ✅ ported(本批 · tsc 0 + 哨兵 11/11 + per-file curl-compile,实景待父重启 dev)
- **5 页**:`pages/market/market.vue`(NEX hero 价格+24h+时段段控+K线+buy/sell → 统计 grid → 上线交易所 → 5 类目 tab → token 列表[NEX 置顶] → footer;sub-page AppChassis active=home + back→home)·`pages/team/team.vue`(team tab,AppChassis active=team;V3+ royalty hero / V 级+升阶进度 / InviteEarnCard / 邀请榜 / 3-up quick-nav / TeamLedgerCard / 网络构成 / Genesis FOMO / quota+ambassador / 可视化)·`pages/team/commissions.vue`(6 类佣金事件流:总览 + 6 类汇总[点击 filter] + filter pills + 事件列表;back→team)·`pages/team/binary.vue`(Balance Match A/B:hero + 阻塞警告 + 双 wing 卡[top member+VBadge] + 强弱差距 + 自动分配 + 历史对碰)·`pages/team/agent.vue`(Regional Ambassador V5 门:hero + eligible/locked + 4 桶 + 申请表单[date/city/budget,gated submit] + 已批案例)。
- **6 组件**(均 < 800 行):`market/nex-chart.vue`(360×120 面积+折线 SVG,涨绿跌红;per-instance gradId 避 url() 冲突)·`market/token-row.vue`(star 切换+图标+symbol+价格 mini-spark+24h 变化,NEX 高亮 OURS)·`team/v-badge.vue`(**可复用** V0-V12 头衔 pill,渐变 V10+ 加 text-shadow;team+binary+agent 共用)·`team/network-orb-backdrop.vue`(2 轨 SMIL 旋转装饰球,P-013 SMIL 双端可用)·`team/team-ledger-card.vue`(月聚合大卡:24px grid + aurora blob + 5 drift 粒子[复用 v5-aurora-drift/v5-dot-drift] + direct/extended split bar + settled/cooling)·`team/invite-earn-card.vue`(单卡邀请 CTA:phase 倍率 promo + 奖励数 + 3 分享键 + 主 CTA + live ticker[nx-step-in re-key fade];复用 PulseDot)。
- **新 store**(zustand → Pinia):`network.ts`(影响力网络 50 member seed[L1-L7 + Track A/B + spillover]内联,`byLayer`/`byBinary`/`leftVolumeMonth`/`rightVolumeMonth`/`binaryMatchToday`/`vDownlineCounts`/`addSpillover`;seed 从 network-seed.ts 内联进来不单建文件)。
- **新 mock**:`mock/tokens.ts`(16 token 行情:NEX + 同类 AI/DePIN,`TOKENS`/`CATEGORIES`/`TIMEFRAMES` + seeded sparkline 生成器,对齐 lib/mock/tokens.ts)。
- **复用现成 store 零改**:v-rank(`V_RANKS`/`nextRankProgress`/`myRank`/`selfBuyUSD`…,结构兼容直传 data)·commission(`events`/`byKind`/`unlockedUSDT`/`coolingUSDT`/`monthUSDT`/`totalUSDTLifetime`/`unlockMatured`)·leadership-pool(`myVotes`/`mySharePct`/`myProjectedPayout`)·app(`user.referralCode`)·ui(toast)·use-product-phase composable(`binaryDailyCapUSD`/`inviteBonusMultiplier`)。**复用 home/pulse-dot.vue**(InviteEarnCard ticker 指示点)。
- **i18n**:全 namespace **已存在零臆测零新增**(`marketPage`/`teamV3`/`team`/`commissions`/`binary`/`agent` 含 nested `agent.buckets.{venue,kol,print,dev}.{title,rule}` 全套,en/zh 镜像,P-016 逐键 grep 验过)。**本批新增 i18n 键 = 0**。
- **pages.json**:+5 页(market/market · team/team · team/commissions · team/binary · team/agent,navigationStyle custom)。⚠️新增路由须父重启 dev(P-011)。
- **优雅降级(基建未迁,父后续接线)**:① market buy/sell CTA 源跳 `/me/wallet/exchange`(exchange 页未建)→降级 nav `/pages/me/wallet`;② InviteEarnCard **SharePoster modal 省略**(`share-poster` 组件 + `useQuest.markComplete` quest store 未迁)→分享/海报键改复制链 + toast;`navigator.share`→`uni.share?.()` + clipboard 兜底(P-028)。
- **导航目标(未建页,均 `fail:()=>{}` no-op)**:`/pages/team/unilevel`(royalty hero / 网络构成 / 自动分配)·`/pages/team/rank`(V 级卡 / agent locked path)·`/pages/team/leaderboard`(邀请榜)·`/pages/team/leadership-pool`(领导池 quick-nav)·`/pages/team/quota`(硬件配额)·`/pages/team/network`(影响力网络可视化)·`/pages/team/tree`(族谱)·`/pages/genesis`(Genesis FOMO)·`/pages/team/commissions-how`(佣金玩法说明)·`/pages/team/binary-how`(对碰玩法说明)·`/pages/me/wallet`(market buy/sell 降级)。
- 新坑 **P-027**(useState(Set)→reactive(Record),`ref(new Set())` 模板 `.has()` 不响应)·**P-028**(uni.share?.() + typeof 兜底跨端分享)·**P-029**(zustand useMemo 跨 store 聚合→Pinia 页面层 computed),见 PITFALLS。

### genesis + staking + daily 域 ✅ ported(本批 · tsc 0 + 哨兵 11/11 + per-file curl-compile 28/28 import,实景待父重启 dev)
- **7 页**(源 7 个 page.tsx 全在):`pages/genesis/genesis.vue`(创世节点预售:gold-obsidian hero[1000 席/$9,999/sales-bar shimmer/gold-dust 粒子/sheen]+ live social proof + holder perks + my holdings + live market 2-col + FAQ + OpenSea link + 折叠进页的 sticky gold dock + 购买 sheet)·`pages/genesis/holder.vue`(持有人 dashboard:hero[holdings/lifetime/today/下次分红倒计时]+ 30d bar chart + live dividend feed + holdings list + 6 perks + quick actions;myOwned=0→空态 CTA+preview banner)·`pages/genesis/marketplace.vue`(OpenSea 式二级市场:collection hero[4 stat+7d floor delta+假 OpenSea 跳转]+ listings/activity/mine 段控 tab + sort pills + 卡片网格)·`pages/genesis/how-it-works.vue`+`pages/staking/how-it-works.vue`(how-* 共享组件 + amber accent)·`pages/staking/staking.vue`(4 档 APY 锁仓:tech-money-card hero[aurora+grid+big$+delta+2col]+ vault picker 4 行[点开 sheet]+ CompoundCalculator + my positions[progress+claim/early-withdraw]+ 变动 APY notice + 折叠进页 stake sheet)·`pages/daily/daily.vue`(每日签到:streak hero[渐变+巨型🔥+签到键]+ lucky hint + 30 天里程碑路线图 + StreakPowerUps + Streak Saver + Top Streakers 榜 + 3-stat grid + 提现 context + history)。
- **19 组件**(均 < 800 行):**how/**6 件(`how-hero`/`how-section`[icon slot 内联 svg]/`how-faq-row`/`how-step-row`/`how-icon-row`/`how-callout-box`,genesis+staking how-it-works 共用)·**genesis/**7 件(`perk-row`/`nft-card`/`purchase-sheet`[v-model:open,组合 debit+purchase+bill]/`listing-card`/`activity-row`/`my-token-card`[list/cancel+confirm]/`opensea-modal`[假连接→error,onMounted/onUnmounted timer])·**staking/**4 件(`vault-row`/`position-row`[now prop 驱动,免每行 timer]/`compound-calculator`[内联段控+2 scroll-grow bar]/`stake-sheet`[v-model:open+term,组合 debit+stake+bill])·**daily/**1 件(`streak-power-ups`[daily-powerup store claim])·**`sub-page-header.vue`**(可复用 in-page 返回头,navigateBack+reLaunch 兜底,替源 SetPageHeader/chassis Header 注册;对齐 store/detail goBack)。
- **新 store**(zustand→Pinia,无显式返回注解 P-017):`staking.ts`(`STAKING_APY`/`STAKING_PENALTY`/`StakingTerm`/`positions`/`stake`/`earlyWithdraw`/`claim`/`markMatured`/`totalLocked`,uni storage 持久)·`daily-powerup.ts`(`claimed`数组+`claimedAt` Record/`claim`/`hasClaimed`,P-027 用数组+Record 非 Set)。
- **复用现成 store 零改**:genesis(`soldSlots`/`myOwned`/`ownedTokenIds`/`myListings`/`unitPriceUSDT`/`remaining`/`soldPct`/`currentDailyDividendPerNodeUSDT`/`purchase`/`listNode`/`cancelListing`/`tickSales`,**Q14 单一分红源**)·points(`signIn`/`useSaver`/`claimMilestone`/`signInStreak`/`history`/`streakSavers`/`claimedMilestones`)·app(`user`/`debitBalance`/`creditBalance`/`creditNex`)·bills(`add`,BillType 含 purchase/bonus/stake/unstake)·ui(toast/confirm)·composables use-scroll-grow-progress(P-019 $el)。
- **i18n**:全 namespace **已存在零臆测零新增**(`genesis`/`genesisHolder`/`genesisHowItWorks`/`stakingV3`/`stakingHowItWorks`/`daily`[含 milestones/saver/topStreakers/powerUps]/`marketplace`[含 openSeaErrorPool/actKind]/`points`,P-016 逐键 grep 验过)。**本批新增 i18n 键 = 0**。
- **tokens.css**:+6 个 genesis keyframe(`gen-dust-rise`/`gen-glow-drift`/`gen-sheen`/`gen-bar-shimmer`/`gen-engrave`/`gen-urgent`,P-023 共享放全局)+ `.gen-anim` reduced-motion gate。复用 `v5-aurora-drift`/`spin`/`mc-pulse`/`mc-ledger-in`/`nx-card-stagger-in`。
- **pages.json**:+7 页(genesis/genesis · genesis/holder · genesis/marketplace · genesis/how-it-works · staking/staking · staking/how-it-works · daily/daily,navigationStyle custom)。⚠️新增路由须父重启 dev(P-011)。
- **范围决策(YAGNI)**:① **lucky-spin / StreakPowerUps achievements 不迁**——daily Day-30「Lucky Spin」里程碑改非货币 unlock(toast,源 lucky-spin sheet store 出本批范围);StreakPowerUps 省 achievements badge 解锁副作用(achievements store 未迁),claim 本身保真。② **SegmentedControl 未迁**——CompoundCalculator/marketplace tab 用内联段控 pill(静态高亮,源 framer 动画高亮简化)。③ chassis 用 `active="me"`(genesis/holder/marketplace/how 从 /me 进,staking/daily 从 /me/wallet 进)。④ **haptic**→`uni.vibrateShort`。⑤ sticky dock + 所有 sheet 折叠进页(uni sub-page 无 chassis-level host),sheet 用 `position:fixed inset` + `<transition>` slide(framer→CSS)。
- **导航目标(未建页,均 `fail:()=>{}` no-op)**:`/pages/team/team`(streak royalty_boost power-up)·`/pages/me/wallet`(premium_trial power-up 降级)·`/pages/me/wallet-withdraw`(daily 提现 context — **此页已建**,真跳)。其余 streak power-up 跳已建 staking/genesis 页(真跳)。
- 新坑 **P-030**(文本叶子误用 `<view>{{x}}</view>` 系统性铺开 131 处 → Node fs 批量转 `<text class="block">`,App 端文本不渲染,单行哨兵 P-026 抓得到、多行靠 review;根因:文本叶子默认该是 `<text>` 不是 `<view>`),见 PITFALLS。

### 内容/工具页 6 页 ✅ ported(本批 · tsc 0 + 哨兵 11/11 + per-file curl-compile 11/11 import,实景待父重启 dev)
- **6 页**(源 6 个 page.tsx 全在,**无 learn/[slug] 子路由**——learn 只有 list page):`pages/search/search.vue`(全局搜索:输入框实时过滤静态路由/FAQ 目录 + live store 商品/用户设备/network 成员,分组结果。源 useMemo→computed,grouped 用有序数组非 Object.entries;路由 href 映射 uni 页路径,未建页 `fail:()=>{}` no-op)·`pages/developer/developer.vue`(开发者 Hub:hero + 4-tab 段控[Overview/Docs/Keys/Webhooks]+ 4 API 卡 + partner 网格 + 申请表单[3 input→toast]+ docs 代码 preview/empty 态。源 SegmentedControl→内联 pill tab;源 banned hex `#3DA9FF`/`#0F0F0F`→`var(--v5-tech-cyan)`+surface token)·`pages/globe/globe.vue`(全球节点图:2 stat tile + 内联 SVG dotmap[程序化大陆点 + region 节点光晕 + SMIL pulse ring + 连接线]+ region 卡列表 + position:fixed 抽屉。P-013 SVG/SMIL 照搬;源点 hex `#1F2D1A`/`#101418`→`var(--v5-brand)` 低透/`var(--v5-ink-4)`;Math.random pulse/uptime 保留[uni 无 SSR 无 hydration 顾虑])·`pages/learn/learn.vue`(Academy:featured hero + Your Learning stats + 6 类目 tab + lesson 卡列表 + footer)·`pages/missions/missions.vue`(任务中心 hub:hero + 6 段 RouteRow[Today/Week/Month/Day-One/Events/Achievements])·`pages/events/events.vue`(活动中心:featured hero + 5-tab 段控 + event 卡列表,join→claim 流)。
- **5 组件**(均 < 800 行):**learn/**3 件(`learn-featured-hero`/`learn-stats-card`/`learn-lesson-card`,各带独立 scroll-grow bar 实例,故拆组件)·**events/**2 件(`events-featured-hero`/`events-card`,状态驱动 status chip + 多分支 CTA,各带 scroll-grow bar)。
- **新 store**:`event-quest.ts`(zustand persist→Pinia + uni storage,**joined/claimed 用 `reactive<Record<string,boolean>>` 非 Set** P-027,join/claim 返回是否新增,uni storage 持久,key 带 v1 版本号)。
- **新 mock**:`globe-regions.ts`(6 region 0-1 坐标,零依赖 cp)·`learn.ts`(15 lesson + 5 类目,tint hex 为数据值非 token)·`events.ts`(10 event,源 evaluateEventProgress evaluator 内联为 `trackable`/`done` flag + rewardNEX 映射,自包含)。
- **复用现成 store/composable 零改**:app(`global.activeDevices`/`todayIncrement`/`devices`/`creditNex`) · network(`members`,search 用) · bills(`add`,events claim 写 achievement bill) · ui(toast) · composables use-scroll-grow-progress(P-019 $el 版,learn/events 卡进度条) · `card-stagger`/`sub-page-header`/`fmt`。
- **i18n**:6 namespace **全已存在**(`search`/`developer`/`globe`/`learn`/`events`/`missions`,P-016 逐键 grep 验过)。**仅新增 2 键**:`missions.weekLabel`/`missions.weekValue`(en/zh 镜像,因源 Week 段嵌 live WeeklyQuestHero 组件,本批降级为 RouteRow 需短文案)。
- **pages.json**:+6 页(missions/missions · events/events · learn/learn · globe/globe · developer/developer · search/search,navigationStyle custom)。⚠️新增路由须父重启 dev(P-011)。
- **范围决策(YAGNI)**:① **missions hub 降级**——源「This Week」嵌 live `WeeklyQuestHero`/`WeeklyQuestList`、「This Month」嵌 `MonthlyChallengeCard`、Day-One 算 quest phase;这些 quest 子系统(quest/event-quest 外的 monthly/weekly store + 组件)未迁,故 Week/Day-One 降级为 RouteRow 指向目的页,Month 显 i18n「launches soon」卡。**但 Events 行 stat 是真的**(ongoing/joined/claimable 由 EVENTS mock + event-quest store 实算)。② **events lucky-wheel sheet 不迁**(无 lucky-spin store)→ wheel 事件 CTA 优雅 no-op;**achievements unlock on claim 省略**(achievements store 未迁),claim 本身(creditNex+bill)保真。③ **SegmentedControl 未迁**——developer/events tab 用内联 pill 段控(对齐 earn.vue PillTabs)。④ chassis active:search/missions/events=`home`(从 Home 进),developer/globe/learn=`me`(从 /me 进)。⑤ globe 抽屉 position:fixed 覆盖整 chassis;SMIL pulse `<circle>` 带 `:key` 含 pulseTick 强制重触发。
- **导航目标(未建页,均 `fail:()=>{}` no-op)**:search 路由目录里 `/pages/me/devices`·`/pages/team/unilevel`·`/pages/team/network`·`/pages/me/goals`·`/pages/me/risk-disclosure`·`/pages/me/achievements`(missions Achievements 行)。已建页(store/detail·earn·store·team·market·wallet·staking·genesis·daily·learn·events·developer·globe·missions)真跳。
- **无新坑**:本批 0 新增 PITFALLS(P-019 $el / P-025 闭合 / P-026·P-030 文本叶子 / P-027 reactive Record / P-013 SVG-SMIL 全为既有规则,写时即遵守,哨兵 11/11 一次过)。

### me 子页 batch 1（devices/profile/security/kyc/goals）✅ ported(本批 · tsc 0 + 哨兵 11/11 + per-file curl-compile 8/8 import,实景待父重启 dev)
- **5 页**(源对应:devices/profile/security/security/kyc-express/goals,**`me/kyc` 在源是 `me/security/kyc-express`**——uni 无嵌套 [seg]/kyc-express 路径,平铺为 `pages/me/kyc`,back→`/pages/me/security`):`pages/me/devices.vue`(设备仓库:slot meter 6 段 + 试用 shadow 行[cancel] + Active/Inventory 两段[activate/deactivate]+ 空态 + go-store CTA + 运行中任务走 DeactivateSheet)·`pages/me/profile.vue`(改 display name/bio/region/timezone[`<picker>`]+ 头像 reroll + tier bar + 钱包绑定链 + dirty-gated save)·`pages/me/security.vue`(改密折叠表单 + 2FA toggle + KYC-Express 链 + 活跃会话[revoke/revoke-all]+ 危险区删账)·`pages/me/kyc.vue`(KYC-Express 说明:how-* 组件 6 段 + 触发/提供/隐私/解锁/伙伴/FAQ + start CTA→wallet-topup?kyc=1)·`pages/me/goals.vue`(收益目标:hero + 目标/期限 setter[4 preset 各]+ 推荐档 + save + 活跃目标列表[每行 scroll-grow bar])。
- **3 me 组件 + 复用 how/***:`components/me/device-inventory-row.vue`(单设备卡 activate/deactivate 行,源 DeviceRow)·`components/me/device-deactivate-sheet.vue`(运行中任务 wait/force/cancel 底部 sheet,源 chassis-store 驱动→page `:device` prop + emit,因 uni 无 root layout P-004;本地 nowMs setInterval + onUnmounted P-021;framer→`nx-sheet-fade-in`/`nx-sheet-slide-up` keyframe)·`components/me/goal-progress-bar.vue`(每行独立 IO scroll-grow bar,`ref="elRef"` 对齐 vrank-card 约定)。kyc 复用现成 `how/how-hero·how-section·how-callout-box·how-step-row·how-icon-row·how-faq-row`(icon slot 内联 svg `stroke="currentColor"`,对齐 staking/how-it-works)。
- **app store 新增 device-CRUD ✅**(扩 `src/store/app.ts`,无显式返回注解 P-017):`addDevice(kind)`→id · `activateDevice(id, reservedSlots=0)`→boolean · `deactivateDevice(id)` · `scheduleDeactivation(id)`,从源 index.ts 459-548 保真移植(零 telemetry/pendingDeactivate/MAX_DEVICES cap)。**关键:slot-cap 用 `reservedSlots` 参数由页面组合 trial-reserved 数**,保 MAX_DEVICES 在 store 权威 + 不让 app.ts import trial store(架构铁律)。**→ orders.advanceOrder 的 `DeviceSpawnApp` 探测现已点亮**(订单进 activated 真 spawn+activate Device)。
- **2 新 store**(zustand persist→Pinia + uni storage):`security.ts`(`passwordChangedAt`/`twoFactorEnabled`/`sessions`[3 mock] + changePassword[server 持 hash,void 参]/setTwoFactor/revokeSession/revokeAllOthers,key v1)·`goals.ts`(`goals[]` + setGoal/markAchieved/remove/clear,key v1)。
- **复用现成 store/util 零改**:app(`devices`/`user.email`/`user.tier`/`user.joinedAt`/`earnings.total` + 新 device-CRUD)·auth(`email`/`signOut`)·profile(`displayName`/`bio`/`region`/`timezone`/`setDisplayName`等/`regenerateAvatar`)·wallet-pairing(`walletPaired`/`pairedWalletAddress`)·free-trial(`status`/`cancel`)·trial-config(`config.cooldownDays`)·device-types(`MAX_DEVICES`)·auth/password-rules(`isPasswordOk`/`PASSWORD_MAX_LENGTH`)·ui(confirm/toast)·composables use-scroll-grow-progress(P-019 $el)。
- **跨 store 编排在 handler 组合**(store 互不 import):删账=`auth.signOut`+reLaunch login(源 /api/gate/logout reviewer-cookie 省略,uni 无 middleware);取消试用=`trial.cancel("explicit")`+toast;profile 保存=4×profile.set*(源 quest markComplete 省略,quest store 未迁)。
- **i18n**:全 namespace **已存在零臆测零新增**(`myDevices`[inventory* 全套]/`deactivateSheet`/`profile`[tierLabels]/`security`/`kycExpress`[triggers/steps/privacy/unlock/trust/faqs 全 nested]/`goals`/`trial`[ghostBadge/deviceRowSub/cancelConfirm*/toastCancelled]/`ui`,P-016 grep 验过;zh.ts 经 `Messages=typeof en` 类型强制镜像,vue-tsc 0 即证齐全)。**本批新增 i18n 键 = 0**。
- **tokens.css**:+2 sheet keyframe(`nx-sheet-fade-in`/`nx-sheet-slide-up` + `.nx-sheet-*` 工具类 + reduced-motion gate,P-023 共享放全局,供 DeviceDeactivateSheet + 未来 sheet 复用)。
- **pages.json**:+5 页(me/devices·me/profile·me/security·me/kyc·me/goals,navigationStyle custom)。⚠️新增路由须父重启 dev(P-011)。
- **范围决策(YAGNI)**:① **TradeInPromoBanner 省略**(devices 页)——源由 `DEFAULT_TRADEIN_CONFIG.promo`(kill switch/cooldown,未迁)gate + 跳 trade-in checkout(出本批);config+flow 落地时回归。核心库存管理(activate/deactivate/slot meter/trial 行/空态)全保真。② **MechAvatar 省略**(profile)——uni 无 SVG 头像生成器,用 me 域 profile-row.vue 既有的首字母头像约定。③ `<select>`→`<picker mode="selector">`(profile region/timezone,对齐 topup-card-form)。④ security 源 iOS 分组列表原语(IOSList/IOSToggle 硬编码 `#1C1C1E`+text-white)重建为 me 域 token surface,结构同、token 纪律;iOS toggle 旋钮 `#FFFFFF`(硬件复刻,两主题皆白,非 banned hex)。⑤ goals 源 hero `#14130A`/`#0F0F0F` 渐变→token(`var(--v5-surface)` + warning 径向),on-warning 文字用 `var(--v5-on-brand)`(项目约定,对齐 task-lock-cumulative-banner)。⑥ recycle/replace/moveToInventory(trade-in 三动作)本批不迁——devices 页只用 activate/deactivate/scheduleDeactivation;trade-in Batch 再补。
- **导航目标(未建页,均 `fail:()=>{}` no-op)**:无(本批 5 页互相打通 + 跳已建页:devices→store/store·me/me;profile→me/wallet·me/me;security→me/kyc·login;kyc→me/wallet-topup·me/security;goals→store/store·me/me)。
- **无新坑**:本批 0 新增 PITFALLS(P-004 chassis 无 root layout→sheet page-mount / P-017 setup store 无返回注解 / P-019 $el / P-021 组件 onUnmounted / P-023 共享 keyframe / P-025 闭合 / P-026·P-030 文本叶子 / P-016 i18n grep 全为既有规则,写时即遵守,哨兵 11/11 一次过)。**正向技法**:slot-cap `reservedSlots` 参数化(store 权威 + 页面组合,不破 store-互不-import);chassis-less 底部 sheet 用 `:device` prop + emit 替 chassis-store 驱动。

### me 子页 batch 2（wallet/records:bills/exchange/nex/cards/cards-new/receipts/proof/wrapped）✅ ported(本批 · vue-tsc 0 + 哨兵 11/11 + per-file curl-compile 8 页 + 5 组件 + 3 store 全 import,实景待父重启 dev)
- **8 页**(源全在):`pages/me/wallet-bills.vue`(交易账本:段控 All/Credit/Debit + 按月分组列表[11 类型图标 + memo + ref + running balance],源 SegmentedControl→内联 pill)·`pages/me/wallet-exchange.vue`(NEX↔USDT swap:pay/receive 卡 + flip + free fee + **v3 风控面板**[$50/天用户上限 + $20K/天平台池 + $100 终身 KYC 门 + 排队] + 历史,confirm 门控→debit/credit + recordSwap + 双 bill + v3.record)·`pages/me/wallet-nex.vue`(NEX 持仓详情:hero[余额+USD+24h+sparkline]+ 4 quick action + position breakdown + P&L + Use-NEX 4grid + 近期活动[mining+commission])·`pages/me/wallet-cards.vue`(绑卡列表:卡行[default badge + set-default/unbind]+ 空态 + add CTA + PCI 披露)·`pages/me/wallet-cards-new.vue`(绑卡表单:PAN[brand 检测]/expiry/CVV/holder + default toggle + ?trial=1 自动扣款披露,提交 add→redirect 列表)·`pages/me/receipts.vue`(Proof-of-Compute 列表:横滑类别 tab[8 类带计数]+ clear-all + 行→ReceiptModal,长按复制签名 P-028)·`pages/me/proof.vue`(分享 hub:3 variant tab[earnings/streak/network]+ 分享卡[VBadge+QR+ref code]+ native-share + 6 目的地 + tip)·`pages/me/wrapped.vue`(年度回顾 6 卡故事流:tap 推进 + 左右 tap 区 + 进度条,framer AnimatePresence→`nx-story-in`,全屏 overlay 锚 chassis content)。
- **5 新组件**:`components/me/bill-type-icon.vue`(11 BillType 内联 SVG,替 lucide)·`components/me/receipt-cat-icon.vue`(7 ReceiptCategory 内联 SVG)·`components/me/receipt-modal.vue`(收据详情 sheet,源 535 行 ReceiptModal→通用 `sections` 描述符渲染[Row/RowCopy/Section/DetailsRows 折叠成 row descriptor];scrim+`nx-step-in`;copy 用 setClipboardData;P-032 page-mount + prop/emit)·`components/me/nex-sparkline.vue`(NEX hero area+line sparkline)·`components/me/proof-sparkline.vue`(proof earnings 上涨 sparkline)。复用 `team/v-badge.vue`(proof V 级徽章)。
- **3 新 store**(zustand persist→Pinia setup + uni storage,无返回注解 P-017):`exchange.ts`(`history`/`rate`/`rateUpdatedAt` + refreshRate/recordSwap,jitter 0.085 ± 0.01,key nexion-exchange-v1)·`exchange-v3.ts`(风控:`todayUserUsedUSD`/`todayPlatformUsedUSD`/`lifetimeExchangedUSD`/`kycVerified`/`queue` + resetIfNewDay/canExchange→Gate/record/enqueue/setKycVerified + USER_DAILY_CAP_USD 50/PLATFORM 20K/KYC 100 常量 + dailyUserPctUsed/dailyPlatformPctUsed,key nexion-exchange-v3)·`cards.ts`(`cards[]`/`defaultTokenId` + add/remove/setDefault/getCard + detectBrand/brandLabel,key nexion-cards-v1,**PAN/CVV 永不存只留 last4/brand/expiry/holder**)。
- **复用现成 store 零改**:app(`user.usdtBalance`/`nexBalance`/`referralCode`/`joinedAt` + creditBalance/debitBalance/creditNex/debitNex + devices/earnings.total)·bills(`add`,已迁)·receipts(`receipts`/`clear`/filterByCategory,已迁)·market(`nexPriceUSDT`/`change24hPct`/`klineHourly`/`tickPrice`)·commission(`events`[sourceUserName/kind/amountNEX]/`totalUSDTLifetime()`)·profile(`displayName`)·v-rank(`myRank`/`V_RANKS`)·network(`totalMembers`)·points(`signInStreak`/`longestStreak`)·wallet-pairing(`walletPaired`)·trial-config(`config.trialDays/trialPriceUSD/trialOffsetCapUSD`)·ui(confirm/toast)。mock/receipt(Receipt/ReceiptCategory/ReceiptDetails/shortenHex,已迁)。
- **跨 store 编排在 handler 组合**(store 互不 import):exchange 确认=`v3.canExchange`门→`app.debit/credit`+`exchange.recordSwap`+`bills.add`×2+`v3.record`(对齐源,纯函数门留 v3 store 权威);nex 活动=页面 computed 聚合 commission.events + mining mock(P-029);proof/wrapped 多 store 派生全在页面 computed。
- **i18n**:**新增 1 namespace 扩展 = exchange 域 +21 键**(v3 风控面板 + 门控弹窗/toast:capsLabel/capsReset/yourDaily/kycVerified/kycUnverified/lifetimeLabel/queuedLabel/kycRequired*/capReached*/queuedToast*/platformExhausted*,源页**硬编码英文**故补 t.* + fmt;en/zh 镜像)。其余全既有零臆测(bills/nexWallet/cards/receipt/proof/wrapped/walletV3.exchangePoolToday,P-016 grep 验过;vue-tsc 0 即证镜像齐)。
- **tokens.css**:+1 keyframe(`nx-story-in` opacity+translateX 入场 + reduced-motion gate,P-023 wrapped 故事卡用,keyed on idx 复播)。`nx-step-in`/`spin` 复用既有。
- **pages.json**:+8 页(me/wallet-bills·me/wallet-exchange·me/wallet-nex·me/wallet-cards·me/wallet-cards-new·me/receipts·me/proof·me/wrapped,navigationStyle custom)。⚠️新增路由须父重启 dev(P-011)。**wallet.vue 既有的 wallet-nex/wallet-cards/wallet-exchange/wallet-bills 链现真跳**。
- **范围决策(YAGNI / 优雅降级)**:① **exchange scroll-grow cap bar 省略**(源 `useScrollGrowProgress`+IO `$el`)→cap bar 直接渲染最终宽度(P-018 先例,省一个 composable + IO 不破 P-019);② **cards trial-unbind-sheet 降级**——源 trial 卡解绑开 retention sheet(`trial-unbind-sheet` store 未迁)→降级普通 confirm+remove(CRUD 保真,sheet 出本批);③ **wrapped overlay 锚 chassis content**——源 `z-[110]` 盖 chassis header,uni overlay 在 `.nx-content`(position:fixed)内填充滚动区,chassis 品牌头/tabbar 保留(对齐 uni sub-page 约定,SubPageHeader 同模式);④ **proof/wrapped 渐变字面量→token color-mix**(源 `rgba(198,255,58,..)`/`#131508` 等暗底渐变→`color-mix(var(--v5-brand/brand-2/warning/tech-cyan) N%, var(--v5-on-brand))`,on-brand 文字用约定);⑤ **品牌资产 hex 保留字面**(bill refer #86E81F·topup #3DA9FF / proof 目的地 Twitter #3DA9FF·WhatsApp #25D366·Instagram #E4405F / QR #ffffff / scrim·shadow rgba(0,0,0,..)——同 card-brand 约定,非 banned hex,sentinel 验过);⑥ **receipt-modal 通用化**——源 8+ 小组件(Row/RowCopy/Section/DetailsRows/LossCurve/KycSections)折叠为 `sections` 描述符数组渲染(loss_curve mini-svg 省略,FT 只留 steps/loss);⑦ **how-it-works 入口**(exchange)指 `/pages/me/wallet-exchange-how`(未建,fail no-op)。
- **导航目标(未建页,均 `fail:()=>{}` no-op)**:`/pages/me/wallet-repurchase`(nex re-invest / Use-NEX reinvest)·`/pages/me/wallet-exchange-how`(exchange how-it-works 入口)·`/pages/me/wallet-topup?kyc=1`(exchange KYC 门 — **此页已建**真跳)·donate(`#` no-op)。其余跳已建页真跳(exchange/staking/genesis/market/bills/me)。
- **@input typing 复用**:uni `<input>` `@input` vue-tsc 判 DOM `Event`,直译 `(e:{detail:{value}})` 报 TS2322 → 用既有 `detailVal(e: Event)` 助手(`(e as unknown as {detail:{value:string}}).detail.value`,对齐 topup-card-form)。**非新坑**(既有约定,但首次在本线踩,记此备查)。
- **无新坑**:本批 0 新增 PITFALLS(全既有规则写时遵守:P-016 i18n grep / P-017 无返回注解 / P-018 省 scroll-grow / P-020 import type / P-021 页面 interval onUnmounted / P-022 透明度 color-mix / P-023 共享 keyframe→tokens.css / P-025 闭合 / P-026·P-030 文本叶子 `<text>` / P-027 无 Set/Map state / P-028 share/clipboard / P-029 跨 store 页面 computed 聚合 / P-032 page-mount sheet prop+emit,哨兵 11/11 一次过)。**正向技法**:v3 风控门(canExchange→Gate union)纯函数留 store 权威,门控弹窗在页面 handler 组合;受多组件复用的 535 行 modal 折叠为 descriptor 数组渲染(DRY,易维护);v-html 内联 SVG 字符串(login/register 既有约定,tint 函数 replace stroke)做大量同形图标。

## Batch 2（team 子页全收口:rank/leaderboard/leadership-pool/network/quota/tree/unilevel + 4 how-it-works）
- **12 页全在源**(`app/(main)/team/*` 除已迁 team/commissions/binary/agent 外全部):`pages/team/rank.vue`(V 级 hero[VBadgeIcon48 + scroll-grow 进度条 + missing 列表 + upgrade CTA→/store] + 13 阶 ladder[VBadgeIcon36 + 条件 + perk chips])·`pages/team/leaderboard.vue`(奖池 hero + 4 段控[today/week/month/all] + 我的位次卡 + 领奖台[#2/#1/#3,#1 抬高] + 奖励行 + #4-100 grid 列表 + 分享 CTA;源 SegmentedControl→内联 pill[events.vue 模式];**LEADERBOARD mock 移植** src/mock/leaderboard.ts[mulberry32 seed,100×4 period,verbatim 无 React])·`pages/team/leadership-pool.vue`(周池 hero + 我的状态[unlocked 分红/locked V3 门+path CTA] + V 级票数表 + 历史分红;复用 leadership-pool store[V_VOTES/totalVotes/myVotes/mySharePct/myProjectedPayout/nextPayoutTs/currentWeekPoolUSDT/history])·`pages/team/network.vue`(2 轨道 orb:metrics + SVG[轨道导线 + 连接线 + member 节点 random pulse + center YOU + 标签] + legend + footer + member 详情 sheet[tap 节点];P-034 page onMounted/onUnmounted pulse interval,P-032 page-mount sheet ref,P-013 SVG `<animate>` 渲染,globe.vue `<g @click>` 节点 tap 模式)·`pages/team/quota.vue`(硬件配额:active 邀请 hero + 2 QuotaTierCard + invite CTA;condition `kind` flag 替 locale 字符串匹配)·`pages/team/tree.vue`(族谱:2 metric + Direct/Extended 折叠 section[TeamRosterSection];P-027 reactive(Record) 替 useState(Record))·`pages/team/unilevel.vue`(partner-program:月版税 hero + Direct Royalty 卡 + Network Yield Bonus 卡 + Partner Status 4 档进度 + filter pills + member 列表;TickerNumber→直接渲染[entrance tween 略,值 store-derived 非 interval])·`*-how.vue`×4(rank/unilevel/leadership-pool/binary/commissions how-it-works:复用 how/* 组件[HowHero/HowSection/HowStepRow/HowIconRow/HowCalloutBox/HowFaqRow];rank-how/binary-how 用 page-local Section[brand/brand-2 code-tag 头]+ 自定 radial hero + binary-how SVG TreeDiagram[P-013])。
- **4 新组件 + 1 mock**(均 < 800 行):`components/team/v-badge-icon.vue`(方形 V 级数字 chip,源 VBadgeIcon;rank/rank-how/leadership-pool 用)·`components/team/quota-condition-bar.vue`(单条件 scroll-grow bar,源 ConditionBar——hook 不能在 v-for callback,**独立组件各自 $el** P-019)·`components/team/quota-tier-card.vue`(单 gated tier 卡,自持 stock bar scroll-grow,emit navigate)·`components/team/team-roster-section.vue`(折叠 Direct/Extended 名册,源 RosterSection+MemberRow,contribution=月vol×UNILEVEL_USDT[layer])·`src/mock/leaderboard.ts`(verbatim port,backend-replaceable)。
- **复用现成 store 零改**(本批 0 新 store):v-rank(`V_RANKS`/`nextRankProgress`/`myRank`/`selfBuyUSD`/`directRefs`/`teamVolumeUSD`/`vDownlineCounts`,结构兼容直传)·network(`members`/`byLayer`[layer/status/monthVolumeUSD/totalVolumeUSD/vRank])·leadership-pool(`V_VOTES`/`globalVDistribution`/`totalVotes`/`myVotes`/`mySharePct`/`myProjectedPayout`/`nextPayoutTs`/`currentWeekPoolUSDT`/`history`/`LeadershipPayout`)·commission(`UNILEVEL_USDT`)·useScrollGrowProgress(P-019 $el 版,rank/quota 进度条)。**复用 how/* 6 组件 + sub-page-header + v-badge(VBadge)**。
- **i18n**:**0 新增 key,全既有零臆测**——en.ts 已有全部 team how-it-works namespace(rank/rankHowItWorks/unilevel/unilevelHowItWorks/binary/binaryHowItWorks/pool/poolHowItWorks/commissions/commissionsHowItWorks/leaderboard/quota/network/tree)。P-016 grep 验过每个 namespace key 列表;vue-tsc 0 即证 en/zh 镜像齐 + key 名对。
- **去 MLM 保真**:源已去 MLM(unilevel→"Influence Network Royalty"/binary→"Balance Match"/wing→"track"/spillover→"REFERRED-IN"/auto-placement),全部保留;meta/MLM 哨兵 **0 hit 绿**(无 unilevel/spillover/binary-leg/upline/downline 漏进用户文案;namespace KEY 名如 binaryHowItWorks 是内部标识符非用户可见,不触哨兵)。
- **pages.json**:+12 页(team/rank·rank-how·leaderboard·leadership-pool·leadership-pool-how·network·quota·tree·unilevel·unilevel-how·binary-how·commissions-how,navigationStyle custom)。⚠️新增路由须父重启 dev(P-011);**team.vue/commissions.vue/binary.vue 既有的这些链现真跳**。
- **AppChassis 约定**:全部 team 子页(含 how-it-works 深页)用 `<AppChassis active="team">`(组件 `active` prop 必填,无"no-active"形态;对齐已迁 commissions.vue),team tab 高亮。back→navigateBack w/ reLaunch fallback 到对应父页(rank-how→/team/rank 等;主 hub 子页→/team/team)。
- **范围决策(YAGNI / 优雅降级)**:① **network/binary-how SVG `<text>` 内节点**保留为 SVG text(非 uni `<text>`,P-013 webview 渲染);② **unilevel TickerNumber→直接 `.toFixed(2)`**(源 700ms entrance tween 是纯入场动画,值由 store computed 派生非 interval 驱动,SKILL cookbook 允许略纯入场动画;use-ticker 是 drift 语义不符,故不误用);③ **quota condition `.includes("volume")` 字符串匹配→`kind:"invites"|"volume"` flag**(跨 locale 稳健);④ **rank-how/binary-how 自定 radial hero 保留**(非 HowHero aurora 组件,源即自定);⑤ **dark base hex→token**:`#0F0F0F`/`#11131A`/`#06080C`/`#0E0E0E`/`#15131F`/`#0F140A`→`var(--v5-surface)`/`var(--v5-bg)`,`#3A3A3A`(offline 节点)→`var(--v5-ink-4)`;⑥ **`${color}NN` alpha-hex→`color-mix(... N%, transparent)`**(P-022,全 tier/chip/badge 透明底);⑦ **medal hex 保留字面**(`#FFC83D`/`#C9D2DC`/`#C77546` 金银铜 + v-badge-icon 渐变档 text hex——intrinsic 非 theme token,非 banned,sentinel 验过);⑧ **quota 购买 CTA→`/pages/store/detail?id=`**(uni detail 用 `?id=`,源 `/store/${productId}` 动态段;product id stellarbox-pro/stellarrack-p1 mock 中存在,真跳)。
- **导航目标(均真跳或 `fail:()=>{}` no-op)**:rank upgrade→`/pages/store/store`(真跳)·quota tier→`/pages/store/detail?id=`(真跳)·leadership-pool/rank/leaderboard/quota 各 CTA→`/pages/team/team`/`/pages/team/rank`(真跳)·各 how-it-works 入口/footer→对应主页(真跳,本批全建齐)。
- **无新坑**:本批 0 新增 PITFALLS(全既有规则写时遵守:P-011 路由重启 / P-013 SVG / P-016 i18n grep[本批 0 新 key]/ P-019 scroll-grow $el[独立组件]/ P-020 import type / P-022 alpha→color-mix / P-027 reactive(Record) / P-032 page-mount sheet / P-034 page interval onMounted-onUnmounted。**注释纪律 P-012/P-015**:首跑哨兵 `useState` 误命中本批注释里"useState→ref"字样[非真代码],已按注释纪律改为中性词"React local state→ref",二跑 11/11 绿)。**正向技法**:hook-per-iteration 不能在 v-for callback→抽独立子组件各持 $el(quota-condition-bar);受多形态复用的折叠名册抽 TeamRosterSection;LEADERBOARD seeded mock verbatim port(零 React 依赖即 backend-replaceable)。
- **验证**:vue-tsc **0**;`bash scripts/verify.sh` **11/11 PASS**(含 meta/MLM 哨兵绿);**每新 .vue curl-compile**`http://localhost:5173/src/pages/team/<x>.vue`→`import{creat…}`(12/12 页 + 4 组件 + mock 全 import,**0 个 500/`<!DOCTYPE`**)。

### 收官批（剩余 20 路由全迁 — me 子页 + trust + ref + tx）✅ ported(本批 · vue-tsc 0 + 哨兵 11/11 + per-file curl-compile 20 页 + 17 组件 + 6 mock/store 全 import,实景待父重启 dev)
- **20 页（源剩余全在，迁移收官）**:`me/achievements`·`me/help`·`me/support`·`me/support/tickets`→`me/support-tickets`·`me/language`·`me/notifications`·`me/preferences`·`me/replay-tour`·`me/risk-disclosure`·`me/trial`·`me/wallet/premium`→`me/wallet-premium`·`me/wallet/nex-v2-lock`→`me/wallet-nex-v2-lock`·`me/wallet/repurchase`→`me/wallet-repurchase`·`me/wallet/repurchase/how-it-works`→`me/wallet-repurchase-how`·`me/wallet/exchange/how-it-works`→`me/wallet-exchange-how`·`me/wallet/withdraw/tracking`→`me/wallet-withdraw-tracking`·`trust`→`trust/trust`·`trust/nex`·`ref/[code]`→`ref/code?code=`·`tx/[hash]`→`tx/hash?hash=`(深路径平铺为文件名,动态段→query)。
- **17 新组件**(均 < 800 行):**trust/**4 件(`trust-section-header`/`trust-stat`/`trust-doc-row`/`nex-anchor-section`[读 market FDV/volume])·**tx/**2 件(`tx-stat`/`tx-row`)·**me/**11 件(`preference-toggle-row`/`nex-lock-stat`/`repurchase-row`/`ticket-stat-box`/`ticket-row`/`replay-config-number-row`/`replay-config-toggle-row`/`replay-demo-action`/`trial-countdown-hero`/`trial-time-cell`/`trial-discount-row`)。trust(550 行)/support-tickets(469)/trial(521)/replay-tour(642)按子组件拆分控制单文件行数。
- **3 新 store**(zustand→Pinia setup + uni storage,无返回注解 P-017):`preferences.ts`(sound/haptics/notifPrefs Record,key v1)·`achievements.ts`(records + unlock/claim,key v1)·`notifications.ts`(items/unread + push/markRead/markAllRead/clearRead/removeOne + 优先级保留 applyPriorityRetention,seed 空,key v1)。
- **3 新 mock**(零框架依赖直接 cp):`faq.ts`(15 FAQ + botReply,**meta 注释删除**避哨兵)·`achievements.ts`(6 成就 def verbatim)·`tickets.ts`(7 工单 + CATEGORY/STATUS_LABEL,**STATUS_COLOR 字面 hex→v5 token** 主题感知)。
- **复用现成 store/util/组件零改**:how/* 6 件(exchange-how/repurchase-how/trust-nex 三 how 页)·SubPageHeader·CardStagger(trust)·app(user/global/devices/earnings + creditNex/creditBalance/debitBalance/submitWithdrawal/advanceWithdrawal/latestWithdrawal/addDevice/activateDevice)·bills(add)·points(points/earn)·staking(stake)·market(nexPriceUSDT/volume24hUSDT/circulating)·free-trial(全 helper liveShadowUSD/NEX/remainingMs/canStart/startWithCard/redeemEarly + boundary refs)·trial-config(config/update/reset + computeDiscountedPrice/computeTrialOffset)·cards(cards/defaultTokenId/brandLabel)·trial-claim-sheet(show/resetCooldown)·risk-disclosure(accepted/accept)·locale(code/setLocale)·use-product-phase composable(premiumSubscriptionAvailable/nexV2LockAvailable)·useProductPhaseOverride(pinned/setPinned)·safeReturnTo·sponsors mock(pickSponsor)·i18n(LOCALES/PRIORITY_LABELS/localesByPriority)·ui(toast/confirm)。
- **跨 store 编排在 handler 组合**(store 互不 import):repurchase = `app.debitBalance` + `points.earn` + `staking.stake` + `bills.add`;trial 早购 = `redeemEarly` 门 → `app.debit/credit/creditNex/addDevice/activateDevice` + `bills.add`×N(charge-disclosing `confirm()` 二次确认守卫,对齐 Model A 资金规则);achievements claim = `ach.claim` 门 → `app.creditNex/creditBalance` + `bills.add`;risk accept = `risk.accept` + nav returnTo。
- **i18n**:全 namespace **已存在**(`exchangeHowItWorks`/`repurchaseHowItWorks`/`nexHowItWorks`/`language`/`preferences`/`support`/`help`/`achievements`/`notifs`/`replay`/`riskDisclosure`/`trial`/`premium`/`nexV2Lock`/`repurchase`/`tickets`/`trust`/`ref`/`tx`/`wallet`,P-016 grep 验过)。**仅补 9 键 ×2(en/zh 镜像)**:`help.botUnmatched`(bot 无匹配回退,源硬编码英文)+ `wallet.trackAmountLabel`/`trackProgressLabel`/`trackViaLine`/`trackEtaDone`/`trackEtaPending`/`trackReviewNote`/`trackBackToWallet`(withdraw-tracking 源硬编码英文 "Amount"/"Progress"/"Back to Wallet" 等)+ `tickets.backToTickets`(源三元 "Back to tickets")。vue-tsc 0 即证 zh 镜像齐(`Messages=typeof en`)。
- **pages.json**:+20 页(navigationStyle custom)。⚠️新增路由须父重启 dev(P-011)。**me.vue/wallet.vue/trust 各处既有的这些链现真跳**。
- **范围决策(YAGNI / 优雅降级)**:① **notifications swipe→tap 降级**——源用 gesture `SwipeRow`(swipe 删除/标记/转化捷径),uni 无 swipe 原语 + 该 feed seed 空(事件由 simulation push,本端只迁读+管理 UI),行降级为 tap-mark-read + ctaHref nav,转化捷径以 ctaLabel chip 呈现(store 保留 push/removeOne 备 seeding)。② **replay-tour dev-only**——`import.meta.env.PROD` gate(对齐源 NODE_ENV;prod reLaunch /me);trial-config 面板 + phase-cycle **全实装**(trial-config/product-phase-override/trial-claim-sheet store 现成);seed-legacy/fast-forward/reset/milestone/Stella-push/extension-sheet 6 个 demo 动作依赖未迁的 `_dev*` app helper + milestones/trial-extension-sheet store + stella-templates mock,降级为说明性 toast(不静默 no-op)。dev 工具中文 label 保留源字面(prod-gated,非用户可见生产文案,哨兵只查 meta/ponzi 词非泛中文,绿)。③ **trust/ref 外链全 href="#"→no-op/复制**(institutional façade 链接本就 theatre);tx Etherscan/TRONScan 外跳源 `target=_blank`,uni 无新窗→复制 hash + toast。④ **品牌资产 hex→token**:`#3DA9FF`(Telegram)/`#0A66C2`(LinkedIn)/`#86E81F`/`#FFC83D`(状态色)/ref 暗底渐变 `#0F0F0F` 全映射 `var(--v5-*)` + color-mix,无 banned hex 残留(grep 验过仅 `#FFFFFF` 硬件开关旋钮 + `rgba(0,0,0)` 阴影)。⑤ **trial CountdownHero/TimeCell/Row 拆组件**(521 行控行数);**1s ticker page onMounted/onUnmounted**(P-021)+ `?trial=1` 自动启动走 onLoad。⑥ **risk-disclosure scroll-to-bottom IO 经 $el**(P-019,sentinel ref 解析 `$el`,无 IO 时降级 scrolledToBottom=true)。⑦ **input 事件 `(e: Event)` + `detailVal` 桥接**(P-033,help/nex-v2-lock/repurchase/support-tickets 受控 input)。
- **导航目标(均真跳或 `fail:()=>{}` no-op)**:全部跳已建页真跳(本批后**全站 80 路由建齐,无 dangling**)。notifications 转化捷径→repurchase/team/staking/market/genesis-marketplace(真跳);replay 步骤→onboarding/intro·estimator·connect?replay=1(真跳);trial→wallet-cards·earn·devices(真跳);ref→register·login·trust(真跳);tx→wallet-bills(真跳)。
- **无新坑**:本批 0 新增 PITFALLS(全既有规则写时遵守:P-016 i18n grep / P-017 无返回注解 / P-019 $el / P-020 import type[WebkitMaskImage 大写修正] / P-021 page interval onUnmounted / P-022 alpha→color-mix / P-025 闭合[首写 support.vue 漏尾 `</script>` 即自检补,印证 P-025 仍是最易漏] / P-026·P-030 文本叶子 `<text>` / P-027 无 Set/Map state / P-033 input detailVal。**注释纪律 P-012/P-015**:首跑哨兵 `useEffect([wd])` 误命中 tracking 注释,已改中性"status watcher",二跑 11/11 绿)。**正向技法**:institutional façade/dev-tool 类页大量"假数据 + 外链 theatre"——外链统一 no-op/复制,dev 动作缺基建时 toast 说明而非静默;seed-空的 notifications store 仍迁全 CRUD 保 backend-replaceable;550/642 行大页按 SectionHeader/Stat/Row/DemoAction/ConfigRow 描述符式子组件拆分,单文件 < 800。
- **验证**:vue-tsc **0**;`bash scripts/verify.sh` **11/11 PASS**;**每新 .vue curl-compile** 20 页 + 17 组件 + 6 mock/store 全 `import…`/`const…`(**0 个 500/`<!DOCTYPE`**)。**迁移 COMPLETE**:73 main 路由 + ref/login/register/onboarding 全有 uni 页。

### 2026-06-13 persona walkthrough gate（FT-013/014/015）✅ runtime verified
- **范围**:`SPEC-L3c02-uniapp-persona-walkthroughs`。不是新增页面批次,而是把 Source B 剩余 persona 任务跑成常驻 gate:提现 KYC 后提交、NEX/USDT 兑换 + 回购、团队 commissions/rank/binary/leadership-pool 导航。
- **修复**:
  - `pages/me/wallet-withdraw.vue`:地址输入框加 `nx-withdraw-address-input`,并用 scoped `:deep(.nx-withdraw-address-input .uni-input-input)` 固定 inner input 高度,防地址输入焦点落回 amount。
  - `pages/me/wallet-withdraw.vue`:提现提交 CTA 加 `nx-withdraw-submit-cta`。
  - `pages/me/wallet-repurchase.vue`:回购 CTA 加 `nx-repurchase-submit-cta`。
  - `pages/team/team.vue` + `components/team/team-ledger-card.vue`:四个 team finance 入口加 `nx-team-commissions-link` / `nx-team-rank-link` / `nx-team-binary-link` / `nx-team-leadership-pool-link`。
- **proof 口径**:所有关键控件都从浏览器 selector 真实点击/填充,然后回源断言 route + body + storage。弹窗不再只看“出现”:exchange confirm modal 必须有 `NEX 10 -> USDT` 业务内容,提交后写 swap history、v3 cap、两条 bill。
- **运行时铁证**:`UNI_BASE_URL=http://localhost:5173 node ..\Nexion-admin-prototype\scripts\uniapp-persona-walkthrough-proof.mjs` 7/7 passed。FT-013:WD tracking + address + points 100→95 + withdraw bill；FT-014:swap/cap/2 bills + repurchase points 95→195 + 90d stake + stake bill；FT-015:四个 team finance route + business needles 全通过。
- **新坑**:P-048（UniApp H5 `<input>`/`<view @click>` 视觉存在不等于真实业务可操作）。已转 admin verify gate + 本表规则。

### 2026-06-13 feature-mapping walkthrough gate（FM-004/005/008/013/016）✅ runtime verified
- **范围**:`SPEC-L3c03-feature-mapping-walkthroughs`。把 Source C 剩余 feature-mapping walkthrough 全部跑成常驻 gate:top-up/KYC Express、staking 前台开仓+后台 APY、unilevel 筛选、i18n copy、feature/module switch。
- **修复**:
  - `pages/me/wallet-topup.vue`:KYC Express 入口加 hash query 回源同步 + `hashchange` 监听,并给生成地址/复制/付款确认/继续提现/top-up channel 补 `nx-*` 业务 selector。
  - `components/staking/vault-row.vue` + `components/staking/stake-sheet.vue`:staking row/sheet/amount/max/submit 补业务 selector,用于证明 30d 开仓会写 position + bill。
  - `pages/team/unilevel.vue`:how/filter/member row 补 `nx-unilevel-*` selector,用于证明 direct/extended 过滤和规则页。
  - `pages/me/language.vue`:语言行补 `nx-language-row-<code>` selector,用于证明 zh 切换后业务页 copy 变化。
- **proof 口径**:FM proof 不只看路由存在。top-up 要生成 KYC 检查和 bill;staking 要写 active position + bill;后台 APY/停售弹窗要有业务字段/业务影响 + reason + confirm + store/audit 持久;FM-016 前台还要证明 staking module row 可见且能打开业务 sheet。
- **运行时铁证**:`UNI_BASE_URL=http://localhost:5173 ADMIN_BASE_URL=http://localhost:3002 node ..\Nexion-admin-prototype\scripts\feature-mapping-walkthrough-proof.mjs` 7/7 passed。Source C operability audit:18/18 `provisionally-operable`, `needsTaskWalkthrough=0`。
- **新坑**:P-049（同页 hash query 切换不重触发 `onLoad`）。已转 admin verify gate + 本表规则。

### 2026-06-13 L5 final sweep / i18n mirror gate ✅ runtime verified
- **范围**:`SPEC-L5a01-final-sweep-gate`。UniApp 不再只靠本仓 `verify.sh` 单点自检,而是被 admin 侧 `scripts/l5-final-sweep.mjs --run-verifiers` 纳入三端终验总闸。
- **新增 gate**:
  - `scripts/i18n-key-mirror.mjs`:用 TypeScript AST 读取 `src/i18n/messages/en.ts` / `zh.ts`,递归比较 3779 个 key,发现任一 en/zh 漏键即 fail。
  - `scripts/verify.sh`:新增 `[1.5] i18n mirror`；同时补 `NODE_BIN` fallback 和 WSL `curl.exe` fallback,避免 PowerShell 调 WSL bash 时 `node` 找不到或 Linux curl 访问不到 Windows H5 dev server。
- **L5 口径**:UniApp route count=81; `uniapp-port-coverage` passed; persona proof rows=7; actionBad=0; meta leak 由 UniApp grep sentinel + Next interaction audit 共同守。
- **运行时铁证**:
  - `bash scripts/verify.sh` → **16 pass / 0 fail**。
  - `node ..\Nexion-admin-prototype\scripts\l5-final-sweep.mjs --run-verifiers` → UniApp verify **16 pass / 0 fail**,L5 **12/12 passed**。
- **已转**:i18n key 镜像从“tsc 间接抓”升级为显式 gate；Windows/WSL 混合验证必须选对 `node`/`curl` 执行端。

### app store 迁移分块策略（新 window 按序执行）
原 `lib/store/index.ts` 1039 行，建议拆分迁移：
1. **Device 类型体系** → `src/store/device-types.ts`：Device interface(baseRate/gpuUsage/gpuTemp/vramUsed/currentTask/recentTasks/battery/isCharging/thermalState/generation/status…) + DEVICE_SPECS + `createDevice(kind,id)` 工厂 + tasks 池 + `pickRandomTask`（原 index.ts 1-160）。
2. **app store state** → 扩展 `src/store/app.ts`：user(email/tier/usdtBalance/nexBalance/cumulativeDepositUsdt/referralCode…) + devices(initialDevices=[phone-1]) + earnings(today/todayNEX/thisWeek/thisMonth/total/history) + global(activeDevices/paidToday/nodes/countries/uptime) + latestWithdrawal（原 194-228）。保留已有 credit/debit。
3. **tick 模拟** → `tick(deltaMs)`：设备 earnings 累积(deviceTimers)+task rotation+lifecycle decay(device-lifecycle.ts)+充电/网络门控+aggregate today earnings（原 240-456，最复杂，是 mission-control live 数据核心，**勿简化**保真）。需先迁 `lib/store/device-lifecycle.ts`(getEfficiency/getLifecycleSummary 纯函数)。
4. **devices CRUD** → addDevice/activateDevice/deactivateDevice/scheduleDeactivation/recycleDevice/replaceDevice/moveToInventory/setPhoneRuntime/recordDeposit（原 459-840，store/me/devices 页用，可按页按需补）。
5. **SimulationProvider** → uni 版 tick 驱动（原 React Provider，uni 用 onShow/setInterval 或全局 tick；App.vue onLaunch 启动）。
6. 然后迁 **home** —— ⚠️**全项目最大单点**：`app/components/home/mission-control.tsx` **3157 行 + 25 内部组件**（…）+ 3 子组件(live-activity-feed/weekly-quest-hero/weekly-quest-list)。
   - **依赖 ~11 v3 store**：commission/v-rank/leadership-pool/market/genesis/points/free-trial/stella/profile/trial-config/trial-claim-sheet + 组件 CardStagger/TrialGhostSlot/TrialHeroBanner/StellaAvatar + app selectors(selectPhoneDevice/derivePromoUpgrade)。
   - **分解策略**：先迁 11 v3 store(Pinia) → 迁 4 组件 → 把 25 个 mission-control section 拆成独立 .vue 组件(src/components/home/*)逐个迁 → page 组装。**需多 window 系统攻坚**。app store 引擎(最核心依赖)✅ 已就绪。

- app store 引擎依赖纯函数(device-lifecycle/server-time/interrupt/tasks/ai-clients/receipt)直接 cp 零改。

## Batch 1 i18n key 教训（P-016）
迁移页面引用 `t.ns.key` 前**必须 grep 目标 namespace 实际 key 列表**，勿臆测（register invitedYou / login useOtp·usePassword·finish·errorInvalidCredentials·resetDone 全臆测错，被 vue-tsc 抓）。vue-tsc 是 i18n key 名的可靠门（property 不存在即报错）。

> **接续提示（home mission-control 攻坚中）**：
> **home ZONE-1 完整 7 section Playwright 实景验过** ✅(Greeting/TechMoney/TrialHero/TrialGhost/Conversion/DayOne/LiveFeed,0 err 双语)。补的基础:useNow/PulseDot/use-ticker · v5-ticket-*/hb-pulse(-success) keyframe · trial-hero.png 资源 · 10+ i18n 键(weeklyQuest 补 4/liveFeed 5/tech 6,en/zh 镜像)。index.vue 已组装 7 section。
> **下一步按序**:**ZONE 2**(QuickActionRow 1527/MyFleetSection 1594+DeviceRow 1646+PhoneTaskProgress 1770+AddDeviceRow 1854/OnGridSection 1930/NetworkPulseCard 2047)→ ZONE 3 StellaCardSlot 2985 → ZONE 4 VRank 1203/LeadershipPool 1421 → ZONE 5 DoTheMath 2358/EarningsLedger 2844 → ZONE 6 NexPrice 1335/MarketBoard 2190 → ZONE 7 TrustChipWall 2934。每 section 拆 `src/components/home/*.vue`(<800 行),按需补 i18n 键(源硬编码就补 t.* 双语镜像)。
> **mission-control.tsx section 行号图**(港时直接 offset 读):GreetingHeader 246✅ · DayOneQuestCard 306(「100%v5稿」) · ConversionBanner 704 · TechMoneyCard 978✅ · VRankCard 1203 · NexPriceCard 1335 · LeadershipPoolCard 1421 · QuickActionRow 1527 · MyFleetSection 1594(+DeviceRow 1646/PhoneTaskProgress 1770/AddDeviceRow 1854) · OnGridSection 1930 · NetworkPulseCard 2047 · MarketBoardCard 2190 · DoTheMathCard 2358(+MathBar 2508) · LiveFeedCard 2584 · EarningsLedgerCard 2844 · TrustChipWall 2934 · StellaCardSlot 2985/StellaCard 2996。helper:useTicker✅/useNow 85/HomeSparkline 94/PulseDot 139。组装顺序见 MissionControl() 198-242。
> **mission-control store 变量名对照**:`useApp`(devices/earnings.today) · `useCommission`(todayUSDT()) · `useVRank`(myRank)+nextRankProgress · `useLeadershipPool`(pool.mySharePct/myProjectedPayout/nextPayoutTs 带括号) · `useMarket`(change24hPct/klineHourly/nexPriceUSDT,marketCap computed 无括号) · `useGenesis`(soldSlots) · `usePoints`(points) · `useStella`(unread/open()) · `derivePromoUpgrade(app.devices)`/`selectPhoneDevice(app.devices)`(device-types.ts)。
> **port 要点**:① zustand selector `useApp((s)=>s.x)`→`const app=useApp()`+`computed(()=>app.x)`,method 带括号保持 function。② style 对象**必 `CSSProperties` 标注**(computed<CSSProperties>/`: CSSProperties`/fn 返回类型),否则 StyleValue TS2322。③ **DOM 测量(IO/getBoundingClientRect)必经 `$el`**(P-019)。④ 裸文本裹 `<text>`,div→view。⑤ 源硬编码英文→补 t.* 双语。
> **每迁一批 section**:写.vue → index.vue 引入 → verify.sh(tsc+哨兵 9/9)→ **Playwright navigate :5173 + console=0 + 截图字段级对原型**(P-019 印证:必实景,verify 绿≠client OK)→ 清截图 → 更新本表。pages.json 没增页则免重启(HMR 处理 index.vue + 新组件)。

## Batch 1 经验（intro 样板沉淀）
- 主题：nexion 默认 **dark**（App.vue onLaunch H5 设 `data-theme=dark`）。onboarding 全屏页不用 chassis。
- SVG/SMIL/framer-motion→CSS 全可用（P-013）。pages.json 增页必重启 dev（P-011）。命令必 cd（P-010）。
- 入场动画：`@keyframes` + `animation: ... delay both`（替 framer-motion initial/animate/transition）。

<!-- 每页迁移后：改状态 + 跑 verify.sh + 更新此表 -->
